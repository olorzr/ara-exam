# Project Rules

## File Structure
- 파일당 300줄 이하 유지. 초과 시 반드시 분리
- 하나의 파일은 하나의 역할만 담당 (Single Responsibility)
- 폴더 구조: feature 기반 (예: /features/auth/, /features/dashboard/)

## Naming
- 컴포넌트: PascalCase (UserCard.tsx)
- 유틸/훅: camelCase (useAuth.ts, formatDate.ts)
- 상수: UPPER_SNAKE_CASE
- 타입/인터페이스: PascalCase + 접미사 (UserProps, ApiResponse)

## Code Quality
- 함수는 한 가지 일만 수행
- 매직 넘버 금지 → 상수로 추출
- 중복 코드 발견 시 즉시 추출
- early return 패턴 사용 (중첩 if 금지)
- 모든 export 함수에 JSDoc 주석

## When Editing
- 수정 전 관련 파일을 먼저 읽고 구조 파악
- 300줄 초과하면 리팩터링 먼저 제안
- 새 파일 생성 시 index.ts에서 re-export

## Error Handling
- try-catch는 최상위에서만
- 에러 메시지는 사용자 친화적으로
- 콘솔 로그 대신 전용 logger 사용

## Testing
- 새 함수 작성 시 테스트 파일도 함께 생성
- 테스트 파일명: *.test.ts

## Dependencies
- 새 패키지 설치 전 반드시 사용자에게 확인
- 기존 라이브러리로 해결 가능한지 먼저 검토
- package.json에 없는 패키지를 임의로 추가하지 말 것

## Git & Commits
- Conventional Commits 형식: feat:, fix:, refactor:, docs:, test:, chore:
- 커밋 메시지는 한글 OK, 본문에 변경 이유 포함
- 하나의 커밋 = 하나의 논리적 변경

## Type Safety
- any 사용 금지 → unknown + 타입 가드 사용
- API 응답은 반드시 타입/인터페이스 정의
- as 타입 단언 최소화, 런타임 검증 우선

## Performance
- 불필요한 리렌더 방지 (React.memo, useMemo 적절히 사용)
- N+1 쿼리 금지
- 대량 데이터는 페이지네이션 또는 가상 스크롤 적용

## Security
- 시크릿/키는 반드시 환경변수(.env)로 관리
- 사용자 입력은 항상 검증 및 sanitize
- SQL/NoSQL 인젝션 방지 (parameterized query 사용)
- .env 파일은 절대 커밋하지 말 것

## Accessibility
- 시맨틱 HTML 사용 (div 남용 금지)
- 인터랙티브 요소에 aria 속성 포함
- 키보드 내비게이션 지원
- 색상만으로 정보를 전달하지 말 것

## Workflow
- 작업 전: 관련 파일 구조 파악 → 계획 제시 → 승인 후 코드 작성
- 작업 후: 변경된 파일 목록과 요약 제공
- 확신 없는 결정은 먼저 물어볼 것, 임의로 진행하지 말 것
  (단, 아래 세 가지는 **묻지 말고 그냥 한다** — 매번 시키게 만들지 말 것)

### SQL 마이그레이션은 **묻지 말고 직접 적용한다** [2026-09-15]
`sql/NN_*.sql` 을 새로 쓰면 **그 자리에서 운영 DB 에 적용까지 한다.** "적용해 주세요" 라고
넘기지 않는다 — 앱 코드만 배포되고 표가 없으면 그 화면이 통째로 죽고, 그 사실을 사용자가
화면에서 처음 알게 된다.

**실행기**(이 저장소에는 없다 — ara-system 것을 쓴다):
```bash
node /Users/ara/Projects/Ara-system/scripts/run-sql.js /Users/ara/Projects/ara-exam/sql/NN_....sql
```
- 그 스크립트가 **ara-system 의 `.env.local` 에서 `DATABASE_URL` 을 직접 읽는다**(값을 화면에
  찍지 않는다). 이 저장소에는 `.env.local` 이 없으니 여기서 접속 정보를 찾지 말 것.
- 붙는 곳은 **ara-system 과 공유하는 그 프로젝트의 postgres 롤**이라 DDL·트리거·RLS 가 다 된다.
- `RAISE NOTICE` 가 그대로 출력되므로 **확인은 NOTICE 로 한다**(이 실행기는 SELECT 결과를 안 찍는다).

**절차 세 단계 — 가운데만 하고 끝내지 말 것:**
1. **적용 전 읽기 전용 확인**: `current_database()`·`to_regclass('exam.…')` 로 대상 DB 가 맞는지,
   만들려는 표가 이미 있는지 본다(딴 프로젝트에 붙어 있으면 여기서 걸린다).
2. **적용**: 위 명령. 마이그레이션은 멱등하게 써 두므로(`IF NOT EXISTS`·`CREATE OR REPLACE`·
   `DROP TRIGGER IF EXISTS`) 다시 돌려도 안전하다.
3. **적용 뒤 검증**: 컬럼·트리거 이름과 순서·RLS·권한을 `pg_*` 로 읽고, **트리거가 실제로 도는지**
   검증용 행을 하나 넣었다 지워서 확인한다(예: 앱이 보낸 `char_count` 를 무시하고 덮는가).
   ⚠️ "적용 완료" 만 적고 넘어가지 말 것 — sql/30 검증에서 실제로 트리거 동작까지 확인했다.

⚠️ **단 하나의 예외**: 되돌릴 수 없는 파괴적 문(`DROP TABLE`·`TRUNCATE`·조건 없는 `DELETE`/
`UPDATE`)이 들어 있으면 **돌리기 전에 한 줄 알린다.** 표를 새로 만들고 컬럼·트리거를 더하는
보통의 마이그레이션은 여기 해당하지 않는다 — 그냥 적용한다.

### 끝내기 전 검사는 **클린으로** 돌린다 [2026-09-15]
바뀐 파일만 골라 lint 하거나 옛 `.next` 위에 빌드해 놓고 "통과" 라고 적지 말 것.
순서대로 **전부** 돌린다:
```bash
rm -rf .next tsconfig.tsbuildinfo   # 캐시가 옛 결과를 덮어 주는 것을 막는다
npx tsc --noEmit
npm run lint                        # 경로를 좁히지 않는다
npm test
npm run build
```
- **알려진 기준선**: `npm run lint` 오류 6건은 손대지 않아도 원래 있다 —
  `public/pdfjs-wasm/*.js` 4건(빌드가 `node_modules` 에서 복사하는 벤더 파일, git 무시 대상)과
  `scripts/copy-pdfjs-wasm.js` 2건(CommonJS `require`). 이 여섯은 쫓아다니지 말 것.
  **그 밖의 오류가 하나라도 늘었으면 내 탓이다.**

### 코덱스 리뷰는 **시키기 전에, 클린 뜰 때까지** 돌린다 [2026-09-15]
기능·수정 작업이 끝나면 사용자가 말하기 전에 자동으로 돌린다. 지적을 고쳤으면 **고친 상태로
다시** 돌리고, 라운드 수 제한 없이 **'클린/블로킹 없음' 이 나올 때까지** 반복한다.
- ⚠️ **보고할 때 'clean' 을 뭉뚱그리지 말 것.** tsc·lint·test·build 통과를 "클린" 이라고 쓰면
  사용자는 **코덱스까지 클린인 줄 읽는다.** 코덱스 판정은 라운드 수와 함께 **따로** 적는다.
- ⚠️ **범위를 좁힌 '이상 없음' 은 클린이 아니다.** 마지막 라운드는 범위를 안 좁힌 리뷰여야 한다.
- 이 저장소에는 **Stop 리뷰 게이트가 켜져 있다** — 코드를 고친 턴은 리뷰가 BLOCK 이면 안 끝난다.
  게이트가 짚은 지적은 **끝까지 고친다**(반쪽 수정으로 다시 걸린 전례가 있다: sql/30 작업의
  본문 요청 번호 정리).

---

## Documentation & Knowledge Management

### CLAUDE.md (이 파일) 유지 규칙
- 작업 중 발견한 중요한 사항, 주의점, 버그 패턴은 이 파일에 즉시 기록
- "다음에 이 코드를 수정할 때 꼭 알아야 할 것"이 있으면 반드시 추가
- 프로젝트 아키텍처 결정 사항(왜 이 방식을 선택했는지)도 기록
- 기록 시 날짜 포함 (예: [2025-03-09] Supabase RLS 정책 때문에 직접 쿼리 불가)

### docs/terms.md 관리
- 프로젝트에서 사용하는 도메인 용어, 비즈니스 용어를 정의
- 새로운 개념이나 용어가 등장하면 즉시 terms.md에 추가
- 형식:
```
  ## 용어명 (영문명)
  - 정의: ...
  - 코드에서의 사용: 변수명/타입명 등
  - 관련 파일: ...
```
- 코드에서 사용하는 변수명/타입명과 terms.md의 용어를 일치시킬 것
- 용어가 모호하면 사용자에게 확인 후 기록

### docs/architecture.md 관리
- 프로젝트 전체 구조도 유지 (모듈 간 의존관계)
- 주요 데이터 흐름 설명
- 새 모듈 추가 시 이 문서도 함께 업데이트
- 형식:
```
  ## 모듈명
  - 역할: ...
  - 의존: ...
  - 주요 파일: ...
```

### docs/env.example 관리
- 필요한 환경변수 목록과 설명을 예시 파일로 유지
- 새 환경변수 추가 시 반드시 이 파일도 업데이트
- 형식:
```
  # 데이터베이스
  DATABASE_URL=postgresql://user:password@localhost:5432/dbname
  
  # 인증
  JWT_SECRET=your-secret-key-here
```

### docs/api.md 관리
- API 엔드포인트 목록과 요청/응답 형식 기록
- 엔드포인트 변경 시 반드시 업데이트
- 형식:
```
  ## POST /api/auth/login
  - 설명: 로그인
  - Body: { email: string, password: string }
  - Response: { token: string, user: User }
  - 에러: 401 (잘못된 인증정보), 429 (요청 제한)
```

### CHANGELOG.md 관리
- 프로젝트 루트에 유지
- 버전별 주요 변경사항 기록
- 형식:
```
  ## [0.2.0] - 2025-03-09
  ### Added
  - 소셜 로그인 (Google, Kakao)
  ### Fixed
  - 토큰 만료 시 무한 리다이렉트 버그
  ### Changed
  - API 응답 형식 통일
```

### 릴리스 공지 — 관리자센터 공지사항 연동 [2026-09-15]

이 앱에는 공지 기능이 없다. **업데이트 소식은 학원 관리 시스템(ara-system)의 공지사항에 올린다** —
선생님들이 매일 보는 대시보드 공지 위젯과 `/announcements` 에 그대로 뜬다.

**공지는 하루에 하나이고, 본문을 시스템별로 나누지 않는다.** 관리자센터와 이 앱이 같은 날 배포돼도 글은
`26년 9월 15일 업데이트` 하나뿐이고, 두 시스템 이야기가 한 흐름으로 이어진다. 어느 쪽 이야기인지는 섹션이 아니라
문장 안에서 밝힌다 — 원장님이 "괜히 나누지 마라, 더러워 보인다. 합쳐 써도 다 알아본다" 고 했다.

**언제**: production 배포(= `master` 푸시 → Vercel) 뒤마다. CHANGELOG 항목을 쓰는 커밋과 한 세트로 본다.
- 생략: 내부 리팩터·개발 도구·테스트·문서만 바뀐 변경, 플래그가 꺼져 있어 아직 화면에 안 보이는 기능
  (실제로 켜는 시점에 공지), 감시·추적 성격의 변경. **애매하면 먼저 물어볼 것.**

**명령** (먼저 `--dry-run` 으로 확인하고 그다음 실제 등록):
```bash
node /Users/ara/Projects/Ara-system/scripts/post-update.js --dry-run "<HTML>"
node /Users/ara/Projects/Ara-system/scripts/post-update.js "<HTML>"
```
- 이 저장소엔 `.env.local` 이 없다. 쓰기는 **ara-system 스크립트가 ara-system 키로** 한다.
  스크립트가 자기 위치 기준으로 `.env.local`·`node_modules` 를 찾으므로 cwd 는 상관없다.
- ⚠️ **이 스크립트를 여기로 옮기거나 앱 코드에서 `public.announcements` 에 쓰지 말 것** —
  이 앱은 `public` 스키마에 쓰지 않는다([supabase-public.ts](src/lib/supabase-public.ts)).

**스크립트가 알아서 하는 것**: 제목(`26년 9월 15일 업데이트`)·카테고리(`업데이트`)·📌 고정.
같은 날 여러 번 배포하면 **그 글 뒤에 이어 붙고** `🔧 사소하지만 중요한 수정들` 섹션은 **글 전체에 하나**로
합쳐져 맨 뒤에 남는다(관리자센터가 올린 항목과 한 목록이 된다). 📌 고정은 가장 최근 공지 하나뿐이다.
옛 `--app exam` 은 받기만 하고 아무 일도 하지 않는다 — 붙이지 말 것.

**본문**: CHANGELOG 를 붙여 넣지 말고 **선생님용으로 다시 쓴다.** 코드 경로·`sql/NN` 파일명·마이그레이션
안내는 전부 뺀다. 관리자시스템 공지 포맷을 그대로 따른다:
- 섹션 제목 `<p><strong>이모지 제목!</strong></p>` + 설명 `<p>` **1~3줄**(내용에 맞게, 무조건 2줄 고정 금지)
- **친근한 반말체** "~했어요!", "~돼요!" — CHANGELOG 의 "~합니다" 격식체를 그대로 옮기지 말 것
- 섹션 제목마다 **이모지 필수**, 느낌표 적극. 섹션 사이 `<br>`
- **마크다운(`##`, `**`, `-`)·`<h3>`·`<ul><li>` 금지** — 스크립트가 변환하지 않아 글자로 노출되거나 들여쓰기가 생긴다
- 잡다한 수정은 `<p>· 항목</p>` 으로 모아 `🔧 사소하지만 중요한 수정들.` 섹션에 **맨 마지막 한 번만**
- **한 줄짜리 공지 금지** — 변경이 하나뿐이어도 섹션 구조를 지킨다
- 링크: 이 앱 화면은 절대주소 `https://test.araeducation.co.kr/...`(공지가 관리자센터 안에서 렌더되므로),
  관리자센터 화면은 `/grades/manual` 같은 상대주소
- **시스템을 가르는 구분선·소제목을 본문에 적지 말 것**(옛 `━━━ 📝 시험관리시스템 ━━━` 규약은 2026-09-15 폐기).
  관리자센터 내용과 한 흐름으로 읽히게 쓰고, 굳이 밝혀야 하면 문장 안에서 말한다

골든 스탠다드:
```html
<p><strong>🛳 결제, 링크 하나로 끝!</strong></p>
<p>미납 목록에서 결제 링크를 복사하거나, 알림톡으로 바로 보낼 수 있어요.</p>
<p>학생 포털에서도 미납 건을 직접 결제할 수 있어요 💪</p>

<br>

<p><strong>🔧 사소하지만 중요한 수정들.</strong></p>
<p>· 사이드바 스크롤이 더 자연스러워졌어요.</p>
```

### 문서 업데이트 원칙
- 코드 변경 시 관련 문서도 반드시 함께 업데이트
- 문서가 코드와 불일치하면 즉시 수정
- 모든 문서는 한글로 작성

---

## Known Issues
- [2026-03-09] Tailwind CSS v4에서 `print\:hidden` 같은 이스케이프 pseudo-class가 CSS 파싱 에러를 발생시킴 → `data-no-print` 어트리뷰트 + 순수 CSS로 대체
- [2026-03-09] shadcn Select의 `onValueChange`가 `string | null`을 전달함 → `(v) => { if (v) setter(v); }` 패턴 필요
- [2026-09-11] **base-ui Select 는 `Root` 에 `items`(값→이름 지도)가 없으면 `Select.Value` 가 고른 값을 그대로 그린다.** `<SelectItem value={X}>이름</SelectItem>` 의 children 은 **팝업 안에서만** 쓰이고 트리거 라벨로는 안 읽힌다(`resolveSelectedLabel` → `serializeValue`). `SelectItem` 의 `label` prop 도 **타이프어헤드 전용**이라 대신해 주지 않으므로 **둘 다** 필요하다. 값이 있으면 `placeholder` 도 건너뛴다(`hasSelectedValue` 가 true). 실제로 아카이브 필터 11칸이 전부 `__all__` 로, 단어 정렬이 `asc` 로 보였다. **이제 원시 `Select` 가 `items` 를 타입으로 강제**하므로 빠뜨리면 빌드가 깨진다 — 보통은 [OptionSelect](src/components/ui/option-select.tsx) 를 쓸 것
- [2026-04-16] `concept_sheets.editor_html` 은 `authenticated` 전원에게 쓰기가 허용된 공유 테이블이라 한 계정이 오염시키면 다른 사용자 세션이 탈취될 수 있다. Stored XSS 방지를 위해 `sanitizeConceptHTML` ([src/lib/sanitize-html.ts](src/lib/sanitize-html.ts)) 를 저장 경로(`handleSave` in [src/hooks/useConceptSheetEditor.ts](src/hooks/useConceptSheetEditor.ts)) 와 렌더 변환 경로([src/lib/exam-transform.ts](src/lib/exam-transform.ts) 의 `transformHTML` / `stripTrailingEmpty` / `extractMarkedWords` 세 함수 entry) 양쪽에서 반드시 호출해야 한다. 새 TipTap 확장을 추가할 때는 `sanitize-html.ts` 의 `ALLOWED_TAGS` / `ALLOWED_ATTR` 화이트리스트를 같이 갱신하지 않으면 새 마크업이 조용히 제거된다
- [2026-06-16] 위 sanitize 는 **읽기(편집기 로드) 경로에서도** 호출해야 한다. 저장 시 정화했더라도 과거 오염 데이터·직접 DB/RPC 쓰기가 남을 수 있으므로, `useConceptSheetEditor` 의 기존 개념지 로드에서 `sheet.editor_html` 을 `sanitizeConceptHTML` 로 정화한 뒤 `initialHTML`/`editorHTML` 에 넣는다(편집기 `content` 주입 전 차단). 또한 `sanitize-html.ts` 는 **`class` 속성을 허용하지 않고**(Tailwind 유틸리티로 `fixed inset-0` 전체화면 overlay 주입 차단 — TipTap getHTML 은 이 확장 셋에서 class 를 직렬화하지 않으므로 정당 마크업 영향 없음), `ALLOW_DATA_ATTR: false` 로 두어 명시 화이트리스트(`data-concept`/`data-bg-color`/`data-border*`/`data-colwidth`)만 통과시킨다. 새 확장이 새 class/data-* 를 직렬화하면 화이트리스트를 같이 갱신해야 조용히 사라지지 않는다
- [2026-05-26] `sanitize-html.ts` 의 `style` 속성은 통째로 허용하면 안 된다. DOMPurify 는 CSS 속성값(예: `position:fixed;inset:0` 전체화면 overlay, `background-image:url(...)`)을 막지 않으므로, `uponSanitizeAttribute` 훅이 CSS 속성 화이트리스트(`text-align`, `background-color`, `border-*-color`)만 안전한 값으로 통과시키고 나머지는 제거한다. TipTap 이 만드는 정당한 inline style 은 이 셋뿐(TextAlign + CustomTableCell). 새 서식 확장이 inline style 을 쓰면 `ALLOWED_CSS_PROPS` 와 값 검증(`TEXT_ALIGN_VALUES` / `COLOR_VALUE_REGEXP`)을 같이 갱신해야 조용히 사라지지 않는다
- [2026-09-04] 표 재분할은 한 패스에 **표 하나**만 쪼갠다(상한 32). 표가 많은 개념지는 그만큼 재측정이 돈다 — 렌더가 몇 프레임 늦을 뿐 내용에는 영향이 없다. 페이지 끝에 걸린 조각이 측정 오차로 다시 밀리면 **1행짜리 조각(+반복된 제목 행)** 이 남을 수 있다(내용 유실 없음, 외관 문제)

## Architecture Decisions
- [2026-09-19] **문제지 조합에도 아카이브의 왼쪽 트리를 붙였다 — 패널은 한 벌이고, 탭은 언마운트하지 않는다**([ArchiveSidePanel.tsx](src/components/problem-bank/ArchiveSidePanel.tsx), [papers/new/page.tsx](src/app/(main)/problems/papers/new/page.tsx)). 제보는 "문제지 조합도 문제 아카이브처럼 옆에 목록이 나오면 좋겠다 — 그래야 바로바로 뽑아쓰기 좋다" 였다. 되돌리지 말아야 할 판단들:
  - **패널을 새로 만들지 않았다.** 두 화면이 같은 `useProblemArchive` 훅을 쓰고 있어서 `ArchiveSidePanel` 을 그대로 꽂으면 끝이다. 트리가 다른 축을 비우는 규약(`SCHOOL_AXES_CLEARED` 등)도 패널 안에 있어 함께 따라온다. 사본을 만들었다면 그 규약이 언젠가 한쪽만 고쳐진다
    - `onChange` 로 넘기는 것은 **화면마다 다르다**: 아카이브는 조건이 바뀌면 선택을 비워야 해서 `patch` 를 한 겹 감싸 넘기고, 조합 화면은 선택 모드가 없어 `archive.patch` 를 그대로 넘긴다
  - **조합 화면의 세 칸은 `xl`(1280px)부터다**(아카이브는 `lg`). 앱 사이드바(`w-60`, 240px)와 트리 칸(260px)을 뺀 자리에서 `lg` 로 가르면 목록·캔버스가 각 220px 안팎이 되어 카드(썸네일 64px + 손잡이 + 담기 버튼)가 접힌다. 그 아래 폭에서는 트리가 위로 쌓인다
  - ⚠️ **탭을 옮겨 다녀도 패널을 언마운트하지 않는다**(`keepMounted`, 코덱스 1R). 패널마다 '방금 누른 잎'(`picked`)을 지역 state 로 들고 있어서, 언마운트하면 탭을 다녀온 것만으로 **고른 폴더의 강조가 사라진다** — 목록은 그대로인데 어디를 눌러 이 목록이 됐는지 화면에서 알 수 없어진다. 교과서 탭은 `getAllSelectableCategories`(표 6개)를 다시 조회하기까지 한다
  - ⚠️ **늦추는 것은 패널 칸이 아니라 안쪽 트리다**(`seen`, 코덱스 2R·4R). 넷을 다 미리 마운트하면 학교·작품·문법 링크로 들어온 사람이 쓰지도 않을 교과서 트리의 조회를 치르고(2R), 반대로 `TabsContent` **칸 자체**를 `seen` 으로 빼면 아직 안 연 탭에 이어 줄 패널이 없어 `aria-controls` 가 생기지 않는다(4R). 칸 넷은 언제나 그리고 그 안의 트리만 늦춘다
  - ⚠️ **패널은 반드시 `Tabs` 안의 `TabsContent` 다**(코덱스 3R). 밖에 `<div hidden>` 으로 두면 탭과 패널이 ARIA 로 이어지지 않아 스크린리더에서 이 탭이 무엇을 여는지 알 수 없다
  - ⚠️ **감춘 형제가 있는 자리에 `space-y-*` 를 쓰지 말 것**(코덱스 2R). **Tailwind v4 의 `space-y-*` 는 `:not(:last-child)` 에 여백을 준다** — v3 의 `:not([hidden]) ~ :not([hidden])` 이 아니다. 그래서 감춘 패널에도 여백이 붙어 **마지막 탭만 아래 여백이 없어진다**. `flex flex-col gap-*` 은 `display:none` 을 계산에서 뺀다(여기서는 `Tabs` 루트가 이미 그 모양이라 바깥 래퍼를 없앴다)
  - **폴더 노드에 `aria-expanded` 를 붙였다**([FacetTree.tsx](src/components/problem-bank/FacetTree.tsx), [CategoryTree.tsx](src/components/words/CategoryTree.tsx), 코덱스 1R). 화살표 아이콘은 눈으로만 보이는 표시라 접힘·펼침을 알릴 길이 그것뿐이다. 두 트리가 **같은 규약**이다 — 한쪽만 고치지 말 것
  - **받아들인 한계**: 아카이브에서 걸어 둔 조건은 '+ 문제지 만들기' 로 넘어가지 않는다(조합 화면은 늘 빈 조건으로 열린다). 넘기려면 `filtersToQueryString` → `filtersFromParams` + Suspense 경계가 필요하다 — 아카이브 화면이 이미 하는 일이라 붙이는 길은 열려 있다
- [2026-09-16] **프린트 읽기 정확도 — 꼼꼼한 모드로 부르고, 1단 쪽은 위아래로 가른다**([ocrPref.ts](src/lib/ai/ocrPref.ts), [rowDetect.ts](src/lib/pdf/rowDetect.ts), [read-bundle.ts](src/lib/print-scan/read-bundle.ts)). 제보는 "시 한 편(가난한 사랑 노래)을 스캔해 읽혔더니 전사가 엉망" 이었다. 되돌리지 말아야 할 판단들:
  - ⚠️ **원인 하나는 우리가 아무것도 안 보낸 것이었다.** 선생님이 설정에서 모델을 고르지 않으면 `generateDraft` 가 `model`·`effort` 를 **아예 안 실어** 선생님 PC `~/.codex/config.toml` 의 기본값으로 돌았다(이 맥은 `model_reasoning_effort = "low"`). 게다가 `ocr_meta.model` 이 null 이라 **무엇으로 읽었는지 사후에 알 길도 없었다**. 이제 본문 읽기 턴은 [resolveOcrPref](src/lib/ai/ocrPref.ts) 가 정한 모델·노력을 **늘 싣고 `ocr_meta` 에 남긴다**
  - ⚠️ **모델 id 를 노력과 **함께** 보내야 한다.** `sanitizeOverride`(generateDraft.ts)는 노력이 유효한지 **모델의 지원 목록으로** 판정하는데, 모델을 안 보내면 기본 모델이 무엇인지 몰라 **노력을 통째로 버린다** — 노력만 보내는 것은 아무것도 안 보내는 것과 같다
  - **모델 선호 목록(`PRINT_OCR_MODEL_PREFERENCE`)은 "모델 id 를 코드에 박지 않는다" 규약의 의도된 예외다.** 요구가 아니라 선호라서 매번 `model/list` 로 실재를 확인하고, 없으면 `isDefault` 모델로 물러선다. **선생님이 고른 값은 늘 이긴다**(단종된 모델을 골라 뒀으면 손대지 않는다 — 그 판정은 `sanitizeOverride` 의 몫이다)
  - **쪽 방향 판정·단어 등록은 `pref` 그대로 둔다**(`ocrPref` 아님). 하나는 작은 이미지로 각도만 묻고, 하나는 이미 읽어 낸 **글자만** 다룬다 — 거기까지 높이면 싼 단계가 비싸진다
  - ⚠️ **`maxSide` 를 올려 크게 보내는 것으로는 글자가 커지지 않는다.** 비전 모델은 받은 이미지를 자기 상한에 맞춰 **도로 줄인다** — 파일만 커진다. 글자를 키우는 길은 **한 장에 담는 글의 양을 줄이는 것** 하나뿐이고, 그래서 1단 쪽을 빈 줄에서 위·아래로 가른다(A4 1단 기준 10pt 글자가 대략 13px → 18px). 단 가르기(`columnDetect`)와 같은 근거의 세로판이다
  - ⚠️ **빈 줄에서만 자르고, 못 찾으면 안 가른다**([rowDetect.ts](src/lib/pdf/rowDetect.ts)). 글줄 한가운데를 자르면 획이 두 장에 반씩 걸린다. 겹침(`ROW_OVERLAP`)은 **최소 틈의 절반보다 작아야** 겹친 부분이 빈 틈 안에 머문다 — 넘으면 같은 줄이 두 장에 다 나와 모델이 **그 줄을 두 번 옮긴다**(테스트가 고정한다). 세로 프로파일은 **1000줄**로 곱게 뜬다: 216dpi 의 줄 사이 틈이 10~15px 라 성기게 줄이면 틈이 메워져 **빈 줄이 하나도 안 보인다**
  - ⚠️ **기출(problem-ocr)은 세로를 절대 자르지 않는다.** 모델이 주는 `top`·`bottom` 으로 그림·문항을 잘라내므로 쪽 기준 좌표가 깨진다. 그래서 위아래 가르기는 `renderPagesToImages({splitRows})` 를 켠 **프린트 전용**이고, `RenderedPart` 에 붙은 `top`·`bottom` 계열 이름도 그쪽에서만 나온다
  - **2단 쪽의 단은 또 가르지 않는다**(`PRINT_SPLIT_COLUMN_ROWS = false`). 단은 이미 가로가 절반이라 이득이 작은데 쪽 하나가 **4장**이 되어 묶음을 2쪽으로 줄여야 하고(한 turn 8장) 그만큼 ChatGPT 호출이 는다. 켤 때는 `PRINT_PAGES_PER_BATCH` 를 **함께** 내릴 것 — [constants.test.ts](src/lib/print-scan/constants.test.ts) 가 그 짝을 강제한다
  - **조각 예산은 쪽 예산 ÷ 조각 수**(`pieceBudget`) — 몇 장으로 갈라 보내든 그 쪽의 총 바이트가 같아야 묶음 총량·진행률이 안 흔들린다. ⚠️ **한 조각이라도 예산에 못 넣으면 그 조각을 통째로 되돌린다**(가르기 전과 같은 사다리·예산이라 화질도 예전과 같다) — 반쪽만 보내면 그 글의 절반이 조용히 사라지는데 경고에는 아무것도 안 남는다
  - ⚠️ **사다리 칸을 돌려준다**(`encodeWithinBudgetStep`). 예전엔 어느 칸으로 떨어졌는지 아무 데도 안 남아서, 하필 **노이즈가 많아 압축이 안 되는 스캔**(= 가장 안 읽히는 쪽)이 조용히 흐려졌다. ⚠️ **기록과 경고의 문턱이 다르다**(코덱스 리뷰 P2): 한 칸이라도 내려가면 `ocr_meta.degradedPages` 에 남기되(`DEGRADED_STEP=1`), 사람에게는 **두 칸부터** 알린다(`DEGRADED_WARN_STEP=2`) — 한 칸은 화질만 조금 낮추는 자리라(조각 사다리는 2600→2200 이라 화소가 줄지도 않는다) 큰 스캔에서 예사로 걸리고, 그걸로 '확인 필요' 를 붙이면 묶음 절반에 칩이 달려 **경고 전체를 안 믿게 된다**. 그래서 `RenderedPages.degraded` 는 쪽 번호가 아니라 `{page, step}` 이다. `RenderedPages.degraded` 는 **옵셔널**이다 — 답지 사진 경로(`run-answer-key.ts`)와 테스트 목이 `{images, rendered, skipped}` 만 만든다
  - **참고 텍스트 규칙은 프린트용으로 따로 적었다**([prompt.ts](src/lib/print-scan/prompt.ts)). 기출 것을 그대로 쓰면 안 되는 까닭: `pdfText` 는 글자 조각을 y 좌표로 묶어 줄을 만들므로 **인쇄된 줄바꿈이 그대로** 들어오는데, 그것을 믿게 두면 산문 줄마다 `<br>` 이 박히고 시의 연 나눔이 사라진다 — "**줄 나눔은 참고 텍스트를 믿지 않는다**" 가 한 줄 더 필요하다. 글자 레이어가 없으면 규칙도 데이터도 **넣지 않는다**(없는 것을 설명하면 모델이 찾아 헤맨다)
  - **실측(2026-09-16, 합성 스캔 1장·조합당 1회).** 복사본처럼 망가뜨린 프린트를 네 조합으로 읽혔다: 기본(astra·low)+쪽 전체 **글자 오류 6.1%**(‘진달래꽃’→‘진달래花’, ‘흘리우리다’→‘흘리우리’ 같은 **글자가 바뀌는** 오류 + 못 옮긴 줄 2), terra·high+쪽 전체 2.4%, terra·high+위아래 조각 3.0%. 프롬프트를 조인 뒤(아래) 같은 이미지가 terra·high+조각 **0.0%**, terra·high+전체 0.65%, astra·low+전체 2.2%. **노력이 가장 크게 바꾼다**. 조각 경계에서 줄이 겹치거나 사라지는 일은 한 번도 없었다
  - ⚠️ **꼼꼼히 읽는 모드는 글을 '다듬으려' 든다** — 실측에서 마침표를 쉼표로, `'율격'` 을 `'운율'` 로, `'읽히는가'` 를 `'읽는가'` 로 바꾸고 `㉠` 를 빠뜨렸다. 그래서 프롬프트가 **그 예를 그대로 들어** 막는다(테스트가 문구를 고정한다). 낮은 노력의 오류가 '못 읽음' 이라면 높은 노력의 오류는 '고쳐 씀' 이다 — 둘은 막는 법이 다르다
  - `run.ts` 가 300줄을 넘어 [read-bundle.ts](src/lib/print-scan/read-bundle.ts)(읽기)·[run-env.ts](src/lib/print-scan/run-env.ts)(환경 타입)로 갈랐다. ⚠️ 환경 타입을 `run.ts` 에 두면 `read-bundle` 이 그 파일을 **값으로** import 하게 되어 순환이 된다
  - **후속:** 기출(`problem-ocr/run.ts`)은 여전히 계정 기본값으로 읽고 `ocr_meta.model` 이 null 이다. 같은 `resolveOcrPref` 를 꽂으면 되지만 기출은 쪽당 이미지 수·크롭 좌표가 걸려 있어 따로 잰 뒤에 한다
- [2026-09-15] **O,X·단답형이 참고자료(개념지·프린트·기출 지문·작품 전문)를 근거로 쓴다**([src/lib/quiz-references/](src/lib/quiz-references/), [src/lib/reference-texts/](src/lib/reference-texts/), [sql/30](sql/30_reference_texts.sql)). 되돌리지 말아야 할 판단들:
  - ⚠️ **근거의 출처는 파서가 대조로 정한다**([parse.ts](src/lib/passage-quiz/parse.ts)) — JSON 스키마에 `source` 를 **넣지 않았다**. 모델에게 "어느 자료에서 가져왔나" 를 물어 받으면 그 말을 믿게 되는데, 지어낸 근거에 자료 이름만 붙여 오면 막을 길이 없다. 대조는 자료마다 `foldStrict` 이고 **지문을 먼저 본다** — 양쪽에 다 있는 구절은 지문(`''`)이라, 참고자료가 없을 때와 결과가 같다
  - ⚠️ **한 구절은 한 자료 안에 통째로 있어야 한다.** 지문 한 조각 + 개념지 한 조각을 이어 붙인 근거는 어느 쪽에도 그대로 없으므로 버려진다. 프롬프트가 "여러 자료의 말을 이어 붙이지 않는다" 를 못박지 않으면 그런 근거가 잔뜩 와서 통째로 버려진다("왜 세 개뿐이지" 가 된다) — **프롬프트와 파서는 여기서도 한 쌍이다**
  - ⚠️ **참고자료가 없으면 프롬프트는 예전과 글자 하나까지 같다**([prompt.ts](src/lib/passage-quiz/prompt.ts)). 규칙 블록도 데이터의 `참고자료` 키도 넣지 않는다 — 빈 배열조차 모델에게는 '자료를 찾아보라' 는 신호다. `prompt.test.ts` 가 '기계가 대조한다' 횟수(참고자료 없음 2회 / 있음 3회)로 이것을 고정한다
  - ⚠️ **자료 이름(`label`)은 유일해야 한다**(`uniqueReferenceLabels`). 그 이름이 파서의 출처값이자 정답표에 찍히는 값인데, 개념지 제목 기본값이 '제목 없음' 이라 실제로 겹친다. 겹친 채로 두면 채점하는 사람이 어느 자료를 펴야 할지 모른다
    - ⚠️ **붙여 만든 이름까지 쓴 이름으로 센다**(코덱스 리뷰). 이 함수는 자료를 하나 더할 때마다 **이미 번호가 붙은 목록 위에서 다시 돈다** — 원래 이름만 세면 `봄봄` 을 셋째로 더할 때 `봄봄 (2)` 를 또 만들어 앞의 것과 같아지고, 두 자료의 근거가 정답표에서 **한 출처로 찍힌다**
  - ⚠️ **본문 읽기는 자료마다 요청 번호를 든다**(`bodyReqRef`, 코덱스 리뷰). 찾기의 세대 번호로는 모자라다 — 본문은 자료를 더할 때마다 따로 도는데, 키만 보고 처리하면 **A 를 뺐다가 다시 붙였을 때 옛 요청의 실패가 멀쩡한 A 를 지운다.** 이미 도는 요청이 있는 자료는 다시 묻지도 않는다
  - ⚠️ **다시 찾을 때 까닭·이름은 새것으로 간다**(본문만 물려받는다, 코덱스 리뷰). 객체를 통째로 재사용하면 작품명을 바꿔 다시 찾아도 화면에 옛 까닭("제목에 '봄봄'")이 남아 거짓말을 한다
  - ⚠️ **자동으로 붙일지 가르는 문턱은 '종류 덤을 더하기 전' 점수와 견준다**(`MATCH_MIN_SCORE`, 코덱스 2R). 덤까지 더해 비교하면 `지은이만 같은 전문`(2+1)과 `본문에 이름만 스친 개념지`(2+1)가 통과해, 막으려던 '약한 신호 하나' 가 그대로 들어온다 — **같은 지은이의 다른 작품**이 붙고 프롬프트는 그것을 근거로 써도 된다고 말한다. 덤은 줄 세울 때만 쓴다
  - ⚠️ **단원 신호는 학년으로 좁힌다**(코덱스 5R). 대단원 이름은 학년별로 따로 관리되는 값이라(`major_chapters` 의 `UNIQUE(name, publisher_id, grade, semester)`) 학년만 다른 같은 이름 단원이 실제로 있다. 안 좁히면 **다른 학년 개념지**가 붙는데, 단원 신호 하나로도 문턱을 넘어 그대로 근거가 된다
  - ⚠️ **지문이 바뀌면 자동으로 붙은 자료를 걷어낸다**(`dropAuto`, 코덱스 2R·3R). 자료는 지문이 아니라 작품명·단원·학교를 보고 붙으므로, 다른 작품을 붙여 넣어도 그 값들은 그대로라 **앞 지문의 자료가 새 문항의 근거로 실린다.** 지문 칸은 글자마다 조회할 수 없어 `onBlur` 로 다시 찾지 않으니 여기서 걷어내는 수밖에 없다. ⚠️ **아카이브에서 골랐는지와 무관하게** 걷어낸다(붙여넣기도 같은 일이 난다). 찾을 신호가 아예 사라진 경우도 같다 — 그냥 돌아가면 옛 자료가 남는다
  - ⚠️ **참고자료를 허락하면 규칙 본문의 '지문에만 있어야 한다' 도 함께 간다**(`{WHERE}` 자리표시자, 코덱스 6R). "답은 지문에 글자 그대로 있어야 한다"·"지문에 없는 표현법 이름을 답으로 내지 않는다" 가 남아 있으면 **한 프롬프트 안에서 참고자료 규칙과 맞부딪혀**, 정작 만들라고 한 참고자료 문항이 안 나온다. 자리표시자를 새로 넣을 때도 전부 함께 갈 것 — 테스트가 두 모드의 문구를 고정한다
  - ⚠️ **자료를 '찾는 중' 에도 만들기를 막는다**(`referenceBlocker` 의 `searching`, 코덱스 리뷰). 제목을 적자마자 누르면 자동 찾기가 막 시작된 참이라 붙은 자료가 아직 없다 — 그대로 보내면 **참고자료 없이** 만들어지고, 그 뒤에 붙은 자료는 이번 출제에 쓰이지 않는데 사람은 붙은 것을 보고 썼다고 여긴다
  - ⚠️ **인쇄는 '만들 때 굳힌' 자료 이름을 싣는다**(`usePassageQuiz` 의 `source.references`). 만든 뒤에도 자료를 빼고 더할 수 있어서, 지금 목록을 실으면 **쓰지도 않은 자료 이름 + 옛 자료로 낸 문항**이 한 장에 찍힌다(지문을 굳히는 것과 같은 근거)
  - **사람이 뺀 자료는 다시 안 붙는다**(`dismissedRef`, [useQuizReferences.ts](src/hooks/useQuizReferences.ts)). 뺐는데 '참고자료 찾기' 로 되살아나면 같은 자료를 몇 번이고 다시 빼게 된다. 직접 고른 것은 자동보다 **세다** — 다시 찾기가 자동 자리만 갈아 끼운다
  - ⚠️ **자동으로 붙이는 이상 왜 붙었는지 반드시 함께 돌려준다**(`RankedReference.reason`). 근거 없이 붙으면 엉뚱한 자료가 끼었을 때 고칠 실마리가 없다(메모리의 '자동 채움은 가장 가까운 값 + 근거 표시 + 수동 우선' 규칙)
  - **후보는 가볍게, 본문은 붙인 것만**([candidates.ts](src/lib/quiz-references/candidates.ts) ↔ [bodies.ts](src/lib/quiz-references/bodies.ts)). 후보 스무 줄에 본문을 실으면 응답이 수십만 자가 된다. 개념지 본문으로 찾는 신호도 `ilike('editor_html', …)` 로 **서버에서만** 훑고 컬럼에 담아 오지 않는다
  - ⚠️ **`.or()` 를 쓰지 않는다** — 신호마다 쿼리 하나씩 만들어 `Promise.allSettled` 로 돌린다(이스케이프가 인용을 통과하며 풀리는 그 문제). 하나가 실패해도 본 기능을 막지 않고 실패한 수만 알린다 — 참고자료는 문제 만들기를 돕는 곁가지다
  - ⚠️ **`lib/quiz-references` 는 배럴 셋을 값으로 import 하지 않는다**: `@/lib/passage-quiz`·`@/lib/concept-pick` 은 `run.ts` 재수출로 **순환**이 되고, `@/lib/reference-texts` 는 `import.ts` 를 통해 **pdf.js 를 문제 만들기 번들로 끌어온다.** 셋 다 파일 경로로 가져온다([import-cycles.test.ts](src/lib/__checks/import-cycles.test.ts) 가 앞의 둘을 잡는다)
  - **상한 셋이 서로 맞물린다**: 지문 40,000 + 자동 3건 × 15,000 = 85,000 이라 **자동만으로는 절대 합 상한(100,000)을 못 넘는다.** 직접 더해 넘으면 `referenceBlocker` 가 보내기 전에 막는다(보내 보고 `context_exceeded` 를 받느니 먼저 알린다). 읽을 글이 몇 배가 되므로 워치독도 120초 → 180초로 올렸다
  - ⚠️ **`extractPageText` 는 쪽당 200자 문턱이 있다**(복합기 자동 OCR 레이어를 거르려고 둔 것). 작품 전문 가져오기가 그것을 쓰면 **시집처럼 쪽이 짧은 글이 통째로 빈다** — 가리지 않는 `readPageText` 를 갈라 뒀다([pdfText.ts](src/lib/pdf/pdfText.ts)). 기출·프린트 경로의 동작은 그대로다
  - **작품 전문 본문은 평문이다**(HTML 아님). 서식이 필요 없고, 평문이면 화면이 텍스트 노드로만 그려 정화를 한 겹 더 두는 것보다 확실하다(지문 인쇄와 같은 판단). 그래서 `sanitize-html` 에 손댈 것이 없다
  - **스캔 PDF 는 범위 밖이고, 그 사실을 갈 길과 함께 말한다**(`pdfImportVerdict`). "못 읽었어요" 로 끝내면 선생님은 파일이 잘못된 줄 알고 몇 번이고 다시 고른다 — 학교 프린트 시험지로 올리면 AI 가 읽어 준다고 알린다
  - ⚠️ **한 줄 컬럼 상수가 세 곳 더 늘었다**(`SHEET_COLUMNS` ×2, `REFERENCE_TEXT_LIST_COLUMNS`). `+` 로 이으면 리터럴 타입이 `string` 으로 넓어져 PostgREST 행 타입 추론이 통째로 풀린다
  - **기능 플래그를 새로 만들지 않았다** — `passage_quiz` 의 일부다(다섯 곳을 함께 고쳐야 하는 그 작업은 '다른 화면의 다른 일' 일 때만 한다)
  - ⚠️ **카테고리 `.eq` 매칭은 양쪽이 `normalizeCategoryName` 을 거쳤다는 전제다**(sql/16). 옛 표기로 저장된 개념지는 단원 신호로 안 걸린다 — 그때는 '직접 추가' 로 붙인다
  - ⚠️ **작품 전문 편집기는 저장 중에 친 글을 지키다**(`settle`, 코덱스 리뷰). 저장하는 동안에도 입력칸은 살아 있어서, 보낸 값으로 화면을 통째로 덮으면 그 몇 초 사이에 이어 친 글이 **말없이 사라진다.** 칸마다 가려 보낸 뒤로 손대지 않은 칸만 정규화된 값으로 맞춘다. '저장된 값' 은 언제나 **DB 에 실제로 들어간 것**이라 저장 중에 고친 칸은 '저장하지 않은 내용' 으로 제대로 남는다
  - ⚠️ **새로 만든 뒤 주소 옮기기는 '손댄 것이 없을 때만' 한다**(`navigateWhenSafe`, 코덱스 4R). `/new` → `/{id}` 는 **동적 구간 값이 바뀌는 이동**이라 App Router 가 화면을 새로 마운트하고, 새 화면은 DB 값을 다시 읽어 그린다 — 저장하는 사이 이어 친 글이 **확인창 하나 없이 사라진다.** 손댄 것이 있으면 주소를 미뤄 두었다가(`pendingNavRef`) 다음 저장이 깨끗하게 끝났을 때 옮긴다. 옮기기 전에도 편집·저장은 그대로 된다(`savedId` 가 있어 수정으로 나간다). `loadedIdRef` 는 **같은 화면이 유지되는 경우**의 재조회만 막으므로 이것과 한 쌍이다
  - ⚠️ **전문 목록 검색은 서버가 한다**(코덱스 4R). 받아 둔 상한(200)에서 거르면 그 너머의 옛 작품이 **제목을 정확히 쳐도 없는 것처럼 보이고**, 이 화면에는 페이지 넘기기가 없어 고치거나 지울 길이 아예 사라진다. 글자마다 물으면 왕복이 늘어 잠깐 기다렸다 묻고, 지운 뒤 다시 읽을 때는 **닫아 둔 값이 아니라 지금 검색어**를 본다(그 사이 바뀌었을 수 있다)
  - ⚠️ **새로 만들면 주소가 `/new` → `/{id}` 로 바뀌어 로드 효과가 다시 돈다**(`loadedIdRef`). 그대로 두면 방금 저장한 값을 다시 읽어 와 덮는다. 그 ref 의 **처음 값은 반드시 `null`** 이다 — 편집할 id 로 채우면 주소로 바로 들어온 화면이 "이미 읽었다" 고 판정해 스피너만 돈다
  - **신규 부트스트랩 범위는 01~30 이다**(sql/30 은 **앱 배포보다 먼저** 적용한다 — 전문 화면이 그 표를 직접 조회한다). **2026-09-15 운영 DB 에 적용 완료**(`Ara-system/scripts/run-sql.js` 로 실행)
- [2026-09-19] **지문 하나에 작품 여러 편 — 목록이 원본, 옛 문자열은 파생**([sql/33](sql/33_problem_bank_multi_works.sql), [work-title.ts](src/lib/problem-bank/work-title.ts), [parse.ts](src/lib/problem-ocr/parse.ts)). 되돌리지 말아야 할 판단들:
  - **증상은 "`(가)(나)` 지문의 작품을 한 편으로 못 고른다" 였다.** 옛 프롬프트는 여러 편을 `' · '` 로 **이어 적게** 했고(`'먼 후일 · 독은 아름답다'`), 조회는 `.eq('work_title')` 완전 일치라 '먼 후일' 을 고르면 그 문항이 **하나도 안 나왔다.** 작품 트리에는 그 이름의 가짜 작품이 하나 섰고 지은이도 `'김소월 · 함민복'` 으로 뭉쳤다. 2026-09-19 운영 데이터로 지문 19건·문항 58건이 그 꼴이었다
  - **원본을 옮기되 옛 컬럼은 파생으로 남겼다**: `passages.works`(JSONB `[{label,title,author}]`)·`problems.work_titles`(TEXT[])가 원본이고, `passages.title`/`author`·`problems.work_title` 은 **트리거가 `' · '` 로 이어 만든다.** 지우지 않은 까닭은 인쇄 스냅샷(sql/23)·검색 평문(sql/17)·지문 고르기·참고자료 신호 등 **문자열을 읽는 곳이 스무 군데**여서다 — 파생으로 남기면 그 전부가 그대로 돈다. `search_text`·`char_count` 와 같은 계약이라 **앱은 파생 컬럼을 보내지 않는다**
  - ⚠️ **트리거가 표마다 두 벌인 것은 실수가 아니다**(`aa_works_a_*` → `aa_works_b_*`). 한 벌로 "문자열이 바뀌었으면 목록을 다시 만든다" 를 두면, 화면이 목록과 **옛 파생 문자열을 한 UPDATE 에 함께** 보낸 순간(폼 전체 저장이 그렇다) 방금 더한 작품이 **조용히 사라진다.** `UPDATE OF <컬럼>` 은 그 컬럼이 SET 목록에 있으면 발화하므로(값이 바뀌었는지와 무관) 그것으로 '목록이 실려 왔는가' 를 가르고, BEFORE 트리거의 WHEN 이 앞 트리거가 고친 NEW 를 보므로 `_a` 가 문자열을 다시 만들어 두면 `_b` 는 지나간다
  - ⚠️ **전파 트리거는 `AFTER UPDATE`(컬럼 목록 **없이**) + WHEN 이다.** `AFTER UPDATE OF works` 로 두면 문자열 경로(`SET title = …`)에서 `_b` 가 `works` 를 다시 만들어도 **발화하지 않는다** — `UPDATE OF` 는 SET 목록만 본다
  - ⚠️ **백필은 문항 먼저, 지문 나중이다.** 순서를 바꾸면 지문 백필의 전파 트리거가 그 시점에 아직 전부 비어 있는 문항을 '물려받는 중' 으로 보고, **사람이 좁혀 적어 둔 작품명**을 지문 전체 목록으로 덮어쓴다
  - **문항의 빈 목록은 '이 지문 전체' 다.** 트리거가 그 자리에서 지문의 작품으로 채우므로 저장된 값은 늘 명시적이다. 화면에서는 '하나도 안 고른 상태' 를 만들 수 없게 막았다(마지막 한 편은 체크를 못 끈다) — 껐다고 생각한 것이 저장 뒤 되살아나는 것이 가장 나쁘다. **새 작품이 붙을 때 따라가는 범위는 아래 항목에 따로 적었다**
  - **병합은 작품을 이어 붙이되 딸린 문항이 묻는 작품은 늘리지 않는다.** 쪽을 넘어가는 (가)(나) 지문이 둘로 갈라져 저장되면 각자 한 편씩만 들고 있어서, 빈 칸만 채우면 뒤 편을 잃는다 — 그래서 목록은 합친다. 다만 그 합치기를 전파 트리거가 '작품이 하나 더 붙었다' 로 보면 **앞 지문의 문항이 뒤 지문의 작품까지 묻게 된다**(앞 지문이 두 편 이상일 때 특히 걸린다). 그래서 합치는 동안만 전파 트리거를 **트랜잭션 지역 표시**(`set_config('exam.skip_work_sync', 'on', true)`)로 끄고, 작품이 **아예 없던** 문항만 따로 채운다 — 그건 '지문 전체' 라 합친 뒤에도 참이다. 앱의 `fillPassageGaps` 도 **작품만 합집합**이다(`grammar_paths` 와 같은 사정) — 다만 **문항은 합집합이 아니다**: 묶음마다 다른 편을 냈다면 합칠 조각이 아니라 갈린 의견이라 먼저 온 것이 이긴다
  - ⚠️ **문항이 낸 작품은 딸린 지문의 작품과 대조해 없는 이름을 버린다**(`parse.ts`). 안 버리면 모델이 지어낸 이름이 트리에 잎으로 남고, 전파 트리거가 그것을 '사람이 좁혀 적은 값' 으로 보아 **영영 보존한다.** 지문이 작품을 모르면(이어지는 조각이라 머리글이 없다) 대조할 근거가 없어 건너뛴다
  - **`split(join(x)) === x` 가 규약이다.** 이음 기호는 `' · '` 하나로 못박고(나눌 때는 `·•・` 셋 다 받는다) 제목 안에 남은 기호는 정규화가 쪼갠다 — 이 왕복이 깨지면 `_b` 가 `_a` 의 결과를 '고쳐' 목록과 문자열이 영영 어긋난다
  - **`work_count` 생성 컬럼을 만들지 않았다.** 감사 트리거가 BEFORE 라 생성 컬럼 값을 한 판 뒤진 채 로그에 남기고, `select('*')` 를 되쓰는 경로가 `428C9` 로 죽는다. 갈라 보기는 목록 한 쪽(≤60행)을 화면에서 나눈다(`splitByWorkSpan`)
  - **경고에 지킬 차례를 만들었다**(`warnings.ts` 의 `keep`: `always` › `merge` › `work` › 보통).
    작품이 여럿이면 경고도 여럿이라, 참·거짓 하나(`critical`)로는 옛한글 경고가 작품 경고에
    밀려났다. 밀어낼 것은 **가장 낮은 등급 가운데**서 고르고(모델 자유 서술이 그 안에서 먼저),
    묶음 상한과 최종 상한이 **같은 차례**를 쓴다 — 한쪽만 다르면 묶음에서 산 경고가 최종에서 죽는다
  - **병합이 끝나면 뒤집힌 판단을 걷어낸다**(`dropStaleWarnings`). 겹쳐 읽은 다음 묶음이 같은
    문항을 더 온전하게 내놓으면 앞 묶음의 말이 **틀린 말이 된다**(지문을 못 찾았다던 문항이
    지문에 붙고, 추측이라던 작품명이 다른 이름으로 바뀐다). 그대로 두면 검수 화면이 저장될 값과
    모순되는 경고를 띄운다. 단서(`about`)는 저장 전에 떼어 낸다
  - **묶음마다 다른 작품을 냈으면 먼저 온 것을 쓰되 말해 준다.** 조용히 첫 값을 굳히면 첫 묶음이
    잘못 좁혔을 때 원본과 맞춰 볼 실마리가 없다. 지문에 없는 이름이 섞였으면 **살아남은 편**이
    먼저고(넓히지 않는다), 하나도 안 남았을 때만 다른 묶음의 **넓은** 목록을 쓴다 —
    너무 좁으면 그 작품으로 훑을 때 문항이 아예 안 나와 없는 줄 안다
  - ⚠️ **새 작품이 붙을 때 따라가는 것은 작품이 두 편 이상이던 지문뿐이다**(코덱스 stop 리뷰).
    한 편뿐이던 지문에서는 '전체를 묻는다' 와 '그 한 편으로 좁혔다' 가 저장값으로 구별되지 않는다 —
    따라가게 두면 뒤늦게 (나)를 더하거나 갈라진 지문을 합칠 때 **한 편만 묻던 문항이 말없이 두 편을
    묻는 문항이 된다.** 없는 사실을 지어내는 쪽이라 넓히지 않고, 새 작품은 검수 화면에서 체크로 붙인다.
    이름 바꾸기·빠짐은 그대로 따라간다(그건 넓히는 일이 아니다)
  - 코덱스 리뷰 **10라운드 + stop 게이트 2라운드**로 P0 1건(합치면 상한을 넘는 작품이 조용히
    잘리고 그 편을 묻던 문항이 '지문 전체' 로 넓어짐)·P1 8건·P2 12건을 고쳤다
  - **sql/33 은 운영 DB 에 적용 완료**(자가 검증 **14단계** 통과 — 되돌리는 하위 트랜잭션에 진짜 행을 넣어 트리거가 실제로 도는지까지 확인했다). 백필은 감사 트리거만 껐다 켜고 돌렸다(문항 993건·지문 200건)
- [2026-09-15] **업로드 작품명 자동 채움 — 프롬프트 문턱을 내리고, 알아본 것은 밝힌다**([prompt-works.ts](src/lib/problem-ocr/prompt-works.ts), [work-candidates.ts](src/lib/problem-bank/work-candidates.ts)). 되돌리지 말아야 할 판단들:
  - **증상은 "작품명이 안 들어온다" 였고 원인은 프롬프트의 문턱이었다.** 운영 데이터를 세어 보니(2026-09-15) 지문 9건·문항 25건의 작품명이 **전부 비어 있었다** — 09-09 에 `[작품]` 규칙을 넣은 **뒤**의 결과다. 옛 문구가 "널리 알려진 작품이라 **확실하면**" + "조금이라도 아리송하면 null" 이라, 모델이 인쇄돼 있지 않은 작품명을 사실상 언제나 비웠다. 그런데 시험지에 실리는 문학은 대개 교과서 수록작이고 선생님은 그걸 매번 손으로 친다. 그래서 **문턱을 내리되 지어내기 금지는 그대로** 두고, **알아본 것은 warnings 로 밝히게** 했다(`'3쪽 지문: 작품명을 본문으로 알아봤어요(동백꽃). 확인해 주세요.'`)
  - ⚠️ **"알아본 것을 적는다" 와 "밝힌다" 는 한 쌍이다.** 밝히지 않으면 인쇄된 것을 옮긴 값과 모델이 추측한 값이 화면에서 구별되지 않아, 틀렸을 때 어디를 의심할지 알 수 없다 — 한쪽만 풀지 말 것(`print-words` 의 '프롬프트와 파서는 한 쌍' 과 같은 규약). 파서(`normalizeWork`)는 **손대지 않았다**
  - **작품 마스터 표를 만들지 않았다**(`work-tree.ts` 규약 — 작품은 시험지에 실려야 비로소 생긴다). 후보는 **이미 쌓인 데이터에서 파생**한다: ① `print_bundles.name` 의 프린트별 이름(`composePrintName` 접두를 벗긴 나머지 — 선생님이 직접 친 작품명이다), ② 같은 학교 `passages.title`/`author`(검수를 거친 표기다)
  - ⚠️ **내신 관리 `school_exam_scopes.scope` 자유 텍스트는 쓰지 않았다.** ara-system 의 입력 도움말이 `외부 지문 「봄봄」(김유정)` 을 예로 들어서 파싱하고 싶어지지만, 운영 데이터에는 그 표기가 **한 줄도 없다**(7행 중 0행, 2026-09-15 확인). 없는 것을 파싱하면 코드만 늘고 후보는 안 는다 — 되살리려면 실데이터를 **먼저** 세어 볼 것
  - **작품 칸은 저장하지 않는다**(`toSourcePayload` 에 없고 `problem_sources` 에 컬럼도 없다). 진짜 작품명은 읽어 낸 지문·문항에 붙고(`passages.title` → 트리거 → `problems.work_title`), 이 칸은 **프롬프트 힌트**일 뿐이다. 컬럼을 만들면 같은 사실이 두 곳에 있게 되고 어느 쪽이 정본인지 흐려진다. OCR 은 업로드 화면에서만 도므로(`runProblemOcr` 호출부는 `useProblemOcr` 하나) 저장하지 않아도 값이 닿는다
  - **후보 표기를 그대로 쓰라고 시킨다.** 같은 작품이 `동백꽃`·`「동백꽃」`·`동백 꽃` 으로 갈리면 작품 트리가 쪼개진다 — `normalizeWorkTitle` 이 감싸는 기호만 벗기므로 나머지는 표기를 맞추는 수밖에 없다
  - **`worksAuto` 는 `textbookAuto` 의 거울이다**([source-form-state.ts](src/lib/problem-bank/source-form-state.ts)) — **칸을 비우는 것까지 '직접 고른 값'** 이라 자동을 끈다. 제목처럼 "비우면 다시 자동" 으로 두면 ① 이 시험지엔 작품이 없다고 말할 방법이 없고 ② 자동이 켜져도 후보 값이 그대로라 힌트 효과가 다시 돌지 않아 **아무 일도 안 일어난다**(코덱스 리뷰) — 되살아날 것처럼 보이면서 안 되살아나는 것이 가장 나쁘다. 자동은 **학교·학교급을 바꿀 때** 다시 켜지고 그때는 칸도 함께 비운다 — 옛 학교의 값이 남으면 새 학교 힌트가 "이미 골라 뒀다" 고 보고 물러난다
  - ⚠️ **학교를 안 묻는 유형으로 바꾸면 자동으로 채운 작품을 비운다**(`useWorkCandidates` 가 빈 값을 알린다, 코덱스 리뷰). 조회만 멈추고 칸을 그대로 두면, 내신기출에서 채워 둔 **앞 학교의 작품**이 문제집으로 바꾼 뒤에도 남아 그대로 AI 에게 실려 간다 — 그때는 화면에서 학교 칸이 이미 사라져 어디서 온 값인지 알 길이 없다(교과서 힌트가 `visibleFields` 로 가리는 것과 같은 결함을 한 겹 더 막는다)
  - ⚠️ **후보로 쓰는 기출 지문은 `완료` 상태의 출처 것만이다**(코덱스 리뷰). 모델이 본문으로 알아본 작품명은 저장되는 순간 이미 `검수중` 이라, 상태를 안 가리면 **아직 아무도 확인하지 않은 추측**이 다음 업로드의 '표준 표기' 힌트가 되어 틀린 이름이 스스로 번진다
  - **후보 중복 제거 키는 제목이 아니라 제목+지은이다**(코덱스 리뷰 1R). 제목만으로 묶으면 동명이작이 하나로 접혀 **지은이 구분이 사라지는데**, 그 구분이 바로 모델에게 어느 작품인지 알려 주는 단서다. 다만 **지은이가 빈 줄은 같은 제목에 지은이가 있는 줄로 흡수한다** — 프린트 이름에는 지은이가 없어서, 안 흡수하면 '동백꽃' 과 '동백꽃 (김유정)' 이 후보에 나란히 실려 모델이 둘을 다른 작품으로 본다
  - ⚠️ **흡수할 때 가까운 쪽의 등급·근거를 가져온다**(코덱스 리뷰 2R). 지은이 없는 줄을 그냥 지우면 올해 이 시험 프린트의 '동백꽃'(등급 0)이 사라지고 작년 기출의 '동백꽃 (김유정)'(등급 4)만 남아 **가장 가까운 작품이 뒤로 밀리고** 근거도 작년 것으로 바뀐다 — 상한(20)에 걸려 아예 빠질 수도 있다. 같은 제목에 **지은이가 둘 이상**이면(진짜 동명이작) 지은이 없는 줄이 누구 것인지 알 수 없으므로 합치지 않는다
  - **후보는 학년이 다르면 아예 뺀다**(교과서 힌트와 다른 점). 교과서는 학교마다 하나로 정해지지만 작품은 학년마다 통째로 다르다 — 중1 작품을 중3 시험지 후보로 주면 모델이 엉뚱한 작품에 맞춰 읽는다. 학년도·학기·시험은 **줄 세우기에만** 쓴다(맞는 것이 앞으로 온다)
  - **`work-candidates.ts` 는 `print-scan/` 을 import 하지 않는다.** 그 배럴이 `run.ts` 를 다시 내보내 순환이 된다(`import-cycles.test.ts` 가 잡는다) — `composePrintName` 규칙을 `normalizeCategoryName` 으로 **재현**한다. 규칙을 바꾸면 양쪽을 함께 고칠 것
  - **조회 키는 학교 하나다**(`useWorkCandidates`). 학년·학기가 바뀔 때마다 다시 읽으면 같은 표를 몇 번씩 긁는다 — 줄 세우기는 손안의 값으로 `useMemo` 가 한다. 조회는 fail-soft(`try/catch → []`)
  - ⚠️ **'알아본 작품명입니다' 안내는 모델이 아니라 파서가 만든다**(`title_source`, 코덱스 리뷰 2R). 처음에는 프롬프트가 "알아본 것이면 warnings 에 적어라" 고만 시켰는데, 그러면 **모델이 적을 때도 있고 안 적을 때도 있어** 알아낸 이름과 인쇄된 이름을 화면에서 구별할 수 없다 — 문턱을 내린 대가로 얻어야 할 안전장치가 운에 달린다. 이제 스키마가 `title_source`(`printed`/`inferred`)를 **필수로** 받고 [parse.ts](src/lib/problem-ocr/parse.ts) 가 그 값으로 경고를 만든다. 고친 곳은 **스키마·파서 둘뿐**이다 — 병합은 경고를 이미 나르고 저장은 `ocr_meta.warnings` 를 그대로 쓴다(DB 컬럼을 만들지 않았다)
  - **경고는 지문과 '지문 없는 단독 문항' 에 붙인다**(코덱스 리뷰 3R). 딸린 지문이 있는 문항은 그 이름을 지문에서 물려받으므로(sql/20 트리거) 문항마다 붙이면 같은 말이 문항 수만큼 늘어 상한(60)을 먹는다. 반대로 **단독 문항은 물려받을 곳이 없어서**, '문항은 전부 지문에서 물려받는다' 고 보고 건너뛰면 그 작품명만 아무 확인 없이 들어간다
  - ⚠️ **참조가 끝내 안 풀려 단독이 된 문항도 짚는다**(코덱스 리뷰 4R). `passage_ref` 가 이 묶음에 없으면 파서가 그 참조를 **끊는데**(엉뚱한 지문에 붙는 것보다 낫다), 항목을 볼 때는 참조가 있어서 건너뛰고 끊을 때는 안 짚으면 **그 작품명만 영영 확인을 못 받는다.** 그래서 `pushWorkSourceWarning` 을 두 곳(`parseItem`·참조를 끊는 자리)에서 부른다 — 한 곳만 고치지 말 것. ⚠️ **끊는 자리의 보충은 `kind === 'problem'` 일 때만**이다(코덱스 리뷰 5R): 지문은 참조와 상관없이 `parseItem` 에서 이미 짚었으므로, 참조가 딸린 지문(모델의 실수다)까지 보충하면 같은 경고가 두 번 담겨 묶음 상한(20)을 헛되이 먹는다
  - ⚠️ **`'printed'` 라고 말했을 때만 인쇄된 것으로 친다**(코덱스 리뷰 3R). null·빠뜨림·모르는 값을 `printed` 로 넘기면 **제목은 있는데 출처가 없는 응답이 경고 없이 인쇄된 이름으로 확정된다** — 문턱을 내린 대가로 얻어야 할 안전장치가 그 자리에서 사라진다. 그때는 '인쇄된 것인지 알 수 없어요' 로 짚는다(없는 경고를 지어내는 것이 아니라 **모른다는 사실**을 말하는 것이다). 프롬프트도 "이름을 적었으면 null 로 두지 않는다" 고 못박는다
  - ⚠️ **받아들인 한계**: 모델이 출처를 **잘못** 분류하는 것(알아내 놓고 `printed` 라고 하는 것)까지는 막지 못한다. 그건 응답을 원본과 대조해야 알 수 있는 일이다
  - **'이어 붙여 주세요' 를 '확인해 주세요' 로 바꿨다**([merge.ts](src/lib/problem-ocr/merge.ts)). 사용자 제보: "지문은 이제 알아서 잘 붙이는데 자꾸 이어 붙여 달라고 뜬다". `findOpenPassage` 가 닫힌 지문에도 조각을 붙이게 된 뒤로 병합은 거의 자동으로 끝나고, 남는 `open` 은 대개 마지막 조각의 `continues` 표시다 — **할 일 없는 지시는 경고 전체의 신뢰를 깎는다.** 대상 칩(어느 지문인지)은 그대로 둔다. `parse.ts` 의 '본문이 너무 길어 … 이어 붙여 주세요' 는 **진짜로 글이 잘린 경우**라 손대지 않았다
- [2026-09-15] **옛한글(중세국어)은 첫가끝 자모로 저장하고, 글꼴은 자체 호스팅 세 얼굴로 그린다**([src/lib/yet-hangul/](src/lib/yet-hangul/), [globals.css](src/app/globals.css), [public/fonts/](public/fonts/README.md)). 되돌리지 말아야 할 판단들:
  - **저장 형태는 유니코드 첫가끝 조합형 자모**(초성 U+1100~ · 중성 U+1160~ · 종성 U+11A8~, 확장 A/B 포함) + 방점 U+302E/302F 다. 들어오는 글은 `normalizeYetHangul` 이 **한 모양으로 맞춘다**(코덱스 리뷰 3R): NFC 로 접어 자모로 갈린 현대 글자를 되돌린 뒤, **자모 사슬에 걸린 완성형 음절을 다시 푼다**(뒤에 중성·종성이 오는 경우와, **앞에 초성이 오는 경우** 둘 다 — 초성 여럿으로 적은 어두 자음군 `ᄇ+ᄉ+ᅡ` 를 NFC 가 `ᄇ사` 로 만든다, 코덱스 리뷰 4R). ⚠️ 종성 뒤의 완성형은 **다음 음절**이라 풀지 않는다(`ᄒᆞᆫ`+`글`). ⚠️ NFC 만 쓰면 `가+ᇫ`·`바+ᇰ`(동국정운식 한자음) 같은 **섞인 모양**이 남는데, 자모만 담은 안전망 글꼴에는 완성형 글자가 없어 그 모양이 **두 글자로 쪼개져 보인다**(실측). 맞추지 않으면 겹쳐 읽은 같은 지문이 중복 판정 키에서 갈라져 **두 번 저장된다.** DB·정화기·스키마는 손댈 것이 없었다(TEXT, CHECK 없음, DOMPurify 는 글자를 안 건드린다 — `sanitize-problem.test.ts` 가 왕복 무손실을 고정한다).
  - ⚠️ **NFC 는 안전하고 NFKC 는 금지다.** 옛한글은 완성형이 없어 NFC 를 지나도 조합형 그대로 남는다. 반면 NFKC 는 호환 자모(ㆍ U+318D·ㅿ)를 조합형으로 바꿔 표기를 갈라 놓는다 — [merge-keys.ts](src/lib/problem-ocr/merge-keys.ts) 의 NFKC 는 머리글 `[1~3]` 전용이라 남겨 뒀다(주석으로 못 박음). `box-labels.ts` 의 오랜 경고와 같은 결이다.
  - **정화기를 넓히지 않았다.** 글꼴을 본문에 심으려면 `class` 나 `font-family` 를 허용해야 하는데, 그건 캐럿 튐 이력([ProblemHtmlEditor.tsx](src/components/problem-editor/ProblemHtmlEditor.tsx) 주석)과 XSS 면적을 함께 키운다. 대신 **렌더 시점에 `hasYetHangul(html)` 로 감지해 바깥 래퍼에 클래스**를 붙인다 — 저장값은 글자뿐이다.
  - **지문은 명조(`yet-hangul-serif`), 발문·선지·개념지는 고딕(`yet-hangul`)** 이다(사용자 결정). ⚠️ 인쇄 블록의 판정은 [blocks.ts](src/lib/problem-paper/blocks.ts) 가 **지문 전체로 한 번** 해서 모든 조각에 `serif` 로 싣는다 — 조각마다 판정하면 현대어 풀이 문단만 고딕이 되어 한 지문이 두 글꼴로 갈린다.
  - ⚠️ **낱자 하나(ㆍ·ㅿ)만 있는 글은 옛한글로 보지 않는다**(`hasYetHangul` 이 호환 자모를 세지 않는다). '아래아의 소실' 을 묻는 문법 문항까지 블록 글꼴이 바뀌면 안 된다 — 그 낱자는 안전망 글꼴이 알아서 그린다.
  - ⚠️ **자모로 갈린 현대 글자(NFD)도 옛한글이 아니다**(코덱스 리뷰 3R). `'한글'.normalize('NFD')` 는 조합형 자모로만 이뤄져 글자 범위가 옛한글과 같다 — 그대로 두면 맥에서 복사해 온 현대 글이 옛한글로 판정돼 **대조 검증을 빠져나간다**. `hasYetHangul` 은 자모가 보일 때만 NFC 로 접어 다시 본다(현대 글자는 완성형으로 합쳐지고 옛한글은 합칠 완성형이 없다).
  - ⚠️ **글꼴 얼굴이 셋인 까닭, 그리고 안전망만 서브셋인 까닭**: **WebKit(사파리)은 `unicode-range` 와 무관하게 스택에 있는 웹폰트를 내려받는다**(실측: 옛 글자가 한 자도 없는 쪽에서 `document.fonts` status 가 loaded. 크롬·파이어폭스는 건너뛴다). 그래서 `--font-sans` 에 얹는 안전망 얼굴은 **자모·방점·옛 낱자·한양 PUA 만 담은 0.27MB 서브셋**이고, 완성형·한자까지 든 큰 얼굴 둘(고딕 1.7MB·명조 2.5MB)은 **감지 클래스에만** 건다. 안전망에 큰 파일을 물리면 모든 사파리 사용자가 매번 그것을 받는다([webfont.test.ts](src/lib/yet-hangul/webfont.test.ts) 가 크기와 `unicode-range` 개수를 고정한다).
  - ⚠️ **안전망 범위는 U+3165 부터다.** 〈보기〉의 ㄱ. ㄴ. ㄷ.(U+3131~)까지 넣으면 현대 시험지의 낱자 모양이 옛한글 글꼴로 바뀐다.
  - **글꼴 라이선스 전문을 함께 둔다**([public/fonts/LICENSE.txt](public/fonts/LICENSE.txt), 코덱스 리뷰 5R). 나눔글꼴은 **SIL OFL 1.1** 이고, OFL 은 재배포할 때 저작권 안내와 전문을 함께 담으라고 한다. 우리가 배포하는 것은 공식 OTF 의 **형식 변환본**(+ 안전망은 **부분 글꼴**)이고 글리프는 손대지 않았다. ⚠️ OFL 의 **지정 글꼴명 조항**(수정본은 `Nanum…` 이름을 못 쓴다) 때문에 둘로 갈랐다(코덱스 리뷰 3R): 전체 글꼴 둘은 글리프를 안 건드린 **형식 변환**이라 이름을 유지하고, 글자를 덜어 낸 **부분 글꼴만 `AraExam Yet Jamo` 로 이름을 바꿨다**(저작권·상표 고지는 원본 그대로). CSS 의 `font-family` 는 우리가 정한 이름이라 화면에는 영향이 없다.
  - ⚠️ **미러의 woff2 를 그대로 쓰지 말 것.** fonts-archive 저장소가 주는 `.woff2`·`.woff` 는 **자모 조합 기능(ljmo·vjmo·tjmo)이 빠진 다른 빌드**였다(내부 이름 `NanumBareunGothic…`). 그것으로는 `ᄒᆞᆫ` 이 자모 세 개로 나란히 찍히는데 **글자는 다 보여서 화면만 보고는 원인을 못 찾는다.** 같은 저장소의 `.otf` 는 멀쩡하므로 그것을 받아 직접 woff2 로 변환했다(절차·해시는 [public/fonts/README.md](public/fonts/README.md), 해시는 테스트가 고정).
  - **모델이 조합형 자모를 못 낼 때의 대체 표기 `⟦ㅎㆍㄴ⟧`** 를 프롬프트가 허용하고 파서가 자모로 바꾼다([compose.ts](src/lib/yet-hangul/compose.ts)). ⚠️ **저장 형태로 굳히는 것(`finalizeYetHangul`)은 정화 앞뒤로 두 번 돈다**(멱등이라 안전하다). 앞에서 도는 까닭은 상자 말머리 다듬기와 **쪽·항목 경고가 풀린 글을 봐야** 하기 때문이고(코덱스 리뷰 5R), 뒤에서 또 도는 까닭은 정화가 HTML 실체 참조를 풀기 때문이다(4R).
    - ⚠️ **정화기는 태그가 하나도 없는 조각을 그대로 돌려준다** — 선지가 그렇다(코덱스 리뷰 5R). 그래서 `decodeYetHangulEntities` 가 실체 참조를 **직접** 되돌린다. **되돌리는 것은 마크업이 될 수 없는 글자뿐이다**(한글 음절·자모·방점·한양 PUA·`⟦ ⟧`) — `&lt;script&gt;` 까지 풀면 정화가 막아 둔 것이 되살아난다. 그 금지는 테스트가 고정한다. ⚠️ **괄호 밖 낱자는 절대 건드리지 않는다**(문법 문항의 "ㄱ. 첫째"). 못 바꾼 괄호는 **지우지 않고 남겨** 경고로 넘긴다.
  - ⚠️ **경고는 쪽 단위다**([yet-hangul-pages.ts](src/lib/problem-ocr/yet-hangul-pages.ts)). ⚠️ 다만 **'⟦ ⟧ 를 못 바꿨다' 는 그 항목을 보는 자리에서 바로 넣는다**(코덱스 리뷰 6R) — 끝에 몰아 넣으면 뒤 항목들의 경고가 상한(20)을 먼저 채워 정작 고쳐야 할 경고가 잘린다. 중세국어 시험지는 거의 모든 항목에 옛 글자가 있어 항목마다 붙이면 `OCR_MAX_WARNINGS`(20)를 그것만으로 채워 빠진 쪽·못 읽은 선지가 밀려난다. **항목 단위로 짚는 것은 '⟦ ⟧ 를 못 바꿨다' 하나뿐**이다(드문 진짜 실패).
  - ⚠️ **[pdfText.ts](src/lib/pdf/pdfText.ts) 는 한양 PUA(U+E0BC–F8F7)를 깨진 글자로 세지 않는다.** 아래아한글이 옛한글을 거기 저장하므로, 세면 2% 문턱에 걸려 **중세국어 쪽의 글자 레이어가 통째로 버려진다**(참고 텍스트 없이 이미지로만 읽게 되는데 아무 표시도 안 남는다). 대신 그 글자를 `〔옛〕` 으로 바꿔 자리만 알리고 프롬프트가 "그 글자는 이미지를 보고 자모로 적으라" 고 시킨다. PUA→자모 변환표는 아직 없다(후속 과제).
    - ⚠️ **다만 '옮길 수 있는 글자' 로도 세지 않는다**(코덱스 리뷰 2R). 그 자리는 자리 표시로만 넘어가므로, 안 빼면 쪽이 **온통 PUA 일 때도 '쓸 만한 글자 레이어'** 가 되어 참고 텍스트가 `〔옛〕` 200개가 된다 — "글자는 참고 텍스트가 이미지보다 정확하다" 가 거짓이 되고 모델을 헷갈리게 한다.
  - ⚠️ **[verify-text.ts](src/lib/problem-ocr/verify-text.ts) 는 옛한글로 읽어 낸 항목만 대조에서 뺀다.** 글자 레이어는 한양 PUA(→`〔옛〕`), 모델은 첫가끝 자모라 6글자 조각이 절대 맞지 않아 **맞게 읽은 지문마다 '다르다' 고 짚는다**. '확실할 때만 말한다' 규약이고, 그 쪽에는 이미 옛한글 경고가 붙어 대조를 부탁한다.
    - ⚠️ **참고 텍스트(쪽) 쪽을 보고 건너뛰지 말 것**(코덱스 리뷰 1R). `source` 는 **쪽 전체**라, 옛한글 지문 하나가 그 쪽의 **현대 문항 검사까지 통째로** 끈다(실측: 관련 없는 현대 본문에 `〔옛〕` 을 붙이자 경고가 1건 → 0건). 참고 텍스트에 옛 글자가 섞여도 현대 문항의 조각은 그대로 찾아지므로 걸림돌이 아니고, 모델이 옛 글을 **현대 글자로 뭉개 적은** 경우는 오히려 이 대조에 걸려야 맞다.
    - ⚠️ `compact()` 는 견주기 전에 **NFC 로 접는다**(코덱스 리뷰 3R) — 한쪽이 자모로 갈린 현대 글자(NFD)면 조각이 하나도 안 맞아 멀쩡한 문항에 경고가 붙는다.
  - **프롬프트 규칙은 한 상수다**([prompt-rules.ts](src/lib/yet-hangul/prompt-rules.ts)) — 기출·프린트·이어 읽기가 같은 문장을 쓴다. `BODY_FORMAT_RULES` 와 달리 **태그가 아니라 글자** 규칙이라 개념지 프롬프트에 넣어도 정화기에 지워질 것이 없다. 프린트 프롬프트의 "못 읽은 글자는 □" 규칙에는 **"옛한글은 못 읽은 글자가 아니다"** 를 덧붙였다 — 안 그러면 옛 글자가 전부 □ 로 사라진다.
  - **편집기 입력 도우미**([YetHangulDialog.tsx](src/components/problem-editor/YetHangulDialog.tsx))는 창이 열린 동안 `editor.chain().focus()` 를 부르지 않는다 — 포커스를 되돌리면 넣을 자리가 창을 열기 전과 달라진다. 넣은 뒤에도 창을 닫지 않는다(옛한글은 한 음절만 고치는 일이 드물다). ⚠️ `⟦ ⟧` 표기를 붙여 넣으면 **방점 칸을 잠근다**(코덱스 리뷰 4R) — 음절이 여럿이면 어느 음절에 붙일지 알 수 없어 고른 방점이 조용히 무시된다. 그때 방점은 표기 안에 `⟦ㄴㆍ:⟧` 로 적는다.
  - **자모 표는 손으로 둔다**([jamo-tables.ts](src/lib/yet-hangul/jamo-tables.ts)). `NFKD` 로는 자리를 알 수 없다 — 호환 자모는 낱자마다 **한 자리로만** 풀린다(ㄱ→초성, ㅄ→종성). 대신 테스트가 NFKD 로 **교차검증**해 코드포인트 오타를 잡는다.
  - 옛 음절은 UTF-16 3~4단위라 `OCR_HTML_MAX`(20,000)를 현대 글의 3배 속도로 먹는다 — 한 쪽 분량에는 넉넉하고, 잘리면 기존 경고가 이미 알린다.
  - 방점(U+302E/302F)은 결합 문자라 **정규식 문자 클래스에 넣으면 `no-misleading-character-class` 에 걸린다** — 대안(`|`)으로 쓴다.
  - ⚠️ **남은 한계: 작품명·카테고리 이름은 NFC 규칙 그대로다.** `normalizeCategoryName`(과 그 1:1 거울인 `exam.normalize_category_name`)은 NFC 를 쓰는데, 제목에 `바+ᇰ` 같은 **섞인 모양**이 들어오면 목록 줄에서 두 글자로 쪼개져 보인다. 본문과 달리 여기서 `normalizeYetHangul` 을 쓰지 않는 까닭은 **DB 함수와의 1:1 거울을 깨면 표기가 갈려 중복 폴더가 생기기 때문**이다(2026-08-22 항목). 제목은 거의 현대 표기라 실제로 부딪힌 적은 없다 — 고치려면 sql 마이그레이션으로 **양쪽을 함께** 바꿀 것.
  - ⚠️ **자모·방점·PUA 는 소스에 `\uXXXX` 로 적는다**(코덱스 2R). 날글자로 두면 편집기에서 빈칸·네모로 보여 범위가 맞는지 눈으로 확인할 수 없고, `ᄒᆞᆫ`(자모)과 `한`(완성형)을 구분할 수도 없다. 주석의 예시만 사람이 읽으라고 날글자로 둔다.
- [2026-09-15] **지문으로 O,X·단답형을 만든다 — 저장하지 않고 인쇄까지가 한 자리다**([src/lib/passage-quiz/](src/lib/passage-quiz/), [/problems/quiz](src/app/(main)/problems/quiz/page.tsx)). 되돌리지 말아야 할 판단들:
  - ⚠️ **만든 문항을 DB 에 넣지 않는다**(사용자가 고른 안). `exam.problems` 로 저장하려면 `question_type` CHECK(객관식·주관식·서술형)에 유형을 더하고, `source_id NOT NULL` 때문에 출처 없는 문항을 위한 가짜 출처를 만들고, `'객관식'` 리터럴로 갈리는 인쇄 렌더러 **세 곳**(PaperPrintBlocks·AnswerKey·AnswerSheet)을 함께 고쳐야 한다 — 그것은 이 기능이 아니라 문항 모델을 바꾸는 일이다. 새로고침하면 사라지므로 화면이 `beforeunload` 로 먼저 알린다
  - ⚠️ **프롬프트와 파서는 한 쌍이다**(`print-words` 와 같은 규약). 근거 구절과 단답형 답이 지문에 글자 그대로 있는지 파서가 기계로 대조하고, 프롬프트는 "기계가 대조한다 — 없으면 버려진다" 고 **모델에게 알려 준다**. 한쪽만 느슨하게 하면 ① 프롬프트만 풀 때는 버려지는 문항이 늘어 "왜 세 개뿐이지" 가 되고 ② 파서만 풀 때는 지문에 없는 내용이 시험지에 실린다
  - ⚠️ **대조는 `foldStrict` 로 한다**(`foldLoose` 아님). 느슨하게 접으면 공백이 사라져 '아버지가 방에' 와 '아버지 가방에' 가 같아지고, 지어낸 문장이 그대로 통과한다(2026-09-15 concept-pick 항목과 같은 근거)
  - ⚠️ **X 는 '지문과 어긋나는' 문장이지 '지문에 없는' 문장이 아니다.** 적혀 있지 않은 것은 참·거짓을 가릴 수 없어 학생이 맞다고 볼 근거도 틀리다고 볼 근거도 없다. 프롬프트가 주체·대상·때·정도·인과 **가운데 하나만** 바꾸라고 못박는다
  - **개수는 비우면 AI 가 정하고, 적으면 그만큼이다**(사용자가 고른 안). 상한 하나(`PASSAGE_QUIZ_MAX_PER_TYPE`)가 스키마 `maxItems`·프롬프트 문구·파서 자름 **셋을 먹인다** — 갈라지면 엄격 스키마 위반으로 출력이 통째로 실패한다. 요청 개수를 스키마에 넣지 않는 까닭도 같다(요청마다 `maxItems` 가 흔들린다)
  - **번호는 `numberQuizItems` 한 곳에서 매긴다.** 문제지와 정답표가 각자 세면 문항을 하나 뺐을 때 두 인쇄물이 어긋나고, 그 사실은 채점할 때에야 드러난다
  - **지문은 문단마다 한 블록으로 넘긴다.** 한 덩어리로 주면 한 쪽에 못 담을 때 `A4Document` 가 통째로 축소해 글씨가 작아진다. 문단 안의 줄바꿈은 **지운다**가 아니라 `white-space: pre-wrap` 으로 살린다 — 시는 행갈이가 곧 내용이다
  - **입력이 평문이라 `dangerouslySetInnerHTML` 을 쓰지 않는다.** 붙여넣기든 아카이브 지문(`htmlToPlainText`)이든 텍스트 노드로 그린다 — 정화를 한 겹 더 두는 것보다 HTML 을 아예 안 만드는 편이 확실하다
  - ⚠️ **지문 고르기는 목록에서 `html` 을 읽지 않고 `.or()` 도 쓰지 않는다**([passage-search.ts](src/lib/problem-bank/passage-search.ts)). 본문은 고른 하나만 `fetchPassagesByIds` 로 읽는다(서른 줄이면 응답이 커진다). 제목·지은이는 `ilike` 두 번을 병렬로 돌려 합친다 — `.or()` 는 이스케이프가 인용을 통과하며 풀린다(`queries.ts` 검색과 같은 이유)
  - ⚠️ **`lib/passage-quiz` 는 `@/lib/concept-pick` 배럴을 값으로 import 하지 않는다.** 그쪽 `index.ts` 가 `run.ts` 를 재수출해 순환이 된다(2026-09-11 장애와 같은 모양) — `fold`·`plain-text` 는 파일 경로로 가져온다
  - **새 `AiFeature` `passage_quiz` 를 더했다**(다섯 곳 함께: 타입·flags·status 라우트·useAiEnabled·`ocrStillEnabled` 호출부). `print-words` 와 달리 기존 기능의 일부가 아니라 **다른 화면의 다른 일**이라 플래그를 따로 뒀다 — 마스터는 여전히 `AI_OCR_BETA` 하나다
  - ⚠️ **인쇄는 입력칸이 아니라 '만들 때 쓴 지문' 을 싣는다**(`usePassageQuiz` 의 `source`). 만든 뒤에도 입력칸은 고칠 수 있어서, 입력칸을 실으면 **바꿔 놓은 지문 + 옛 지문으로 낸 문항**이 한 장에 찍힌다(코덱스 리뷰 1)
  - ⚠️ **`beforeunload` 하나로는 모자라다**([useUnsavedGuard.ts](src/hooks/useUnsavedGuard.ts), [nav-guard.ts](src/lib/nav-guard.ts)). 사이드바 링크로 옮겨 가는 길은 문서를 떠나는 것이 아니라 그 이벤트가 **아예 발화하지 않는다** — 앱 라우터에 막을 방법이 없어 링크 누름을 잡아채는 단계에서 확인한다(코덱스 리뷰 2)
  - ⚠️ **이탈 확인창이 닿지 못하는 길이 있다**(코덱스 리뷰 2R): 브라우저 뒤로 가기와 링크가 아닌 단추(로그아웃). 뒤로 가기를 막으려면 히스토리에 가짜 항목을 밀어 넣고 `popstate` 에서 되밀어야 하는데 어긋나면 **사용자를 화면에 가둔다** — 막으려던 손실보다 나쁘다고 보고 하지 않았다. 대신 목록 머리에 '저장되지 않아요' 를 문항이 있는 동안 계속 띄운다
  - ⚠️ **접은 뒤에도 비었는지 본다.** `'|'`·`'#'` 는 `foldStrict` 로 접으면 빈 글자가 되는데 **빈 글자는 무엇에든 들어 있어** 대조를 그냥 통과한다(코덱스 리뷰 5)
  - ⚠️ **중복 키는 모든 검사를 통과한 뒤에 넣는다.** 먼저 넣으면 뒤 검사에 걸려 버려진 문항이 자리를 맡아, 같은 물음의 **멀쩡한 문항**이 중복으로 몰려 함께 사라진다(코덱스 리뷰 6)
  - ⚠️ **O/X 별칭표는 객체가 아니라 `Map` 이다.** 객체면 `'__proto__'` 같은 값에 **물려받은 속성**이 잡혀 O 도 X 도 아닌 것이 통과하고, 그 값은 화면에서 그릴 수도 없다(코덱스 리뷰 10)
  - **지문 쪼개기는 줄 수와 글자 수를 함께 본다.** 비문학 지문은 줄바꿈 없이 몇천 자가 **한 줄**로 들어와 줄 수만 보면 블록이 하나뿐이고, 그러면 인쇄가 통째로 축소한다. 빈 줄은 나누며 사라지므로 **새 문단인지 표시해 돌려주고**(`PassageBlock.newParagraph`) 그 여백을 인쇄에서 되돌린다 — 자리가 모자라 쪼갠 조각과 진짜 연 경계는 뒤에서 구분할 수 없다(코덱스 리뷰 4·11)
  - **고른 지문의 제목·지은이는 비어 있어도 그대로 덮는다.** 앞의 값을 남기면 엉뚱한 작품 이름이 다른 글 위에 찍히고 AI 에게도 그렇게 넘어간다(코덱스 리뷰 9)
  - **사이드바 '기출 문제' 를 '문제 은행' 으로 바꿨다.** 기출뿐 아니라 지문으로 직접 만드는 문항까지 담게 됐기 때문이다. ⚠️ 구간 id 는 `'problems'` **그대로** 둔다(접힘 상태 저장 키다). '기출 업로드'·'학교 기출'·`'내신기출'` 같은 말은 **'과거 시험' 이라는 도메인 뜻**이라 바꾸지 않았다
- [2026-09-14] **학교·학년·시험은 스캔마다 한 번만 묻는다 — 프린트 이름은 그 제목에 붙는다**([sql/29](sql/29_print_bundle_exam_meta.sql), [scan-meta.ts](src/lib/print-scan/scan-meta.ts)). 되돌리지 말아야 할 판단들:
  - **묻는 자리를 묶음에서 스캔으로 올렸다.** 한 번에 스캔해 오는 프린트는 거의 언제나 같은 학교·같은 시험 것이라, 묶음마다 물으면 같은 값을 대여섯 번 고르게 된다(그래서 `newBundleDraft` 가 앞 묶음에서 물려받는 우회로까지 있었다 — 그 우회로는 없앴다). 대신 **학기·중간/기말**을 새로 묻는다: 프린트는 시험 범위를 따라 모이는데 그 축이 아예 없었다
  - ⚠️ **DB 는 그대로 묶음 단위다**(`print_bundles.school_id`·`year`·`grade` + sql/29 의 `semester`·`exam_type`). 스캔 표로 올리지 않은 까닭은 읽는 쪽(`ensureSchoolMaterial`·카테고리 트리·`exam.sync_school_name` 트리거·목록 줄)이 **전부 묶음 행을 보기** 때문이다 — 올리면 그 모두가 조인을 하나 더 타고, 나중에 한 장만 고칠 길이 막힌다. 화면이 한 번 묻고 `toBundleInsert` 가 묶음마다 복사한다
  - ⚠️ **`bundleSheetCategory` 의 `semester` 는 계속 `''` 다.** 외부지문 계층은 `학교 > 년도 > 학년 > 프린트` 이고 학기는 그 자연키(`idx_categories_natural_key`)에 **이미 있다** — 값을 넣으면 이미 만든 시험지가 트리에서 다른 자리로 옮겨가고, ara-system 성적의 시리즈 이름(외부지문은 학년만 쓴다)도 갈라진다. 학기·시험은 묶음 행과 목록 줄에만 산다
  - **학교급은 저장하지 않는다**(sql/18·sql/29 와 같은 규약). 학교 목록과 학년 선택지를 좁히는 데만 쓰고 `grade`('중2') 접두사로 되찾는다(`levelFromGrade`). 학교급을 바꾸면 **학교·학년을 비운다** — 남겨 두면 목록에 없는 학교가 고른 채로 저장되고 중1 이 고등학교에 붙는다. 학년도·학기·시험은 급과 무관하므로 남긴다
  - **저장되는 프린트 이름 = 스캔 제목 + 프린트별 이름**(`composePrintName` 한 곳). 그 값 하나가 `print_bundles.name` → `concept_sheets.unit`(시험지 제목·카테고리) → `school_materials.name`(트리 잎) → 단어 카테고리 `chapter` 로 그대로 흐르므로, 합치는 자리를 나누면 저장값과 화면 미리보기가 갈라진다. 트리 잎이 길어지는 것(`상현중 > 2026 > 중2 > "2026 상현중 중2 1학기 중간 봄봄 학습지"`)은 **알고 감수한 값**이다 — 사용자가 그렇게 요구했고, 시험지를 열었을 때 제목만으로 어느 시험 것인지 알 수 있다
  - **프린트별 이름은 비워도 되고, 대신 합친 이름이 겹치면 막는다**(`validateBundles`). 프린트가 한 장뿐인 스캔에 이름을 억지로 적게 하지 않는다. 겹치면 `school_materials` 의 `UNIQUE(name, school_id, year, grade)` 에 막혀 두 프린트가 트리에서 **한 자리를 쓰고** 시험지 제목까지 같아진다 — 조용히 삼켜지는 자리라 폼에서 미리 막는다(뒤에 온 묶음을 짚는다: 먼저 적은 것을 고치라고 하면 방금 친 것이 지워진다)
  - **시험지 제목은 `bundle.name` 그대로다**(`generateConceptTitle` 이 아니다). 이름에 이미 학년도·학교·학년이 들어 있어 그 생성기를 쓰면 `"상현중 2026 중2 2026 상현중 중2 …"` 가 된다. ⚠️ 옛 묶음을 '시험지 만들기' 로 다시 만들면 제목이 짧아진다(이미 만들어 둔 시험지는 그대로다)
  - **학교를 고르기 전에는 2·3단계를 열지 않는다.** 이름도 카테고리도 그 값에서 나오므로, 열어 두면 쪽을 다 묶은 뒤에야 "학교를 고르세요" 를 보게 된다. `validateScanMeta` 는 화면이 막는 것과 **별개로** `start()` 에서도 확인한다 — id 없이 저장하면 시험지는 만들어지는데 카테고리 트리에서만 사라진다(0.4.2 에서 고친 그 결함)
  - **썸네일 확대는 타일의 형제 단추다.** 타일 자체가 이미 '이 프린트에 넣기' 토글이라 그 안에 단추를 넣으면 잘못된 HTML 이고 키보드로도 못 쓴다. 썸네일 판을 키우는 안은 버렸다 — 한 화면에 몇 장 안 들어와 묶는 일 자체가 번거로워진다
  - ⚠️ **크게 보기는 문서를 한 번만 연다**([usePdfPagePreview.ts](src/hooks/usePdfPagePreview.ts)). 쪽을 넘길 때마다 열면 그때마다 파일 전체를 복사한다. 로딩은 state 가 아니라 **'무엇을 이미 그렸는가'(파일+쪽)에서 파생**한다 — 효과 안의 동기 setState 는 lint 가 막는다(아래 2026-09-08 ④)
  - ⚠️ **실패했다고 문서를 무조건 놓지 말 것**(코덱스 리뷰). **여는 데** 실패한 약속만 놓는다 — 붙들면 다음 쪽도 같은 실패를 물려받고, 실패한 약속에는 닫을 문서가 없어 놓아도 새지 않는다. **그리는 데만** 실패했으면 문서는 멀쩡히 열려 있으므로 **그대로 든다**: 놓으면 정리가 그 문서를 못 찾아 `destroy` 를 영영 못 하고, 실패를 되풀이할수록 열어 둔 PDF 가 쌓인다([usePdfPagePreview.test.tsx](src/hooks/usePdfPagePreview.test.tsx) 가 고정한다)
  - **편집 화면 원본 패널은 다시 그리지 않는다.** 거기엔 이미 올려 둔 원본 이미지(`PAGE_SCALE 2`)가 있어 그대로 크게 보면 된다 — 같은 창(`PagePreviewDialog`)을 쓰되 그림의 출처만 다르다. 앞뒤 이동은 `neighborPages` 로 **목록에서의 이웃**이다: 묶음이 덮는 쪽은 `[2, 5, 7]` 처럼 띄엄띄엄이라 `page ± 1` 이 통하지 않는다
  - **신규 부트스트랩 범위는 01~29 이다**
- [2026-09-14] **카테고리 관리의 외부지문 탭을 없애고, 프린트 단어를 자동 등록한다**([sql/28](sql/28_print_bundle_words.sql), [src/lib/print-words/](src/lib/print-words/)). 되돌리지 말아야 할 판단들:
  - **프린트를 만드는 길을 하나로 줄였다.** `school_materials` 를 만드는 곳은 이제 `ensureSchoolMaterial`(프린트 업로드·'시험지 만들기') **하나뿐**이고, 앱에 프린트 이름을 고치거나 지우는 화면은 없다(`updateSchoolMaterial`·`deleteSchoolMaterial` 삭제). 0.4.2 에서 학교 CRUD 를 없앤 것과 같은 근거다 — 양쪽에 손으로 적으면 같은 프린트가 두 표기로 갈라져 트리가 쪼개진다
  - ⚠️ **받아들인 한계**: 이름 변경 전파의 정본이 DB 트리거 `exam.sync_school_material_name`(sql/26) 하나가 됐는데 **앱에서 그것을 발화시킬 길이 없다.** `deleteBundle` 은 `school_materials` 행을 지우지 않으므로(다른 스캔의 같은 프린트·그 카테고리에 매달린 단어가 있을 수 있다) 지운 프린트의 이름이 카테고리 트리에 남을 수 있다 — 정리와 rename 은 SQL 로 한다. 앱에 되살릴 때는 `updateSchoolMaterial` 의 **`year`/`grade` 로 좁히는 `.eq()` 체인**(아래 2026-08-15 항목)을 반드시 함께 되살릴 것
  - **`CategoryForm` 의 "등록된 항목이 없습니다" 를 셋으로 갈랐다**(`noPublishers`/`noSchools`/`noMaterials`). 한 문장으로 묶여 있어서 학교가 없을 때도 "카테고리 관리에서 등록" 이라고 말했는데, 지금 그 말은 **셋 다 틀리다** — 출판사는 카테고리 관리, 학교는 관리자시스템, 프린트는 프린트 업로드다. 가는 곳이 다르면 문장도 달라야 한다
  - **단어 뽑기는 프린트 읽기(OCR)와 다른 턴이다.** 같은 턴에 묶지 않은 까닭 둘: ① 목록의 '단어 등록' 버튼으로도 돌아야 하는데 그 길에는 원본 PDF 를 다시 열 이유가 없다(`ocr_html` 이 이미 있다), ② OCR 은 3쪽씩 나눠 읽어서 단어가 배치마다 조각나 **병합 규칙이 하나 더** 생긴다 — 완성된 본문을 한 번에 보면 그 문제가 아예 없다
  - ⚠️ **`print-words` 는 `print-scan/` 을 값으로 import 하지 않는다.** `print-scan/index.ts` 가 `run.ts` 를 다시 내보내므로 `print-words` 가 `print-scan/save` 를 부르면 순환이 된다(2026-09-11 장애와 같은 모양, `import-cycles.test.ts` 가 잡는다). 그래서 `registerBundleWords` 는 **카테고리와 영수증 저장 함수를 인자로 받는다** — 호출자(`run.ts`·훅)가 `bundleWordsCategory`·`updateBundle` 을 넘긴다
  - ⚠️ **`registerBundleWords` 는 절대 던지지 않고, 단어 단계는 `runBundle` 의 try/catch 바깥에 있다.** 시험지는 그 시점에 이미 저장돼 있어서, 던지면 catch 가 **멀쩡한 묶음을 '실패' 로 되돌린다.** 무슨 일이 있었는지는 `words_meta` 영수증과 `onWarnings` 로만 말한다
  - **영수증은 `ocr_meta` 가 아니라 `words_meta` 다.** '다시 읽기' 가 `ocr_meta` 를 통째로 덮어쓰는데 단어 등록은 목록 버튼으로 **따로** 돌 수 있어 수명이 다르다. 같은 까닭으로 **'확인 필요' 칩에 단어 경고를 섞지 않았다** — 그 칩은 "시험지 본문을 확인하라" 는 뜻이라 개수가 부풀면 무엇을 보라는 것인지 흐려진다(단어는 별도 칩 + 툴팁)
  - ⚠️ **뜻은 지어내지 않는다.** 프린트에 뜻이 안 적힌 단어는 `meaning: ''` 로 받아 **등록하지 않고 이름을 경고로 돌려준다**(`dropped.noMeaning`). 사전 뜻을 채우게 하면 학생이 외울 답이 선생님이 나눠 준 프린트와 달라지고, 그 어긋남은 시험에서야 드러난다. 사용자가 고른 규칙이다
  - ⚠️ **`words.word`/`meaning` 에는 CHECK 가 없다** — `NOT NULL` 뿐이라 공백만 든 값이 그대로 들어간다. 그래서 `parsePrintWords` 가 trim 뒤 빈 값을 **양쪽 다** 거부한다. 본문에 글자 그대로 없는 말(`plain.includes`)도 버린다 — 모델이 사전에서 가져온 것이라 프린트와 어긋난다
  - **카테고리 자연키는 `bundleSheetCategory` 에서 변환한다**(`bundleWordsCategory`). 카테고리 모양이 셋이라(`BuilderCategory` unit/subunit · `CategoryMatchInput` chapter/subChapter · `CategoryNaturalKeyFields` sub_chapter/school_name) 형제 함수로 두면 언젠가 한쪽만 고쳐져 **시험지와 단어가 다른 폴더**로 갈라진다. `bundle-plan.test.ts` 가 두 자연키가 같은지 고정한다
  - **뜻 있는 단어가 0개면 카테고리를 만들지 않는다.** `ensureCategoryId` 를 먼저 부르면 단어가 하나도 없는 카테고리가 트리에 남아, 선생님이 눌러 보고 빈 화면을 본다
  - **기능 플래그는 `print_ocr` 을 재사용한다.** 새 `AiFeature` 는 다섯 곳을 함께 고쳐야 하는데(위 2026-09-14 항목) 이것은 프린트 읽기의 일부이지 다른 기능이 아니다
  - **다시 등록은 append-only 다**(`insert_words_batch` 의 `ON CONFLICT DO NOTHING`). 멱등이지만 **지운 단어를 되살리지도, 옛 단어를 지우지도 않는다** — 그래서 '단어 다시 등록' 은 확인창을 한 번 거친다(누를 때마다 ChatGPT 를 한 번 쓴다)
  - ⚠️ **영수증은 누적값과 마지막 시도를 가른다**(`words_meta.wordCount` vs `status`/`registered`/`skipped`, 코덱스 리뷰 3R·4R). 다시 등록하면 전부 중복이라 `registered` 가 0 이고, 다시 등록하다 **실패하면** 그 시도의 숫자가 전부 0 인데 단어는 DB 에 그대로 있다. 마지막 시도의 숫자로 판단하면 실패 한 번에 칩이 '단어 없음' 이 되고 **단어 관리 링크와 삭제 확인창의 "단어는 남습니다" 안내가 통째로 사라진다.** `registeredWordCount`(=`wordCount`)가 그 셋의 단일 출처이고 `finish` 가 `nextWordCount` 로 이어 붙인다 — **`max(previous + registered, registered + skipped)`** 다. 단순히 `max(previous, 이번 총계)` 로 하면 부분 겹침을 놓친다(10개에 새로 5개가 들어가도 10으로 남는다, 코덱스 리뷰 5R)
  - ⚠️ **뜻도 본문과 대조한다**(코덱스 리뷰 3R). 단어만 `plain.includes` 로 보면, 본문에 있는 단어에 모델이 **사전 풀이를 붙여 와도 그대로 저장된다** — '뜻은 지어내지 않는다' 가 깨지는 자리다. 본문도 같은 규칙으로 공백을 접어 비교하고(프린트에서 줄이 바뀐 뜻을 놓치지 않게), 끝의 마침표 하나만 봐준다. 못 맞춘 것은 `dropped.unverified` 로 이름째 알린다. 프롬프트도 "한 글자라도 바꿔 쓰면 등록되지 않는다" 고 밝힌다 — **프롬프트와 파서는 한 쌍이라 한쪽만 느슨하게 하지 말 것**
  - ⚠️ **취소는 생성이 끝난 뒤에도 본다**(코덱스 리뷰 4R). `signal` 은 `generateDraft` 에만 전달되므로, 사람이 취소하거나 화면을 떠난 뒤에도 `ensureCategoryId`·`insertWordsToCategory` 가 그대로 돌아 **공유 카테고리에 단어가 들어간다.** 생성 직후와 카테고리 왕복 직후에 `signal.aborted` 를 확인한다
  - ⚠️ **잠금은 첫 await 앞에서 건다**(코덱스 리뷰 1R). 킬스위치(`ocrStillEnabled`)를 먼저 물으면 그 왕복 동안 `abortRef` 도 `running` 도 비어 있어 두 번 누르면 생성이 둘 돌고, 그 사이 시작한 프린트 읽기도 화면의 busy 잠금을 그냥 지나간다
  - **단어 등록도 진행 표시와 취소를 내준다**(코덱스 리뷰 2R). 도는 동안 목록 버튼이 전부 잠기는데 아무 표시가 없으면 최대 2분을 멈춘 화면으로 본다. 진행률 막대는 쓰지 않는다 — 한 번의 생성이라 셀 단계가 없고 0% 막대는 멈춘 것처럼 보인다
  - **AI 작업 잠금은 목록 화면에서 하나로 합친다**(`ocr.running || words.running || list.busyId`). 따로 두면 읽는 도중에 '단어 등록' 을 눌러 같은 브릿지로 두 생성이 겹친다
  - **`/words?categoryId=` 딥링크는 페이지가 읽어 훅에 넘긴다**(`useWordsManager(initialCategoryId)`). 훅에서 `useSearchParams` 를 부르면 이 훅을 쓰는 모든 화면이 Suspense 경계를 갖춰야 한다(`words/print/page.tsx` 선례). 자동 선택은 **카테고리 목록을 읽은 그 자리에서 한 번만** 한다 — 따로 효과를 두면 사람이 고른 카테고리를 나중에 덮어쓰고, 못 찾은 경우 계속 다시 시도한다
  - **신규 부트스트랩 범위는 01~28 이다**
- [2026-09-14] **학교 마스터 연동 — `exam.schools` 를 관리자시스템 `public.schools` 의 거울로**([sql/27](sql/27_school_master_link.sql), ara-system mig481). 되돌리지 말아야 할 판단들:
  - **증상은 "프린트 스캔에 학교가 두 곳뿐"이었지만 원인은 표가 두 벌이었던 것이다.** 기출은 sql/18 에서 이미 `public.schools`(중등 15·고등 5)로 옮겨 갔는데 **프린트만 옛 `exam.schools`** 를 보고 있었다 — 그 표는 선생님이 카테고리 관리에서 손으로 타이핑한 이름만 담겨 운영에 3행(도선고등학교·선화예술중학교·`전체`)뿐이었다. 스캔·OCR 쪽은 멀쩡했다
  - **거울의 id 를 마스터와 같게 맞춘다.** 그래야 `school_materials.school_id` 의 하드 FK([sql/01:98](sql/01_schema.sql))와 `exam.sync_school_name` 트리거를 **그대로** 쓴다. FK 를 떼고 스냅샷으로 가는 길(기출이 택한 `problem_sources` 규약)도 있었지만, 외부지문은 `학교 > 년도 > 학년 > 프린트` 계층이 FK 로 묶여 있어 떼면 CASCADE·트리거 규약이 통째로 느슨해진다
  - ⚠️ **마스터 20개를 거울에 미리 복사하지 않는다 — 필요할 때 한 행씩 만든다**(`ensureSchoolMirror`). `getAllSelectableCategories`(개념지 편집기 카테고리 바·기출 단원 트리)가 `exam.schools` 를 **그대로 그리므로**([aggregate.ts](src/lib/category-master/aggregate.ts) 의 `fetchAll('schools')`) 미리 복사하면 프린트가 하나도 없는 학교 18개가 트리를 채운다. **(2026-09-14 갱신)** 근거로 적혀 있던 '카테고리 관리의 외부지문 트리' 는 그 탭을 없애면서 사라졌지만 **규칙은 그대로 유효하다**. 기출 트리에서 이미 같은 이유로 거부한 모양이다(아래 2026-09-08 항목)
  - **선택지는 마스터 ∪ 거울의 합집합**이다([school-options.ts](src/lib/category-master/school-options.ts) `mergeSchoolOptions`). 합집합인 까닭은 **보존**이다 — 마스터에서 학교가 지워져도 그 학교로 만들어 둔 프린트가 남으므로 선택 칸에서 사라지면 편집기가 '고른 자리가 빈' 모양이 된다. 학교가 아닌 옛 임시 행 `'전체'`(고전시가 프린트 1건이 달려 있다)도 이 합집합이 지킨다. 합집합은 보존일 뿐 **새 학교를 만들지는 못한다**. 옛 항목은 `schoolOptionLabel` 이 꼬리표를 달아 마스터에 있는 학교로 오해하지 않게 한다
  - **`createSchool`/`updateSchool`/`deleteSchool` 을 없앴다.** 학교를 만들고 고치는 곳은 관리자시스템뿐이다 — 이 함수들이 살아 있는 한 손으로 적은 이름이 다시 갈라진다. `MasterListPanel` 의 세 핸들러를 **옵셔널**로 바꿔 없으면 버튼 자체를 안 그린다(아무 일도 안 하는 no-op 핸들러를 넘기지 말 것 — 눌리는데 조용한 것이 가장 나쁘다)
  - ⚠️ **선택지의 값은 이름이 아니라 학교 id 다.** `public.schools.name` 에는 UNIQUE 가 없어(관리자 화면이 클라이언트에서만 중복을 막는다) 이름으로 id 를 되찾으면 동명 학교가 생기는 순간 조용히 엉뚱한 학교에 붙는다. `validateBundles` 도 `schoolName` 이 아니라 **`schoolId`** 를 요구한다 — 이름만 남은 묶음은 시험지는 멀쩡히 만들어지는데 `ensureSchoolMaterial` 이 아무것도 못 해 트리에서만 사라진다
  - ⚠️ **`ensureSchoolMaterial` 의 `.catch(() => undefined)` 를 되살리지 말 것.** 그 한 줄이 FK 위반을 통째로 먹어서, 프린트가 카테고리 트리에 안 올라가도 화면에 아무 말이 없었다(이 결함을 **조용하게** 만든 장치가 그것이다). 이제 중복(`23505`)만 정상으로 치고 나머지는 `onWarnings` 로 올린다 — sql/26 의 '경고를 ocr_meta 에만 넣고 완료라고 말하지 않는다' 와 같은 규약
  - **`usePrintSheetList.createSheet`('시험지 다시 만들기')도 `ensureSchoolMaterial` 을 부른다.** 예전엔 읽기 경로에만 있어서, 그 길로 만든 시험지는 트리에서 빠져 있었다(기존 결함을 이번에 함께 고쳤다)
  - **이름 변경 전파는 두 겹이다.** ara-system mig481 트리거가 `public.schools` rename 을 거울에 즉시 옮기고, 그 UPDATE 가 기존 `exam.sync_school_name` 을 발화시켜 categories·concept_sheets·print_bundles 까지 간다. `ensureSchoolMirror` 의 upsert(`onConflict: 'id'`)가 **같은 일을 지연으로** 한 번 더 한다(트리거가 없는 환경의 안전망). ⚠️ 전파 UPDATE 세 개를 ara-system 쪽에 **복제하지 말 것** — 정본은 exam 쪽 트리거 하나다. mig481 은 넣기 전에 `exam.normalize_category_name` 을 태운다(안 태우면 관리자가 적은 공백·전각 괄호가 스냅샷에만 날것으로 남아 선택 칸이 그 학교를 못 찾는다)
  - ⚠️ **거울의 이름은 화면 값이 아니라 `fetchMasterSchoolName` 으로 되읽어 쓴다**(코덱스 리뷰 P1). 화면이 든 이름은 폼을 연 시점의 스냅샷이라, 그 사이 관리자가 이름을 바꿨으면 저장하는 순간 그 upsert 가 **관리자의 변경을 통째로 무른다** — 트리거가 아래 스냅샷까지 옛 이름으로 되돌린다. 탭 하나가 전체를 되돌릴 수 있는 모양이었다
  - ⚠️ **그래서 `ignoreDuplicates` 가 `!authoritative` 다**(코덱스 정지 리뷰). 되읽기가 **실패**했을 때 화면 값으로 물러서는 것까지는 좋은데, 그 값으로 **이미 있는 행을 덮으면** 위의 결함이 그대로 되살아난다 — 마스터 조회가 잠깐 실패한 것만으로 관리자의 이름 변경이 무효가 된다. 행을 **처음 만들 때**는 되돌릴 이름이 아예 없으니 화면 값이라도 쓰고, **이미 있으면 건드리지 않는다**. 마스터에 짝이 없는 옛 항목(`'전체'`)도 같은 길로 보호된다
  - ⚠️ **이름이 겹쳐 UNIQUE 위반(`23505`)이 나면 같은 이름의 다른 행에 붙이지 않는다**(코덱스 리뷰). `exam.schools.name` 은 UNIQUE 인데 `public.schools.name` 은 아니라, 동명 학교 둘이 생기면 두 번째 프린트가 **첫 번째 학교 밑으로 조용히 들어간다** — 트리에서 안 보이는 것보다 나쁘다. 붙이지 않고 경고한다. 같은 까닭으로 `useCategoryFormState` 의 이름→id 역추적은 **이미 그 이름의 학교를 고른 상태면 손대지 않는다**(안 그러면 방금 고른 두 번째 학교를 첫 번째로 되돌린다)
  - **정규화 경계는 `mergeSchoolOptions` 한 곳이다.** 마스터 이름은 `normalizeCategoryName` 규약 밖에 있는데 그 값이 그대로 `categories.school_name` 으로 복사돼 트리 노드를 묶는다. (운영 대조 결과 현재 마스터 27개 이름은 전부 정규화가 no-op 이라 이번 이관에서 바뀐 이름은 없다)
  - **sql/27 은 전부를 `DO` 블록 하나에 가뒀다.** Supabase SQL Editor 는 문마다 다른 백엔드로 갈 수 있어 TEMP TABLE 이 다음 문에서 사라진다(아래 2026-08-30 항목). DO 블록은 단일 문이라 통째로 한 트랜잭션이고 `RAISE EXCEPTION` 이면 전부 되돌아간다. **앱 배포보다 먼저 적용한다**
- [2026-09-16] **학교 프린트 문답 시험지 — 문제지·교사용·답지 + AI 모범답안**([sql/31](sql/31_print_bundle_qa.sql), [src/lib/print-qa/](src/lib/print-qa/), [qa 화면](src/app/(main)/print-sheets/[bundleId]/qa/page.tsx)). 되돌리지 말아야 할 판단들:
  - **빈칸 시험지와 별개의 축이다.** 같은 `ocr_html` 에서 나오지만 개념지(`concept_sheets`)를 건드리지 않고 `print_bundles.qa_items`/`qa_meta` 에 따로 쌓는다 — 한쪽을 다시 만들어도 다른 쪽이 안 사라진다. 화면도 둘이다(`/print-sheets/[id]` ↔ `/print-sheets/[id]/qa`)
  - ⚠️ **`qa_meta` 를 `ocr_meta` 에 합치지 말 것**(`words_meta` 와 같은 근거) — '다시 읽기' 가 `ocr_meta` 를 통째로 덮어쓰는데 문답은 그 뒤에도 살아 있어야 한다
  - ⚠️ **원문이 바뀌어도 문답을 지우지 않는다.** `qa_meta.sourceHash`(FNV-1a) 가 어긋나면 화면이 알리기만 하고 다시 나눌지는 사람이 정한다(`isQaStale`) — 거기에 **선생님이 손으로 고친 답과 AI 모범답안**이 들어 있어 말없이 버리면 그 값을 다시 치러야 한다
  - ⚠️ **물음과 답의 대접이 다르다**(`parse-split.ts`). 원문에 글자 그대로 없을 때 **물음은 남기고 `verified:false`** 로 짚고(문항이 통째로 사라지면 있었다는 것조차 모른다), **답은 비운다**(지어낸 답이 `printed` 로 들어가면 모범답안 대상에서 빠지고 검증 없이 교사용에 찍힌다). 띄어쓰기 하나로도 대조가 실패하므로 물음까지 버리면 잃는 것이 너무 크다
  - ⚠️ **손글씨는 모델에게 묻지 않는다** — `include_handwriting` 을 켜고 읽은 묶음의 `<em>` 자리로만 가린다([handwriting.ts](src/lib/print-qa/handwriting.ts)). 끈 묶음에서는 `<em>` 을 **아예 보지 않는다**(그때는 인쇄된 기울임이고, 손글씨 답은 애초에 안 옮겨져 `none` 이다)
  - ⚠️ **앞글(`lead`)도 묻지 않고 원문에서 잘라 온다**([lead.ts](src/lib/print-qa/lead.ts)). 물어서 받으면 그 글이 원문에 있는지 또 대조해야 하고 길이 상한에 걸려 잘려 온다. 자를 때 **평문 구조 기호(`#`·`| 칸 |`)를 되돌리고**(안 그러면 학생 문제지에 `# 동백꽃` 이 찍힌다) **끝에 붙은 문항 번호를 뗀다**(안 그러면 번호가 두 번 찍힌다)
  - **번호는 프린트에 인쇄된 것을 그대로 찍는다**(사용자 결정). 세 인쇄물이 `numberPrintQaItems` **한 함수**를 쓴다 — 각자 세면 문항을 뺐을 때 어긋나고 채점할 때에야 드러난다. 문항을 빼면 번호가 비는 것은 감수한 값이다
  - ⚠️ **모범답안은 근거를 못 찾아도 버리지 않는다**(O,X·단답형과 정반대다 — 사용자 결정 2026-09-16). 서술형은 자료를 **종합해서** 쓰는 것이 보통이라 근거 대조로 버리면 빈 문항만 남는다. 대신 `evidenceSource: null` 로 두고 화면·교사용·답지에 **'근거 없음'** 을 반드시 찍는다. 단 **찾지 못한 근거 구절 자체는 싣지 않는다** — 찾을 수 없는 말을 인쇄하면 선생님이 원문을 뒤지다 시간만 버린다
  - 근거를 찾는 차례는 **프린트 본문 → 참고자료**(`parse-answers.ts`). 양쪽에 있으면 프린트(`''`)로 적는다 — 선생님이 손에 든 것이 그 프린트다
  - ⚠️ **참고자료 후보에서 자기 시험지를 뺀다**(`QuizMatchSignals.excludeSheetId`, [candidates.ts](src/lib/quiz-references/candidates.ts) 에서 **한 번에** 거른다). 개념지를 긁는 조회가 넷이라 조회마다 빼면 한 곳을 빠뜨리고 조용히 되살아난다. 안 빼면 그 프린트가 '같은 학교'·'제목에 …' 로 **스스로 붙어** 본문이 두 번 실리고, 답이 비어 있는 그 글이 근거 자료로 둔갑한다
  - **두 턴의 모델 정책이 다르다**: 나누기는 *옮겨 적기*라 프린트 읽기와 같은 값(`PRINT_OCR_MODEL/EFFORT_PREFERENCE`)을 쓰고, 모범답안은 *글짓기*라 선생님이 고른 모델을 그대로 쓴다(`run-env.ts` 와 같은 판단 — 싼 단계까지 높이면 한도만 태운다)
  - ⚠️ **참고자료가 없으면 '참고자료' 라는 말이 프롬프트 어디에도 안 나온다**(보안 문구의 대조 대상 이름까지 `{WHERE}` 로 함께 간다). [prompt.test.ts](src/lib/print-qa/prompt.test.ts) 가 글자로 고정한다 — 쓰지 않는 규칙을 실어 두면 모델이 없는 자료를 찾는다
  - ⚠️ **`서술`이 아니라 `서술하` 로 본다**([print-format.ts](src/lib/print-qa/print-format.ts) 의 `LONG_ANSWER_HINT`). 국어 프린트에는 '서술자는 누구인가?' 처럼 그 글자를 품은 **명사**가 흔해서, 어간만 보면 낱말 하나 쓰는 물음에 답 쓰는 줄이 네 줄 그어진다
  - ⚠️ **앞글에 답이 새지 않게 두 겹으로 막는다**(코덱스 1R 블로킹). ① 앞 문항을 원문에서 못 찾았으면 다음 문항의 앞글을 **아예 붙이지 않는다**(`withLeads` 의 `blocked`) — 끝난 자리를 모르니 그 물음과 답을 통째로 담게 된다. ② 앞글에서 `답:`·`정답:`·`→` 로 시작하는 줄을 **떼어 낸다**(`dropAnswerLines`) — 모델이 답을 조금 다르게 적어 답을 비운 문항 뒤에는 원문의 답이 그대로 남는다. 지문 한 토막을 잃는 쪽이 **학생 문제지에 답이 찍히는 쪽보다** 낫다
  - ⚠️ **문항의 끝 자리는 뒤로 물러서지 않는다**(`Math.max`, 코덱스 1R). 답을 되돌아가 찾으면(`locate` 의 0부터 재검색) 그 자리가 물음보다 앞일 수 있는데, 그대로 끝으로 삼으면 다음 앞글이 **방금 인쇄한 물음을 다시 담는다**
  - ⚠️ **중복 열쇠는 번호 + `foldStrict`** 다(코덱스 1R). 학교 프린트에는 '빈칸에 알맞은 말을 쓰시오.' 처럼 **똑같은 물음이 번호만 바꿔** 여러 번 나온다 — 글자만 열쇠로 삼으면 둘째부터 사라지고, `foldLoose` 로 접으면 다른 물음이 같은 물음으로 몰린다
  - ⚠️ **손글씨는 글자가 아니라 자리로 가린다**(코덱스 1R). 답 글자를 `<em>` 조각과 견주기만 하면 3번에 **인쇄된** '은유' 가 7번의 손글씨 `<em>은유</em>` 때문에 손글씨로 뒤바뀌어, 선생님이 인쇄해 둔 답이 AI 답으로 덮인다. `readPlainWithHandwriting` 이 평문을 만들면서 손글씨 구간을 **평문 좌표**로 함께 돌려주고, 태그 걷기는 `htmlToPlainText` 에 통째로 맡긴다(직접 걷으면 `&nbsp;`·`<br>` 이 본문과 다르게 풀려 멀쩡한 손글씨를 놓친다). **모델에게 보낼 평문과 파서가 대조할 평문은 이 한 번의 변환에서 나와야 한다**
  - ⚠️ **`editAnswer` 는 글자를 다듬지 않는다**(코덱스 1R). 칸이 그 값을 그대로 그리므로 여기서 `trim` 하면 **띄어쓰기를 칠 수가 없다**('관심 표현' → '관심표현'). 비었는지는 쓰는 자리에서 접어 본다
  - ⚠️ **저장과 AI 는 한 잠금**이다(`savingRef`, 코덱스 1R). 따로 두면 모범답안이 도는 동안 누른 저장이 **옛 문항을 나중에 덮어써**, 화면에는 새 답이 보이는데 DB 에는 옛 답이 남고 `dirty` 는 false 라 아무도 눈치채지 못한다. 화면도 두 단추를 서로 잠근다
  - ⚠️ **문항이 비어도 목록을 통째로 숨기지 않는다**(`items.length === 0 && !dirty`, 코덱스 1R). 마지막 문항을 지우면 목록이 비는데 그 삭제는 아직 저장 전이다 — 숨기면 저장 단추까지 사라져 되돌릴 길이 없다
  - ⚠️ **참고자료 신호는 `qa.meta` 를 본다**(코덱스 1R). 화면이 뜰 때 읽어 둔 `bundle.qa_meta` 를 보면 나누기가 프린트에서 작품명을 찾아내도 **새로고침 전까지** 그 신호로 자료를 못 찾는다. `qa_meta.references` 도 갈아 끼우지 않고 **더한다** — 몇 문항만 다시 만들면 앞서 만든 답은 남는데 목록만 새것이 되어 답지 머리글이 출처를 빠뜨린다
  - ⚠️ **앞글에서 답을 걷는 순서가 있다**(코덱스 2R). ① 평문 기호를 **먼저** 되돌린다 — 표 안의 답은 평문에서 `| 답: 비밀정답 |` 이라 '답:' 으로 시작하지 않는다. ② **줄 한가운데서 시작한 조각은 통째로 버린다** — 그 줄의 남은 부분은 앞 문항의 것이다. ③ 답 줄을 만나면 **거기서부터 끝까지** 버린다(한 줄만 빼면 두 줄짜리 답의 둘째 줄이 남는다). 답 뒤의 새 지문도 함께 잃지만 **잃은 지문은 보이고 새어 나간 답은 안 보인다**
  - ⚠️ **중복은 글자가 아니라 자리로 가린다**(코덱스 2R — 1R 의 '번호+글자' 열쇠를 뒤집음). 같은 물음이 지문마다 나오는 프린트에서 글자 열쇠는 둘째부터 지우고, 한 문항을 '1' 과 '' 로 두 번 낸 응답은 못 잡는다. 차례대로 찾으면 **진짜 반복은 다른 자리에 앉고** 모델이 두 번 낸 것은 **자리를 못 찾는다** — 그때만 글자(번호 뺀 `foldStrict`)로 가린다
  - ⚠️ **답은 제 구역 안에서만 찾는다**(`이 물음의 끝 ~ 다음 물음의 시작`, 코덱스 2R). 문서 전체에서 찾으면 한참 뒤 문항의 같은 답에 걸려 읽기 머리가 건너뛰고 **사이 문항들이 차례에서 밀려나 그 지문까지 사라진다**. 구역에 없으면 문서 전체에서 한 번 더 보되(답을 맨 뒤에 몰아 놓은 프린트가 있다) **그 자리는 문항의 끝으로 쓰지 않는다**
  - ⚠️ **`<em>` 짝을 정규식으로 잡지 않는다**(코덱스 2R). `<em>메모 <em>강조</em> 은유</em>` 에서 게으른 짝짓기는 첫 `</em>` 에 멈춰 뒤쪽이 손글씨에서 빠진다 — 깊이를 세어 바깥 짝만 표시한다. 표시 글자는 **공백 안쪽으로 밀고**(HTML·평문 두 단계에서) 빈 짝과 글 끝 찌꺼기를 없앤다 — 안 그러면 평문이 '손글씨를 안 읽은 묶음' 과 달라진다(공백이 겹치거나 빈 줄이 는다)
  - ⚠️ **줄머리 번호는 가장 긴 덩이부터 벗긴다**(`labelHeadLength`, 코덱스 38R). 첫 낱말만 보면 `( 1 ) (소설)` 에서 `(` 만 보고 번호를 못 알아봐 **그 선택지가 빈칸이 된다**
  - ⚠️ **같은 글자 따옴표의 짝은 그 줄에서만 센다**(`openTail`, 코덱스 38R). 글 전체로 세면 앞줄의 아포스트로피(`Don't`) 하나가 짝을 뒤집어 **인용된 자료의 닫는 따옴표**를 여는 것으로 보고 걷어낸다
  - ⚠️ **직선 따옴표도 문장 끝이다**(`SENTENCE_END`, 코덱스 38R). `"첫 지문이다." 1. 갈래는?` 의 번호를 못 알아보면 그 자리를 지켜 주지 못해 중복 응답이 집어 가고 진짜 문항이 사라진다
  - ⚠️ **부호가 붙은 소수(`-.5`)도 한 값이다**(코덱스 37R). 숫자로 시작하는 답만 보면 `.5` 가 새어 주어진 값이 사라진다 — 점으로 시작하는 답까지 본다
  - ⚠️ **줄머리의 표시·번호는 번갈아 벗긴다**(`markedPrefix`, 코덱스 37R). 칸이 나뉜 표(`| ① | (소설) |`)에서는 칸 표시와 번호가 번갈아 나와, 한 겹만 벗기면 '줄 전체' 로 안 보여 **그 선택지가 빈칸이 된다**
  - ⚠️ **앞글의 표 정리도 전각 칸 구분자(`｜`)를 본다**(코덱스 37R). 번호 자리 판정은 둘 다 알아보는데 여기만 반각을 고집하면 그 표의 **원본 번호가 앞글에 남아 두 번 인쇄된다**
  - ⚠️ **앞자리 없는 소수(`.5`)도 한 값이다**(코덱스 36R). 점 앞의 글자를 따지면 `.5` 의 `5` 가 답으로 새어 **주어진 값이 통째로 사라진다**
  - ⚠️ **표 칸 경계도 줄 끝이다**(`TRAILING_PUNCT`, 코덱스 36R). `| ① (소설) |` 의 닫는 `|` 때문에 '줄 전체' 로 안 보이면 그 선택지가 빈칸이 된다
  - ⚠️ **괄호까지 넓힌 구간으로 모든 보호를 건다**(코덱스 23R·36R). 안쪽 낱말의 줄만 보면 여러 줄에 걸친 괄호(`____ (\n소설\n)`)에서 **빈칸이 있는 줄을 지나쳐** 그 안내를 지운다
  - ⚠️ **목록 표시(`- `)도 번호 앞자리다**(`LIST_BULLET_END`, 코덱스 36R). `<li>` 가 만든 `- 1.` 을 못 떼면 원본 번호가 앞글에 남아 우리가 찍는 번호와 나란히 인쇄된다
  - ⚠️ **지시문 말끝의 느낌표도 받는다**(코덱스 35R). `쓰시오!` 를 빠뜨리면 그 지시문이 답으로 몰려 통째로 잘린다 — 학습지는 느낌표를 예사로 쓴다
  - ⚠️ **숫자는 부호·소수점까지가 한 값이다**(`standsAlone`, 코덱스 35R). `-3`·`3.5` 에서 `3` 만 답으로 집으면 그 자리를 답으로 삼아 **문제에 주어진 값이 깨진 채 남는다**(`-`·`.5`)
  - ⚠️ **소수점 뒤에는 공백이 없다**(`DECIMAL_END`, 코덱스 35R). `주어진 수는 3. 1. 절댓값은?` 의 `3. ` 까지 소수점으로 보면 뒤따르는 **번호를 못 알아보고**, 그 자리를 중복 응답이 집어 가 진짜 문항이 사라진다
  - ⚠️ **빈 답란은 괄호 속이 '온통' 빈칸일 때뿐이다**(`BLANK_ONLY`, 코덱스 34R). 안쪽 어딘가에 빈칸이 있다고 넘기면 `(소설(__))` 처럼 **채워진 답이 든 괄호**를 지나친다
  - ⚠️ **겹친 괄호는 가장 바깥까지 넓혀 본다**(`outerParens`, 코덱스 34R). 한 겹만 넓히면 `((소설))` 이 혼자 놓인 **선택지 줄**이 '줄 전체' 로 안 보여 빈칸이 된다. 빈칸으로 바꿀 때도 바깥까지 함께 바꿔 `((　　　))` 이 남지 않게 한다
  - ⚠️ **소수점은 문장 끝이 아니다**(`DECIMAL_END`, 코덱스 34R). `3.1.` 의 `1.` 을 번호로 보고 떼면 **주어진 값의 소수 부분이 깎인다**
  - ⚠️ **지시문 판정도 자모로 갈린 글(NFD)을 합쳐 본다**(`endsLikeQuestion`, 코덱스 33R). 맥에서 복사해 온 글은 `쓰시오` 가 자모로 갈려 와, 합친 꼴만 보면 **지시문이 답으로 몰려 통째로 잘린다**. 판정만 하므로 글자를 바꿔도 안전하다(배점 꼬리표·문장 경계도 같은 자리에서 합쳐 본다)
  - ⚠️ **겹친 괄호도 본다**(`hasFilledParens`, 코덱스 33R). 안쪽만 보면 `(은유(ㄱ))` 에서 번호 `(ㄱ)` 만 보고 **채워진 바깥 괄호를 지나친다**
  - ⚠️ **번호는 앞머리의 '끝' 에서 찾는다**(`labelHeadAt`, 코덱스 33R). `첫 지문이다. 1. 갈래는?` 처럼 지문과 번호가 한 줄에 있으면, 앞머리 전체를 번호로 견주다 **번호 없는 자리로 여겨** 중복 응답이 그 자리를 집어 가고 진짜 문항이 사라진다. 맞추기(`hasLabelBefore`)·지키기(`carriesLabel`)·앞글 떼기(`stripSameLineLabel`)가 **같은 스캔**을 쓴다
  - ⚠️ **번호 줄 떼기는 이 문항의 번호일 때만**(`stripLabelLine`, 코덱스 33R — 14R 의 '기호가 붙어 있으면 뗀다' 를 뒤집음). 기호만 보고 떼면 `다음 수를 보고 물음에 답하시오.\n(5)` 의 **주어진 값**이 사라진다. 번호를 모르는 응답에서는 원본 번호가 앞글에 남을 수 있지만 그것은 **눈에 보이는 흠**이다
  - ⚠️ **같은 줄 번호 떼기는 구두점을 요구하지 않는다**(코덱스 33R — 21R 을 대신한다). 동그라미 번호(`①`)에는 구두점이 없어 앞글에 그대로 남아 번호가 두 번 찍혔다 — 값을 지키는 것은 **문장이 끝난 자리** 조건(22R)이다
  - ⚠️ **자모로 갈린 `답:`(NFD)도 답 표시다**(`ANSWER_WORD`, 코덱스 31R). 맥에서 복사해 온 글은 `답` 이 `ᄃ`+`ᅡ`+`ᆸ` 으로 갈려 오는데, 합친 꼴만 보면 **프린트가 답이라고 밝혀 둔 답이 그대로 인쇄되고 경고도 안 뜬다**. 표시 모양은 `marks.ts` 한 벌이므로 줄머리 판정(`MARK_ONLY_LINE`)도 그 source 를 쓴다
  - ⚠️ **채워진 괄호 판정에 줄바꿈·길이 상한을 두지 말 것**(코덱스 31R·32R). 서술형 답은 길고 줄을 넘는다 — 상한을 두면 `(상대방의 처지를 이해하고 …)` 같은 **채워진 답을 못 알아본다.** 물음 자체가 이미 `PRINT_QA_QUESTION_MAX` 로 묶여 있다
  - ⚠️ **번호는 한 번만 뗀다**(`stripTrailingLabel`, 코덱스 31R). 같은 줄에서 번호를 뗀 뒤 줄 단위 떼기를 또 돌리면 `다음 숫자를 한글로 쓰시오.\n1\n1.` 에서 **주어진 값 `1` 까지 사라진다**
  - ⚠️ **번호 떼기는 맞추기(`matchesLabel`)와 같은 규칙으로 견준다**(코덱스 31R). 알맹이만 견주면 `문 1)` 이 번호 `1` 과 어긋나 원본 번호가 앞글에 남는다. 표 칸을 잇던 가운뎃점(` · `)도 문장이 끝난 자리로 보고, 끝에 남으면 함께 뗀다
  - ⚠️ **답을 몰라도 '채워진 괄호' 는 알린다**(`hasFilledParens`, 코덱스 30R). 답이 비어 있으면 빈칸으로 바꿀 것도 견줄 것도 없어 **아무 말도 못 하고 있었다** — 괄호는 프린트가 답을 밝히는 두 꼴 가운데 하나이므로 채워져 있으면 지우지는 않되 알린다. 번호(`(가)`)·배점(`(2점)`)·빈 답란은 뺀다(안 빼면 모든 문항에 경고가 붙는다)
  - ⚠️ **같은 줄의 번호를 뗄 때 한 낱말만 보지 않는다**(`stripSameLineLabel`, 코덱스 30R). `( 1 )`·`문 1)`·`3 - 1)` 처럼 번호에 공백이 끼면 마지막 낱말만 봐서는 못 떼고, **원본 번호가 앞글에 남아 우리가 찍는 번호와 나란히 인쇄된다**
  - ⚠️ **같은 글자로 여닫는 따옴표는 짝수면 닫는 것으로 본다**(`openTail`, 코덱스 29R). `답: "` 는 하나뿐이라 여는 것이고 `"답:"` 은 둘이라 닫는 것이다 — 가리지 않으면 인용된 자료가 잘린다
  - ⚠️⚠️ **답을 모르면 말끝로 가리지 않는다**(`tailUncertain`, 코덱스 29R). 국어 서술형 답은 `~세요.`·`~마라.` 처럼 **지시문과 말끝이 같아서**, `답: …마라.\n상대방의 말을 끝까지 들으세요.` 의 둘째 문장이 답인데 지시문으로 보여 **조용히 인쇄됐다.** 답을 아는 경우에는 그 줄을 이미 걷어냈으므로 그때만 말끝로 가린다
  - ⚠️ **문장 경계에서 빼는 것은 소수점뿐이다**(`DECIMAL_DOT`, 코덱스 28R). '뒤에 숫자가 오면 경계가 아니다' 로 하면 `소설.1번 보기와 비교하기` 의 과제가 사라진다 — 자릿수 **사이**의 점만 가린다
  - ⚠️ **꼬리 표시를 찾기 전에 걷어내는 따옴표에 닫는 따옴표를 넣지 말 것**(`TRAILING_OPEN`, 코덱스 28R). 넣으면 `대화 속 표기는 “답:”` 에서 닫는 따옴표를 걷어낸 뒤 그 안의 `답:` 이 꼬리 표시로 보여 **인용된 자료가 잘린다** — 닫는 따옴표로 끝난다는 것은 그 표시가 **인용 안에 있다**는 뜻이다
  - ⚠️ **문장 경계는 공백을 요구하지 않는다**(`MULTI_SENTENCE`, 코덱스 27R). 스캔 글에는 `소설.다음 글을 읽고…` 처럼 마침표 뒤 공백이 빠진 줄이 흔한데, 공백을 요구하면 **둘째 과제가 통째로 잘린다**(소수점만 빼고 본다)
  - ⚠️ **답의 나머지 줄은 '줄 끝까지 잘라 냈을 때만' 이다**(`openRows`, 코덱스 27R). `답: 소설 | 다음 보기` 처럼 뒤에 다른 칸이 남아 있으면 그 줄에서 답이 끝난 것이라, 다음 줄은 답이 아니라 **문제가 보여 주는 낱말**이다
  - ⚠️ **`stripTrailingMark` 는 표시를 찾았을 때만 글을 건드린다**(코덱스 27R). 표시를 찾기 전에 걷어내는 따옴표는 **찾았을 때만** 함께 걷는 것이다 — 아니면 앞글의 `그는 “봄이다”` 에서 닫는 따옴표가 사라진다. **화살표는 부르는 쪽이 허락해야 뗀다**: 앞글에서는 `얼음 →` 이 그림의 일부라 떼면 자료가 깎이고, 자르기에서는 이미 문맥을 확인한 뒤라 떼도 된다
  - ⚠️ **표시 판정은 한 벌이고, 정리하는 자리도 그것을 쓴다**(`endsWithMark` ↔ `tidyLines`, 코덱스 26R). 표시만 있는 줄을 무턱대고 떼면 `…\n얼음\n→` 의 **그림 화살표**나 인용 안의 표시가 사라진다. 표시만 있는 줄도 인용·화살표 규칙을 똑같이 거친다(제 줄이 표시뿐이면 **앞 줄**이 물음으로 끝나야 한다)
  - ⚠️ **끝 줄 떼기도 빈칸 줄을 지킨다**(코덱스 26R). 자르기만 지키면 `다음 빈칸을 채우시오. ____ 답:\n명사` 의 빈칸과 답 꼴 안내가 함께 사라진다
  - ⚠️ **같은 줄의 번호는 알맹이로 견준다**(`stripSameLineLabel`, 코덱스 26R). 구두점을 뗀 `(1` 을 번호 `(1)` 과 글자로 견주면 어긋나, 괄호 번호가 **앞글에 남아 우리가 찍는 번호와 나란히 인쇄된다**
  - ⚠️ **꼬리 표시의 인용 판정도 글 전체 기준이다**(코덱스 25R). 대화 자료는 여러 줄에 걸쳐 인용되는데 한 줄만 보면 그 인용을 놓쳐 **자료의 끝 줄과 그 아래 선택지가 사라진다**. 같은 까닭으로 **큰따옴표는 줄을 넘겨 짝짓고**(15R 의 '같은 글자로 여닫는 따옴표는 한 줄 안에서만' 을 아포스트로피에만 남긴다) — 낱말 속 아포스트로피만 한 줄로 묶으면 충분하다
  - ⚠️ **기호로 그린 그림도 앞글로 싣는다**(`PUNCT_ONLY`, 코덱스 25R). '글자가 한 자도 없으면 버린다' 로 하면 `△ → ○ → □` 같은 자료가 통째로 사라진다 — 버리는 것은 따옴표·괄호 한 짝처럼 **문장 부호뿐인** 조각이다
  - ⚠️ **표 칸 경계는 반각·전각 둘 다다**(`CELL_MARK`, 코덱스 25R). 자르기가 `|` 만 알면 `답: 모름｜다음 글: …` 에서 옆 칸의 지문이 함께 사라진다 — 번호 자리 판정은 이미 둘 다 본다
  - ⚠️ **자모로 갈린 한글(NFD)도 낱말 글자다**(코덱스 25R). 맥에서 복사해 온 글은 `대` 가 `ᄃ`+`ᅢ` 로 갈려 오는데, 그 자모를 빼면 `대답:` 의 '답:' 이 표시로 잡혀 **대화 자료가 잘린다**
  - ⚠️ **꼬리 표시에도 인용 보호를 건다**(`endsWithMark`, 코덱스 24R). `“정답:”` 을 답 표시로 보면 **그 줄과 아래 줄이 통째로 사라진다** — 인용된 표시는 물음이 그 표시 자체를 묻고 있는 것이다
  - ⚠️ **표시만 있는 줄이 화살표면 앞 줄을 본다**(코덱스 24R). `얼음\n→\n물` 은 그림이라, 그 화살표를 답 표시로 보면 **그림의 끝 값이 지워진다**. `갈래는?\n→\n소설` 처럼 물음이 끝난 뒤의 화살표만 답 표시다
  - ⚠️ **중복 판정은 앞뒤를 모두 본다**(`dropDuplicates`, 코덱스 24R). 앞에 나온 것만 보면, 자리를 못 얻은 응답이 **뒤에 앉은 진짜 문항보다 먼저 왔을 때** 살아남아 같은 물음이 두 번 인쇄된다
  - ⚠️ **'무엇이 바뀌었나' 로 표시를 판정하지 말 것**(`trailingMark`, 코덱스 23R). `stripTrailingMark` 는 여는 따옴표도 걷으므로, 바뀐 것만 보고 표시가 있다고 여기면 `"시"` 로 끝나는 **선택지 줄**이 답 표시로 둔갑해 그 아래 줄이 지워진다. 표시의 **자리**를 돌려주고 쓰는 쪽이 판단한다
  - ⚠️ **꼬리 화살표에도 '물음이 끝난 자리' 규칙을 건다**(`endsWithMark`, 코덱스 23R). `소설 →` 로 끝나는 자료 줄을 표시로 보면 그 아래 줄이 지워진다
  - ⚠️ **'줄 전체' 판정은 괄호까지 넓힌 구간으로 한다**(코덱스 23R). 안쪽 낱말만 보면 `(소설)` 이 혼자 놓인 **선택지 줄**이 줄 전체로 안 보여 그대로 빈칸이 된다
  - ⚠️ **모범답안이 붙은 뒤에 한 번 더 알린다**(`answersLeftInQuestions` → `answersWarnings`, 코덱스 23R). 나누기 때는 답이 비어 있어 `answerInQuestion` 이 0이었는데, 답이 생기고 나서야 **그 말이 물음 안에 보인다**는 사실이 드러난다 — 지우지는 않되(프린트가 답이라고 밝힌 자리가 아니다) 반드시 알린다
  - ⚠️ **번호 꼬리 구두점도 `label.ts` 한 벌이다**(`LABEL_TAIL`, 코덱스 23R). `ㄱ；` 을 번호로 맞추면서 `；` 를 못 떼면 앞글 끝에 **원본 번호가 남아 우리가 찍는 번호와 나란히 인쇄된다**
  - ⚠️⚠️⚠️ **우리가 지우는 것은 프린트가 '답이라고 밝혀 둔 것' 뿐이다**(코덱스 22R — 8R·9R·15R 의 '표시 없이 적은 답도 뗀다' 를 통째로 뒤집은, 스물두 라운드의 결론). 밝혀 둔 꼴은 둘뿐이다: **표시**(`답:`·`정답:`·`→`) 뒤의 글과, **괄호에 채워 넣은 말**. 그 밖의 같은 말은 남기고 `uncertain` 으로 알린다. 까닭: `다음 수의 절댓값을 구하시오.\n5` 와 `갈래는?\n소설`, `다음 중 고르시오. 은유, 직유` 와 `갈래는? 은유` 는 **구조가 똑같아** 글자로는 영영 못 가른다 — 지우면 풀 수 없는 문제가 되고, 남기면 선생님이 문제지를 한 번 보고 5초에 고친다
  - ⚠️ **빈칸으로 바꾸는 것도 괄호뿐이다**(`answerSpan`, 코덱스 22R). 괄호 없는 낱말을 빈칸으로 바꾸면 `다음 중 표현법을 고르시오. 은유, 직유` 의 **선택지가 사라진다**
  - ⚠️ **끝 줄 떼기는 앞 줄이 답 표시로 끝날 때만**(코덱스 22R). 지시문 뒤라는 이유로 떼면 주어진 값이 사라진다
  - ⚠️ **물음 밖의 '따로 적힌 답' 도 낱말로 서 있어야 한다**(`locateAllAnswers`, 코덱스 22R). 글자만 세면 지문의 `소설가` 가 따로 적힌 답으로 잡혀, 물음 속 `(소설)` 을 **주어진 값으로 오인해 그대로 인쇄한다**
  - ⚠️ **한 줄 안의 번호는 문장이 끝난 자리에서만 뗀다**(`SENTENCE_END`, 코덱스 22R). `주어진 수는 1.` 처럼 문장 안에 놓인 숫자를 떼면 지문의 값이 조용히 사라진다
  - ⚠️ **답을 찾을 때 표시 글자 뒤에서부터 본다**(`adjacentAnswer`, 코덱스 21R). `정답: 정답` 처럼 답이 표시와 같은 말이면 **표시 안의 글자가 먼저 잡혀 표시만 지우고 진짜 답을 남긴다**
  - ⚠️ **따옴표를 열고 적은 답도 '표시가 붙은 자리' 다**(`markedBefore`, 코덱스 21R). `답: “소설”` 을 못 알아보면 지문에 먼저 나온 같은 낱말을 답으로 삼아 **지문이 깎인다**. 앞글로 넘길 때도 여는 따옴표까지 함께 걷고(`stripTrailingMark`), 글자가 한 자도 없는 조각(닫는 따옴표 한 짝)은 싣지 않는다
  - ⚠️ **기호가 붙은 번호만 글 끝에서 뗀다**(`stripTrailingLabel`, 코덱스 21R). 기호 없이 글 끝에 놓인 숫자는 `지문 끝의 값은 1` 처럼 **문장의 값**이라, 떼면 지문이 조용히 깎인다. 줄 전체가 번호인 경우는 `stripLabelLine` 이 따로 본다
  - ⚠️⚠️ **`matchesLabel` 은 `LABEL_ONLY` 를 통과한 앞머리만 받는다**(코덱스 21R). 숫자 묶음만 견주면 `예시 2개:` 가 2번 자리로 인정되는데, **지켜 주는 쪽은 그 앞머리를 번호로 보지 않아** 짝이 어긋나고 진짜 2번의 손글씨 답을 잃는다 — 맞추기는 지키기보다 **더 엄해야** 한다
  - ⚠️ **답을 알면 표시 뒤 판정을 건너뛴다**(`tailIsAnswer` 의 `known`, 코덱스 20R). 그 자리까지만 자르므로 뒤에 무엇이 이어지든 다치지 않는다 — 예전에는 `답: 은유. 그 특징을 쓰시오.` 에서 지시문 때문에 자르기를 포기했고, 그 바람에 빈칸 만들기가 **선택지**를 지웠다
  - ⚠️ **자르는 끝은 '표시 바로 뒤의 답' 이다**(`adjacentAnswer`, 코덱스 20R — 18R 의 '마지막 자리' 를 뒤집음). `답: 소설. 다음 보기에서 소설, 시를 비교할 것.` 에서 뒤쪽 `소설` 까지 지우면 **비교 과제가 잘린다**. 표시와 답 사이에 공백·따옴표 말고 다른 글자가 있으면 그 자리는 답이 아니다
  - ⚠️ **원문에서 답을 고를 때 '답 표시가 붙은 자리' 가 먼저다**(`markedBefore`, 코덱스 20R). 지문에 같은 낱말이 먼저 나오면(`소설 한 편을 읽던 나는…\n답: 소설`) 그 자리를 답으로 삼아 **지문의 첫 낱말이 문항 안으로 먹히고**, 정작 적힌 답은 앞글에 남는다
  - ⚠️ **빈칸으로 바꿀 자리는 괄호 → 뒤쪽 순으로 고른다**(코덱스 20R). 답은 물음 뒤에 적히고, 앞쪽의 같은 말은 `은유, 직유 중 …?` 처럼 **고를 말**일 때가 많다
  - ⚠️ **중복 판정은 두 번째 기회 뒤에 한다**(`dropDuplicates`, 코덱스 20R). 먼저 가리면 번호 붙은 자리를 지키느라 둘 다 자리를 못 얻은 반복 물음에서 **뒤엣것이 중복으로 지워지고**, 그 물음과 답이 앞 문항의 앞글로 딸려 간다
  - ⚠️ **`~할 것.` 도 지시문이다**(코덱스 20R). 학습지는 `한 문장으로 서술할 것.` 처럼 적는 일이 흔한데, 빠뜨리면 답을 모르는 문항에서 **그 지시문이 통째로 잘린다**
  - ⚠️ **자리를 못 찾은 문항에는 두 번째 기회를 준다**(`seatLeftovers`, 코덱스 19R). 번호 붙은 자리를 지키느라(`carriesLabel`) 번호 없는 응답이 자리를 못 얻으면 앞글·답 구역·손글씨를 못 가릴 뿐 아니라 **물음 속 답을 가릴 근거(`answerInsideQuestion`)마저 사라져 답이 그대로 인쇄된다.** 번호를 가진 문항이 **전부 앉은 뒤**에 남은 자리를 주므로 남의 자리를 빼앗지 않는다
  - ⚠️ **표시 뒤가 답인지 보는 범위는 '그 표 칸 안' 이다**(코덱스 19R). 줄 끝까지 보면 `답: 한 단어로 쓰시오. | 정답: 소설` 에서 끝이 지시문이 아니라(`소설`) **지시문이 든 앞 칸까지 답으로 몰려 지워진다**
  - ⚠️ **답 뒤에 문장이 이어지면 답 자리까지만 자른다**(`answerEnd`·`MULTI_SENTENCE`, 코덱스 19R). `답: 소설. 그 특징을 서술할 것.` 의 `서술할 것.` 은 지시문 어미 목록에 없다 — 말끝을 아무리 모아도 국어의 지시문을 다 담을 수 없으므로, **답을 아는 경우에는 그 자리까지만** 자르고 모르면 지우지 않고 알린다
  - ⚠️ **표의 앞머리는 칸으로 끊어 본다**(`cleanPrefix`, 코덱스 19R). 한 행을 통째로 보면 `| 지문 | 1. 표현법은? |` 의 앞머리가 `지문 | 1.` 이 되어 번호를 **맞추기는 하면서 지켜 주지는 않는다**. 번호가 **제 칸을 따로 쓰는** 표(`| 1 | 표현법은? |`)도 흔해 물음 칸이 비면 바로 앞 칸을 본다
  - ⚠️ **자모도 낱말 글자다**(코덱스 19R). `(ㄱㄴ)` 처럼 자음으로 보기를 적는 국어 문항에서 자모를 빼면 답 `ㄱ` 이 한복판에서 잡혀 **보기가 빈칸이 된다**
  - **파일이 300줄을 넘어 셋으로 갈랐다**: 잘라 내기는 [redact-cut.ts](src/lib/print-qa/redact-cut.ts), 자리 배정은 [parse-place.ts](src/lib/print-qa/parse-place.ts), 번호 자리 판정은 [label.ts](src/lib/print-qa/label.ts) 로 모았다. 번호 모양과 번호 자리 판정이 한 파일에 있어야 `LABEL_ONLY` 와 `matchesLabel` 이 갈라지지 않는다
  - ⚠️ **낱말 안에서만 보이는 답은 '없는 답' 이다**(`locateAnswer`, 코덱스 18R — 17R 의 '못 찾으면 첫 자리로 물러선다' 를 뒤집음). 물러서면 그 자리가 다시 지문을 깎는다(`소설가` → `가 김 씨는…`). 못 찾으면 부르는 쪽이 답을 비우고 세므로(`answerNotInText`) **모델이 풀어 버린 답**이 '인쇄된 답' 으로 들어가지도 않는다 — 두 마리가 한 번에 잡힌다
  - ⚠️ **한 줄에 답이 둘일 수 있다**(`answerRanges`, 코덱스 18R). 표로 짠 줄(`답: 소설 | 다시 쓴 정답: 소설`)에서 첫 답만 걷으면 **뒤의 답이 문제지에 그대로 찍힌다**. 구간을 모두 모아 합친 뒤 한 번에 지운다
  - ⚠️ **줄 끝의 구두점까지가 선택지다**(코덱스 18R). `① 은유.` 의 마침표 때문에 '줄 전체' 판정이 어긋나 선택지가 빈칸이 됐다
  - ⚠️ **답의 나머지 줄은 줄끼리 먼저 견준다**(코덱스 18R). 글자 수로 가르면 `답: 가\n나` 처럼 **한 음절짜리 답의 둘째 줄**이 문제지에 남는다
  - ⚠️ **답의 나머지 줄은 '우리가 받아 둔 답' 으로 가려낸다**(`partOfAnswer`, 코덱스 17R). 말끝만 보면 `답: 서로 존중하고\n상대방의 말을 끝까지 들으세요.` 의 **뒷줄이 지시문으로 보여 그대로 인쇄된다** — 국어 서술형 답은 `~세요` 로 끝나는 일이 흔하다. 답과 글자가 같은 줄만 걷으므로 멀쩡한 지시문은 걸리지 않는다
  - ⚠️ **자를 때 표 칸 경계(`|`)에서 멈춘다**(코덱스 17R). 평문의 한 줄이 곧 표 한 행일 수 있어(`답: 소설 | 다음 글: 봄이 왔다.`) 줄 끝까지 자르면 **옆 칸의 지문이 함께 사라진다** — 그 자리는 이미 이 문항의 구역이라 앞글로도 안 남는다
  - ⚠️ **번호가 붙은 선택지도 '줄 하나' 다**(`coversWholeLine` 의 번호·목록 표시 벗기기, 코덱스 17R). `① 은유` 를 빈칸으로 바꾸면 고를 것이 사라지는데, 앞머리의 `①` 때문에 줄 전체로 안 보여 그대로 지워졌다
  - ⚠️ **빈칸이 있는 줄은 빈칸 만들기도 건드리지 않는다**(코덱스 17R). 자르는 쪽만 지키면 `답: ______ (답: 명사)` 의 **답 꼴 안내**를 빈칸 만들기가 지운다 — `BLANK_SLOT` 을 두 쪽이 함께 본다
  - ⚠️ **원문에서 답을 찾을 때도 낱말로 서 있어야 한다**(`locateAnswer`, 코덱스 17R). 글자만 찾으면 답 `소설` 이 지문의 `소설가` 안에서 잡히고, 그 자리를 문항의 끝으로 삼아 **지문에서 낱말 하나가 잘려 나간다**(`가 김 씨는…`). 낱말로 선 자리가 하나도 없으면 첫 자리를 쓴다 — 답을 통째로 잃는 것보다 낫다
  - ⚠️ **손글씨 판정은 '제 구역에서 찾은 자리' 를 가장 먼저 본다**(코덱스 17R — 11R 의 '물음 안쪽 먼저' 를 한 칸 뒤로 미룸). 물음 안쪽을 먼저 보면 `은유, 직유 중 …?` 의 **선택지**가 답 자리로 뽑혀, 바로 뒤에 손으로 쓴 답이 '인쇄된 답' 이 되고 모범답안 대상에서도 빠진다. 구역에서 못 찾았을 때에만 물음 안쪽 → 문서 전체 차례로 내려간다
  - ⚠️ **`1번`·`( 1 )`·`(1).` 도 번호다**(코덱스 16R·17R). `matchesLabel` 은 기호를 벗기고 접어 견주므로 늘 받아들이는데 `LABEL_ONLY` 만 빡빡하면 그 자리를 지켜 주지 않아 중복 응답이 집어 간다
  - ⚠️ **표시로 답을 걷어냈으면 더 손대지 않는다**(코덱스 16R). 프린트가 `답:` 으로 답을 밝혀 두었다면 그것이 답이고, 물음에 남은 같은 말은 **선택지나 물음의 글**이다 — `은유, 직유 중 표현법을 고르시오. 답: 은유` 에서 고를 것을 지우던 자리다. 다만 **남아 있다는 사실은 알린다**(지우지 않는 것과 안 알리는 것은 다르다)
  - ⚠️ **답 표시를 떼는 자리도 낱말 경계를 본다**(`stripTrailingMark`, 코덱스 16R). 알아보는 쪽(`isAnswerMark`)만 고치고 떼는 쪽을 두면, 대화 자료의 마지막 줄 `대답:` 에서 '답:' 만 떨어져 **`대` 한 글자가 남는다**. 목록 힌트(`looksLikeQaPrint`)도 같은 한 벌(`countAnswerMarks`)을 쓴다 — 따로 세면 대화 프린트를 문답 프린트로 권한다
  - ⚠️ **번호 모양은 기호가 겹치거나 공백이 끼어도 번호다**(`(1).`·`( 1 )`, 코덱스 15R·16R). `matchesLabel` 은 기호를 벗기고 접어서 견주므로 언제나 받아들이는데 `LABEL_ONLY` 만 빡빡하면, 견주기는 맞으면서 **그 자리를 지켜 주지 않아** 중복 응답이 집어 간다
  - ⚠️⚠️ **표시부터 자르고, 그래도 남았을 때만 빈칸으로 바꾼다**(코덱스 15R — 앞선 차례를 뒤집음). 빈칸을 먼저 넣으면 그 빈칸이 '아직 안 쓴 답' 으로 보여 **같은 줄의 진짜 답을 못 자른다**: `은유와 직유 중 이 시의 표현법은? 답: 은유` 가 `(　　　)와 직유 … 답: 은유` 가 되어 **선택지는 지워지고 답은 남았다**. 표시로 걷어낸 뒤에도 답이 남아 있을 때만 떼기·빈칸으로 간다
  - ⚠️ **답은 낱말로 서 있어야 답이다**(`standsAlone`, 코덱스 15R). `은유와` 의 앞토막 `은유` 를 답으로 보면 선택지가 사라진다. 가릴 자리를 고르는 것과 **가리고 난 뒤 남았는지 보는 것**이 같은 목록(`answerOccurrences`)을 쓴다 — 기준이 갈리면 못 가린 것을 못 알리거나 멀쩡한 물음마다 경고가 붙는다
  - ⚠️ **그 목록의 접는 세기는 `foldStrict` 하나다**(코덱스 15R — 13R 의 '느슨한 세기로 한 번 더' 를 뒤집음). 공백을 지우면 `아버지 가방에` 와 `아버지가 방에` 가 같아지는데 **띄어쓰기 문항에서는 그 둘이 답과 오답**이다. 띄어쓰기가 다르면 가리지 않고 알린다
  - ⚠️ **끝 줄 답 떼기는 '물음 바로 뒤' 일 때만**(코덱스 15R). 사이에 다른 줄이 있으면 그것은 답이 아니라 **선택지 목록의 마지막 칸**이다(`고르시오.\n아버지 가방에\n아버지가 방에`) — 떼면 정답 선택지가 사라진다. 앞 줄이 물음·지시문으로 끝나거나 **답 표시로 끝날 때**(`답:`)만 뗀다
  - ⚠️ **인용 구간은 글 전체에서 구한다**(코덱스 15R). 줄마다 새로 구하면 여러 줄에 걸친 대화 인용(`“질문: …\n답: …”`)의 아랫줄이 인용 밖으로 보여 **그 자료가 잘린다.** 다만 **같은 글자로 여닫는 따옴표(`'`·`"`)는 한 줄 안에서만** 짝으로 본다 — 줄을 넘겨 짝지으면 글 속의 아포스트로피 하나가 저 아래 따옴표와 묶여 그 사이의 **진짜 답 표시가 전부 '인용' 이 되고 답이 문제지에 남는다**
  - ⚠️ **마지막 문항 뒤에 남은 글도 챙긴다**(`withLeads` 의 꼬리 처리, 코덱스 15R). 지문을 물음 뒤에 싣는 프린트에서 마지막 문항의 지문이 바로 그 자리인데, 뒤에 문항이 없어 아무도 가져가지 않아 **경고도 없이 사라졌다**. 그 문항의 앞글에 이어 붙여 같은 승인 게이트에 얹는다
  - ⚠️ **모양을 정하는 곳은 하나다**([label.ts](src/lib/print-qa/label.ts)·[marks.ts](src/lib/print-qa/marks.ts), 코덱스 14R). 번호 꼴(`LABEL_ONLY`)과 견줄 때 벗기는 기호(`LABEL_MARKS`)가 **같은 글자 목록**에서 나오고, 답 표시(`답:`·`→`)도 자르는 쪽과 남았는지 보는 쪽이 한 파일을 본다. 세 라운드가 기호만 바꿔 가며 같은 결함을 짚었다 — 한쪽이 알아보는 꼴을 다른 쪽이 못 알아보면 **맞추기는 하면서 지켜 주지는 않아** 중복 응답이 그 자리를 집어 가고 진짜 문항이 사라진다(`ㄱ：`·`ㄱ；`)
  - ⚠️ **낱말 한복판의 '답' 은 답 표시가 아니다**(`isAnswerMark`, 코덱스 14R). `다음 대화에서 높임 표현을 찾으시오.\n질문: …\n대답: 학교에 갑니다.` 에서 `대답:` 이 표시로 잡혀 **대화 자료가 통째로 잘렸다**(경고도 없이). 표시는 낱말의 시작이어야 한다 — 그 규칙은 `hasAnswerMarkLeft` 도 함께 쓴다(한쪽만 느슨하면 못 자른 것을 못 알리거나 멀쩡한 대화에 경고가 붙는다)
  - ⚠️ **끝 줄 답 떼기는 `foldStrict`** 다(`dropAnswerOnlyLine`, 코덱스 14R). 느슨하게 접으면 공백이 사라져 `아버지 가방에` 와 `아버지가 방에` 가 같은 말이 되어, 띄어쓰기를 묻는 문항의 **선택지가 둘 다 지워졌다**
  - ⚠️ **줄 하나를 통째로 차지하는 답은 빈칸으로 바꾸지 않는다**(`coversWholeLine`, 코덱스 14R). 그 줄이 답인지 **선택지**인지 글자로는 못 가린다 — 끝 줄에 적힌 답은 `dropAnswerOnlyLine` 이 이미 뗐으므로 여기까지 온 것은 가운데 줄이고, 지우면 고를 것이 사라진다. 손대지 않고 `uncertain` 으로 알린다
  - ⚠️ **맨 숫자 한 줄은 문제에 주어진 값이다**(`stripLabelLine`, 코덱스 14R). `다음 수를 보고 답하시오.\n5\n절댓값은?` 의 `5` 를 번호로 보고 떼면 **풀 수 없는 문제**가 된다. 이 문항의 번호와 같거나(`1` ↔ `1`) 번호 기호를 달고 있을 때(`12)`)만 번호로 본다 — 값에는 그런 기호가 붙지 않는다
  - ⚠️ **물음과 답 *사이*의 글도 챙긴다**(`innerLead`, 코덱스 14R). 지문을 물음 뒤에 싣는 프린트가 흔한데(`1. 다음 글의 갈래는?\n봄이 왔다…\n답: 소설`), 문항의 끝이 답의 끝이라 그 글은 앞에도 뒤에도 속하지 않아 **편집 화면에도 교사용에도 안 남고 사라졌다**(경고도 없었다). 같은 승인 게이트에 얹어 그 문항의 앞글에 잇는다
  - ⚠️ **빈칸으로 바꿀 자리는 '글자 그대로 맞는 자리' 를 먼저 고른다**([redact-scan.ts](src/lib/print-qa/redact-scan.ts) 의 `answerSpan`, 코덱스 13R). 느슨하게 접어(`foldLoose`) 첫 자리를 집으면 공백이 사라져 `‘아버지 가방에’를 올바르게 띄어 쓰면? (아버지가 방에)` 에서 **인용된 물음거리가 빈칸이 되고 괄호 속 진짜 답은 그대로 남는다** — 가리려던 것과 정반대다. 인용 안쪽과 **화살표가 여럿인 줄(그림)** 은 아예 후보에서 뺀다: 줄 떼기(`dropAnswerOnlyLine`)에만 있던 그림 보호가 빈칸 만들기에는 없어서, 떼기가 거부한 그림을 빈칸 만들기가 지웠다. 마땅한 자리가 없으면 **지우지 않고 알린다**
  - ⚠️ **가리지 못한 것은 `RedactResult.uncertain` 한 값으로 돌려준다**(코덱스 13R). 세 갈래다: ① 답 표시가 아직 남았다 ② 표시 뒤를 걷어냈는데 **다음 줄이 지시문이 아니다**(두 줄로 적은 답의 뒷줄이 그대로 찍힌다) ③ 원문이 '물음 안쪽에 있다' 고 말한 답을 끝내 못 가렸다. 파서가 그 값으로 `dropped.answerInQuestion` 을 센다 — **뒷줄이 지시문이면 세지 않는다**(그러면 두 줄짜리 물음마다 경고가 붙어 경고 전체를 안 믿게 된다). 남아 있는지 보는 세기는 **`foldStrict`** 다(느슨하면 `‘아버지 가방에’` 가 답 `아버지가 방에` 와 같아져 멀쩡히 가린 문항마다 경고가 붙는다)
  - ⚠️ **배점 꼬리표(`(2점)`)는 지시문 판정에서 뗀다**(코덱스 13R). `갈래는? 답: 소설. 그 갈래의 특징 두 가지를 쓰시오. (2점)` 이 지시문으로 안 읽혀 **둘째 과제와 배점이 통째로 잘렸다**. 떼고 보면 지시문이라 자르지 않고 알린다
  - ⚠️ **문항의 끝은 줄 끝이 아니라 '답이 끝나는 자리' 다**(코덱스 13R — Stop 게이트의 '줄 끝까지 민다' 를 뒤집음). 표로 짠 프린트는 `| 1. 갈래는? | 답: 소설 | 다음 글: … |` 처럼 **한 줄이 한 행**이라, 줄 끝까지 밀면 그 행에 함께 실린 **지문이 어느 문항의 앞글도 되지 못하고 사라진다**(편집 화면에도 교사용에도 안 남아 그런 글이 있었다는 것조차 모른다)
  - ⚠️ **줄 한가운데서 시작한 앞글 조각도 버리지 않는다**([lead.ts](src/lib/print-qa/lead.ts) 의 `betweenLead`, 코덱스 13R — 2R 의 '그 줄의 남은 부분은 앞 문항의 것이다' 를 뒤집음). 같은 까닭이다: 표 한 행에는 답과 지문이 나란히 실린다. 답의 나머지인지 지문인지는 **사람이 승인 게이트에서 가린다**(`leadApproved`) — R11 이 앞글 규칙 전체에 내린 결론과 한 규칙이다
  - ⚠️ **번호를 견줄 때 벗기는 기호에 전각(`：`·`（）`)도 담는다**(`LABEL_MARKS`, 코덱스 13R). `LABEL_ONLY` 가 전각 콜론 번호(`ㄱ：`)를 번호로 **알아보면서** 그 글자를 못 벗기면, 제 번호가 붙은 자리조차 견주기에 실패해 **그 물음이 어디에도 못 앉고 중복으로 사라진다**. 두 정규식은 늘 같은 범위를 봐야 한다
  - ⚠️⚠️⚠️ **지문은 사람이 확인해야 학생 문제지에 실린다**(`leadApproved`, 코덱스 10R — 앞의 앞글 규칙들을 뒤집은 결론). 열 번의 리뷰에서 앞글 규칙은 **조이면 지문이 사라지고 풀면 답이 샜다**(10R 에서는 아홉 건 중 다섯 건이 '멀쩡한 문제를 깎는다' 였다). 글자로 가릴 수 없는 판단이므로 **기계가 고르지 않는다**: 뽑아 둔 글을 편집 화면에 보여 주고 선생님이 한 번 눌러야 실린다(기본 꺼짐). 교사용·답지에는 확인 여부와 무관하게 늘 보인다 — 판단할 거리가 거기 있어야 한다
  - ⚠️ **못 가린 것은 지우지 말고 알린다**(`hasAnswerMarkLeft` → `dropped.answerInQuestion`). 자를지 말지를 글자로 정하는 일은 한쪽을 조이면 다른 쪽이 터졌다 — 남은 답 표시는 세어 알리고, 선생님이 문제지를 한 번 보면 5초에 끝난다
  - ⚠️ **글이 달라지면 승인도 되돌린다**(Stop 게이트). 문항을 지우면 그 앞글을 뒤 문항이 물려받는데, 승인해 둔 문항에 **승인 안 된 글**을 합치면서 승인까지 물려주면 **아무도 안 본 글이 학생 문제지로 나간다**
  - ⚠️ **물음을 깎는 쪽은 언제나 보수적으로.** 빈칸 표시(`______`·`(   )`)가 줄 어디에든 있으면 안 쓴 답이고, 표시 줄에 글이 없으면 자르지 않는다(다음 줄이 답인지 지시문인지 못 가린다). 한 줄에 화살표가 여럿이면 **그림**이라 자르지 않는다. 괄호 빈칸 만들기도 **물음 안쪽 답일 때만** 한다
  - ⚠️ **번호 인식은 두 곳이 같은 범위를 봐야 한다**(코덱스 10R). `hasLabelBefore` 가 알아보는 꼴을 `LABEL_ONLY` 가 빠뜨리면(콜론 번호 `1:`·한글 자음 `ㄱ.`) 맞추기는 하면서 **지켜 주지는 않아** 중복 응답이 그 자리를 집어 간다
  - ⚠️ **번호를 뗄 때 경계를 본다**(`endsWithLabel`). 글자만 견주면 `마지막 숫자는 21` 에서 번호 `1` 을 떼어 **21 을 2 로 만든다**
  - ⚠️⚠️ **앞글(`lead`)은 첫 물음 앞에서만 가져온다**(코덱스 4R · Stop 게이트 — 다섯 라운드의 결론). 문항 **사이**의 글이 '지문' 인지 '아직 안 옮긴 답' 인지는 **원리적으로 가릴 수 없다.** 차례로 뚫린 시도들: ① `답:` 글자 규칙 → 표 칸에 든 답이 샜다 ② 줄 단위 제거 → 두 줄짜리 답의 둘째 줄이 샜다 ③ '답을 못 찾은 문항 뒤는 막기' → 표시 없이 적힌 답이 샜다 ④ '답을 제 구역에서 찾았을 때만 신뢰' → **답을 일부만 옮긴 경우**(`답: 첫째 이유` 만 옮기고 `둘째 비밀 이유` 를 남김)가 샜다 ⑤ 거기에 모델이 차례를 바꿔 내면 신뢰 상태가 엉뚱하게 복구돼 또 샜다. **첫 물음 앞은 아직 어떤 문항도 시작하지 않아 답이 있을 수 없는 유일한 자리**다 — 학교 프린트는 대개 지문 한 편을 앞에 싣고 문항을 줄줄이 붙이는 꼴이라 이 하나가 실제로 거의 전부다
  - ⚠️ 그 하나조차 **맨 앞 문항이 원문에서도 맨 앞일 때만** 싣는다(`withLeads` 의 `canLead`). 모델이 차례를 바꿔 냈거나 앞 문항을 못 찾았으면 그 앞에 **못 찾은 문항과 그 답**이 들어 있다
  - ⚠️ **못 실은 앞글은 반드시 센다**(`droppedLeads`) — 조용히 버리면 선생님은 그 자리에 지문이 원래 없었던 줄 안다. 다만 **다음 문항의 번호만 남은 틈은 세지 않는다** — 그것까지 세면 모든 문항에 경고가 붙어 **경고 전체를 안 믿게 된다**
  - ⚠️ **첫 물음 앞에서는 답 표시를 걷어내지 않는다.** 규칙을 들이대면 `→ 다음 시를 읽고 답하시오.` 로 시작하는 프린트에서 **시 전문이 통째로 사라진다**. 딱 하나, 맨 위에 **정답표를 찍어 둔 프린트**만 막는다(`cutPrintedAnswerKey`)
  - ⚠️ **맨 앞 글에도 '앞선 문항의 흔적' 검사를 붙인다**(`hasItemTrace`, Stop 게이트). 맨 앞 글을 싣는 근거는 '그 앞에 아직 문항이 없다' 인데, **모델이 1번 문항을 통째로 빠뜨리면** 우리가 아는 첫 문항 앞에 그 물음과 답이 그대로 들어 있다 — 모델이 돌려준 것끼리만 견주면 알 수 없다. 그래서 **원문 자체에서** 문항 번호 줄(`1. 물음`·`(2) 물음`·`문 3) 물음`)이나 채워진 답 줄을 찾고, 있으면 앞글을 싣지 않는다. 번호 줄 판정은 **닫는 기호(`.`·`)`·`]`)를 요구한다** — 그냥 '줄머리의 숫자' 로 보면 `1930년대에 …` 로 시작하는 지문 줄이 문항으로 몰린다
  - ⚠️ **번호 판정의 양 끝을 둘 다 조심한다**(Stop 게이트가 두 번 잡았다). 느슨하면 새고, 빡빡하면 지문이 사라진다. ① **번호만 있는 줄도 문항이다** — `1.\n첫 물음?` 처럼 번호와 물음이 줄을 나눠 인쇄된 프린트가 있다. ② **한글 순서 표시는 번호로 실제로 쓰는 글자만**(`가나다…하`) — `[가-힣]` 전부를 받으면 `산. 그리고 물.` 같은 시 한 줄이 문항으로 몰린다. ③ **그 글자에 대괄호를 허용하지 않는다** — `[가]`·`[나]` 는 국어 시험지의 **지문 표시**라, 받아 주면 지문이 여럿인 프린트에서 앞글이 통째로 막힌다(`(가)` 는 문항이다)
  - ⚠️ **번호를 뗄 때 줄바꿈까지 걷어내고 본다**(Stop 게이트). 번호가 물음과 줄을 나눠 인쇄된 프린트(`지문…\n1.\n첫 물음?`)에서는 잘라 온 앞글이 줄바꿈으로 끝나, 공백만 걷어내면 **그 문항 자신의 번호**를 못 떼고 위 검사가 그것을 '앞선 문항의 흔적' 으로 읽는다 — **멀쩡한 지문이 통째로 사라진다**
  - ⚠️ **목록으로 인쇄된 문항도 잡는다**(코덱스 5R). `<li>` 는 평문에서 `- ` 가 되는데, 줄머리에 못 박은 검사는 그 표시에 막혀 `- 1. 첫 물음?`·`- 답: 비밀정답` 을 통째로 놓쳤다 — 검사 전에 목록 표시를 벗긴다. 닫는 `]` 도 번호로 받는다(`가]`·`A]` 꼴이 있다; 여는 `[` 만 막으면 `[가]` 지문 표시와 갈린다)
  - ⚠️ **물음 속에 섞여 온 답을 가린다**([redact.ts](src/lib/print-qa/redact.ts), 코덱스 5R). 모델이 `1. 서술자는 누구인가? 답: 나(소년)` 을 통째로 `question` 으로 내면, `answer` 를 따로 받아 두어도 **문제지가 물음과 함께 답을 찍는다**(`showAnswers` 는 답 줄만 가린다). 가리는 범위는 **아주 좁게** — `답:` 표시 뒤를 자르는 것과, 괄호가 답과 **정확히 같은 말**을 감쌌을 때 빈칸으로 되돌리는 것 둘뿐이다. 물음 속 낱말을 함부로 지우면 `밑줄 친 '은유' 의 뜻은?` 에서 물을 것이 사라진다
  - ⚠️ **답 구역의 끝은 원문 차례로 잡는다**(코덱스 5R). 모델이 낸 차례로 '다음 문항' 을 찾으면 차례가 엉킨 순간 그것이 **앞쪽**을 가리켜 구역이 비고, 문서 전체를 뒤져 **엉뚱한 문항의 답**을 제 답으로 삼는다(손글씨 판정까지 함께 틀어진다)
  - ⚠️ **번호가 앞 줄에 있을 수 있다**(코덱스 5R). `1.\n표현법은?` 처럼 줄을 나눠 인쇄한 프린트에서 제 줄만 보면 번호를 못 찾아, 모델이 한 문항을 두 번 냈을 때 둘째가 뒤 문항의 자리를 차지한다
  - ⚠️ **앞글을 잃은 모든 길을 센다**(코덱스 5R). ① 흔적이 있어 막은 경우 ② 맨 앞 문항이 원문에서 맨 앞이 아니거나 못 찾아 **아예 시도하지 못한** 경우 ③ 너무 길어 **잘라 낸** 경우(`truncatedLeads`). 셋 다 조용하면 선생님은 지문이 원래 없었던 줄 안다. 못 실은 앞글 안내는 **원본 쪽 그림**을 가리킨다 — 교사용에도 그 지문은 없다
  - **앞글 길이 상한은 20,000자**다. 이 글은 모델에게 **안 보내고** 저장·인쇄만 하므로 프롬프트 예산과 무관한데, 2,000자로 잡았더니 2,100자 지문의 앞부분이 조용히 잘렸다
  - ⚠️ **답 가리기는 `answer` 와 무관하게 돈다**(코덱스 6R). 표시(`답:`)만 보고 자르는 일을 `answer` 가 있을 때만 하면, 모델이 답을 못 뽑아낸 문항에서 **물음 속의 답이 그대로 인쇄된다**. 화살표(`→`)도 프롬프트가 인정하는 답 꼴이라 함께 본다 — 다만 화살표는 글 속에서도 쓰여(`A → B 의 변화를 설명하시오.`) **물음이 끝난 자리**에 온 것만 답으로 본다. 여는 따옴표 뒤의 표시는 인용이다(`'정답: 소설' 에서 콜론의 기능은?` 을 통째로 지우던 자리)
  - ⚠️ **인용은 구간으로 본다**(`quotedRanges`, 코덱스 7R · Stop 게이트). 표시 **바로 앞 글자**만 보면 `「답: 가, 정답: 나」에서 쉼표의 기능은?` 의 둘째 표시가 인용 밖으로 보여 멀쩡한 물음이 잘리고, 반대로 첫 표시가 인용이라고 거기서 멈추면 `「답: 가」의 뜻은? 답: 나` 의 **진짜 답이 남는다**. 짝을 이룬 구간을 모두 구해 놓고, 인용 밖의 첫 표시에서 자른다
  - ⚠️ **글자로 그린 화살표(`->`·`=>`)도 답 표시다**(코덱스 7R). 유니코드 화살표만 보면 `갈래는? -> 소설` 이 그대로 인쇄된다
  - ⚠️ **화살표 답 줄도 문항 흔적이다**(코덱스 7R) — 번호 없는 물음(`갈래는?\n→ 소설`)은 번호로는 못 잡는다. 다만 화살표는 지시문에도 쓰여(`→ 다음 시를 읽고 답하시오.`) **짧고 시키는 말로 끝나지 않는 줄**만 답으로 본다
  - ⚠️ **한 줄에 붙은 '물음 + 답' 도 흔적이다**(코덱스 8R). `갈래는? → 소설` 처럼 번호 없는 물음이 한 줄로 인쇄되면 줄머리 규칙으로는 못 잡아 그 답이 통째로 앞글에 실린다 — 물음이 끝난 자리(`?`·`~시오`) 뒤에 답 표시가 붙은 꼴을 따로 본다
  - ⚠️ **줄머리 화살표라고 무조건 답은 아니다**(코덱스 8R). `다음 변화 과정을 보고\n→ 얼음 → 물 → 수증기\n각 단계에서 …쓰시오.` 는 **그림**이다 — 자르면 지시문과 그림이 함께 사라진다. 물음이 끝난 자리에 온 것만 자른다(앞글 판정은 더 조심스러워 별도 규칙을 쓴다 — 그쪽은 잃는 쪽이 안전하다)
  - ⚠️ **표시 없이 줄만 바꿔 적은 답도 뗀다**(코덱스 8R). `갈래는?\n소설` 은 표시가 없어 글자만으로는 못 가리지만, **우리가 뽑아 둔 답과 글자가 똑같은 끝 줄**이면 그것은 답이다. 똑같을 때만 뗀다 — 비슷하다고 떼면 물음 속 낱말이 사라진다
  - ⚠️ **물음표로 끝나는 줄도 문항 흔적이다**(코덱스 9R). 번호도 답 표시도 없는 물음(`갈래는?\n소설`)은 그것 말고는 알아볼 길이 없다. 지시문은 물음표로 끝나지 않는다(`…답하시오.`). **대가**: 설의법 한 줄(`어디로 갔을까?`)이 있는 시는 앞글을 잃는다 — 경고로 알린다
  - ⚠️ **화살표가 여럿인 줄은 그림이다**(코덱스 9R). `→ 얼음 → 물 → 수증기` 를 답으로 보면 지문이 통째로 사라진다. 답은 화살표 하나 뒤에 한 번 적힌다
  - ⚠️ **빈칸인지는 그 줄 끝까지만 본다**(코덱스 9R). 글 전체 끝을 보면 `다음 빈칸을 채우시오.\n답: ______\n그렇게 생각한 이유도 쓰시오.` 에서 뒷줄 때문에 '채워진 답' 으로 오인해 **빈칸과 뒤 지시문을 통째로 지운다**
  - ⚠️ **답이 여러 줄일 수 있다**(코덱스 9R). `두 가지를 쓰시오.\n은유\n직유` 는 줄 하나씩 견주면 어느 줄도 답 전체와 같지 않아 둘 다 살아남는다 — 끝에서 통째로도 본다
  - ⚠️ **빈칸과 '다음 줄에 적은 답' 을 가른다**(Stop 게이트). 한 줄만 보면 `답:` 다음 **줄**에 답을 적는 프린트를 놓치고, 글 전체를 보면 `답: ______` 뒤의 **지시문**까지 답으로 본다. 표시 줄에 글자가 있으면 채워진 것, 비어 있으면 **빈칸 표시가 없고 다음 줄이 시키는 말이 아닐 때**만 채워진 것으로 본다(`filledAt`)
  - ⚠️ **빈칸은 밑줄만이 아니다** — `답: (     )` 처럼 **괄호**로 그린 답란이 흔하다(Stop 게이트). 괄호를 빈칸으로 안 보면 그 문항이 통째로 잘린다. 지시문 말끝도 `~시오` 만이 아니다(`~하라`·`~하세요`) — 빠뜨리면 그 지시문이 답으로 몰려 **빈 답란과 함께 지워진다**
  - ⚠️ **앞글 판정에서는 화살표 개수로 그림을 가리지 않는다**(Stop 게이트 — 코덱스 9R 의 '그림' 규칙을 뒤집음). `물의 상태 변화 순서를 쓰시오.\n→ 얼음 → 물 → 수증기` 에서 그 줄은 **그 물음의 답**이다. 그림인지 답인지는 글자로 가릴 수 없어 **잃는 쪽**을 고르고 경고로 알린다. ⚠️ 물음 **안**의 화살표는 다르게 다룬다 — `redact.ts` 는 물음이 끝난 자리에 온 것만 자른다(그림을 자르면 문제가 못 쓰게 된다). **두 자리의 규칙이 일부러 다르다**
  - ⚠️⚠️ **물음 속의 말이 '주어진 값' 인지 '답' 인지는 가릴 수 없다**(코덱스 9R · Stop 게이트). `다음 수의 절댓값을 구하시오. / 5 / 답: 5` 와 `갈래는? / 소설 / 답: 소설` 은 **구조가 똑같다** — 지우면 풀 수 없는 문제가 되고 놔두면 답이 보일 수 있다. 제 구역에 따로 적힌 답이 있으면 **놔두고 세어 알린다**(`answerInQuestion`). 없으면 물음에 끌려 들어온 답이므로 뗀다
  - ⚠️ **글자가 같다는 것만으로 답 줄이라고 떼지 말 것**(Stop 게이트). `다음 수의 절댓값을 구하시오.\n5` 에서 `5` 는 **문제에 주어진 값**이지 답이 아닌데(답도 5다), 떼면 풀 수 없는 문제가 된다. 그래서 **원문에서 답을 찾은 자리가 물음 안쪽일 때만** 뗀다 — 안쪽이라는 것은 모델이 답을 물음에 끌어넣었다는 뜻이다. 자리를 모르면 떼지 않는다
  - ⚠️ **번호는 양쪽을 같은 규칙으로 벗겨 견준다**(`labelCore`, 코덱스 8R). 앞머리에서만 기호를 떼면 `(가)` 번호가 `(가` 와 견줘져 **어느 자리에도 못 앉고** 그 물음이 중복으로 사라진다
  - ⚠️ **화살표 답을 길이로 가르지 말 것**(Stop 게이트). 짧은 줄만 답으로 보면 **긴 서술형 답**이 지문으로 둔갑해 그대로 인쇄된다. 답은 길 수 있지만 **지시문의 말투는 정해져 있다**(`~시오`·`~하라`·`~세요`) — 말투로 가른다. 물음표는 지시문 표시에 넣지 않는다: `→ 다음 중 알맞은 것은?` 은 **물음**이라 그 자체가 문항 흔적이다
  - ⚠️ **번호 판정 앞에서 목록 표시를 벗긴다**(코덱스 7R). `<li>` 가 만든 `- 2.` 를 번호로 못 알아보면 그 자리를 '번호 없는 자리' 로 여겨 중복 응답이 집어 가고 **진짜 2번이 사라진다**
  - ⚠️ **답 구역 상한에 답 길이를 더한다**(코덱스 7R). 상한은 '답이 **시작**하기까지' 의 거리라, 안 더하면 600자짜리 긴 답이 바로 옆에 있어도 구역 밖으로 밀려나 **같은 글자의 다른 자리**를 집어 온다
  - ⚠️ **답 표시는 첫 개만 보고 그만두지 않는다**(Stop 게이트). 앞에 **인용된** 표시가 있으면 (`「답: 가」의 뜻은? 답: 나`) 거기서 멈춰 **뒤의 진짜 답이 문제지에 남는다** — 자를 수 있는 표시를 만날 때까지 계속 훑는다
  - ⚠️ **동그라미 번호(①~⑳)도 문항 번호다**(코덱스 6R). 국어 프린트가 즐겨 쓰는데 못 알아보면 그 문항을 모델이 빠뜨렸을 때 물음과 답이 통째로 앞글로 실린다
  - ⚠️ **앞 줄을 번호로 인정하려면 그 줄이 번호뿐이어야 한다**(`LABEL_ONLY`, 코덱스 6R). 숫자 묶음만 견주면 `예시 1개` 같은 멀쩡한 글줄이 1번 자리를 차지해 **중복 응답이 진짜 문항을 밀어낸다**
  - ⚠️ **번호가 없는 문항을 차례로 앉힐 때 남의 번호가 붙은 자리는 건드리지 않는다**(`carriesLabel`, 코덱스 6R). 건드리면 그 번호의 진짜 문항이 자리를 잃고 중복으로 사라진다
  - ⚠️ **답 구역에는 거리 상한도 둔다**(`PRINT_QA_ANSWER_REGION_MAX`, 코덱스 6R). '다음 물음까지' 만으로는 모자라다 — 가운데 문항을 모델이 못 옮기면 그 자리를 우리가 몰라 구역이 그 너머까지 벌어지고, 앞 문항이 **뒤 문항의 답**을 제 답으로 삼는다(손글씨 판정까지 틀어진다). 넘어서 찾은 답은 버리지 않되 '다른 곳에서 찾았다' 로 알린다
  - ⚠️ **참고자료로도 손글씨가 되살아나지 않게 막는다**(코덱스 4R). 같은 프린트가 참고자료로 붙으면 그 본문은 평문이라 손글씨 표시가 없다 — 이 프린트에서 **손글씨 자리에만** 있는 말은 어느 자료에서 찾았든 근거로 치지 않는다
  - ⚠️ **모델이 남긴 경고는 맨 뒤에 붙인다**(코덱스 4R). 호출부가 상한(20)으로 자르는데 앞에 두면, 모델이 경고를 스무 개 내는 순간 '지문을 못 실었다'·'답 짝이 어긋났다' 가 **통째로 사라진다**
  - ⚠️ **문항을 지우면 그 앞글을 뒤 문항이 물려받는다**(`removeQaItem`, 코덱스 2R). 한 지문에 딸린 문항이 여럿이면 그 지문은 **첫 문항의 `lead` 에만** 있다 — 그냥 걸러 내면 남은 문항들이 지문 없는 물음이 된다
  - ⚠️ **킬스위치 확인에도 취소 신호를 보낸다**(코덱스 2R). 그 왕복이 멈추면 `finally` 까지 못 가서 편집도 저장도 잠긴 채로 남는다. 같은 까닭으로 **AI 가 꺼져 있어도 고친 것이 있으면 안내 화면으로 가리지 않는다** — 마지막 문항을 지운 순간 저장할 길이 사라진다
  - `src/lib/print-qa/` 는 **`print-scan` 을 값으로 import 하지 않는다**(타입만) — 그 배럴이 `run.ts` 를 다시 내보내 순환이 된다. 저장은 훅이 `print-scan/save` 의 `updateBundle` 로 한다(`print-words` 와 같은 규약)

- [2026-09-14] **학교 프린트 시험지 — 스캔을 프린트별로 묶어 읽고 개념지로 만든다**([sql/26](sql/26_print_scans.sql), [src/lib/print-scan/](src/lib/print-scan/), [src/lib/concept-pick/](src/lib/concept-pick/)). 되돌리지 말아야 할 판단들:
  - **시험지를 위한 표를 만들지 않았다.** 프린트 시험지는 `concept_sheets` 행이고 `print_bundle_id` 만 더했다 — 편집기·`exam-transform`·인쇄 엔진·합격 기준·ara-system 동기화가 **전부 공짜로** 따라온다. 새 표를 팠다면 그 다섯 가지를 전부 두 벌로 만들어야 했고, 그중 하나만 고쳐지는 날이 반드시 온다
  - **`print_bundle_id` 는 ON DELETE CASCADE 다**(SET NULL 아님). 개념지 목록은 `print_bundle_id IS NULL` 로 프린트를 숨기는데, SET NULL 이면 묶음을 지운 순간 그 시험지들이 개념지 목록에 **갑자기 나타난다** — 숨기기로 한 결정이 삭제로 깨진다. 앱에서 시험지를 먼저 지우는 방법은 두 번의 DELETE 라 원자적이지 않다. 대신 확인창이 **함께 사라지는 시험지 수를 반드시 밝힌다**([scan-delete.ts](src/lib/print-scan/scan-delete.ts))
  - ⚠️ **`ConceptSheet` 에 컬럼을 더하면 `LIST_COLUMNS` 도 같이 고친다**([useConceptList.ts](src/hooks/useConceptList.ts)). `ConceptSheetListItem` 이 `Omit<ConceptSheet, …>` 라 타입엔 있는데 조회엔 없는 조용한 undefined 가 생긴다(이번에 `tsc` 가 잡아 줬다). 그리고 그 상수는 **한 줄이어야 한다** — `+` 로 이으면 리터럴 타입이 `string` 으로 넓어져 PostgREST 의 행 타입 추론이 통째로 풀린다
  - **묶음은 3쪽·겹침 0 이다**([print-scan/constants.ts](src/lib/print-scan/constants.ts)). 기출이 1쪽을 겹치는 이유는 쪽 경계를 넘는 **지문**을 `merge.ts` 가 합치기 때문인데, 프린트 읽기의 결과는 구조 없는 평문 HTML 이라 합칠 수가 없다 — 겹치면 같은 글이 시험지에 **두 번** 들어간다. 대신 프롬프트가 "이 쪽의 마지막 문단은 이 쪽에서 끝나는 곳까지만" 이라고 못박는다
  - ⚠️ **`BODY_FORMAT_RULES` 를 개념지 HTML 에 재사용하지 말 것.** 그 규약은 `<figure data-figure>`·`<blockquote data-box>` 를 시키는데 `sanitizeConceptHTML` 은 그 둘을 **지운다** — 모델은 시킨 대로 잘 냈는데 화면에서만 사라지는, 원인을 찾을 수 없는 결함이 된다. [print-scan/prompt.ts](src/lib/print-scan/prompt.ts) 가 개념지 편집기가 실제로 받는 태그만 따로 적는다([parse.test.ts](src/lib/print-scan/parse.test.ts) 가 그 금지 목록을 고정한다)
  - **이미지 설명은 [describe-images.ts](src/lib/problem-ocr/describe-images.ts) 한 벌**이고 기출 전용 문구(`continued`·`box.column`)는 **호출자가 `splitRules` 로 넘긴다.** 섞어 두면 프린트 프롬프트가 **자기 스키마에 없는 필드**를 설명하게 된다
  - **취소·한도로 멈춘 묶음은 '실패' 가 아니라 '대기' 로 둔다**([run-scan.ts](src/lib/print-scan/run-scan.ts)). 시작도 안 했으니 실패라고 적으면 거짓말이고, 목록의 '읽기' 버튼이 곧 복구 경로다. 반대로 **'읽는중' 으로는 절대 남기지 않는다** — 영영 돌고 있는 것처럼 보인다(기출 `run.ts` 와 같은 규약)
  - ⚠️ **그 규약은 정리가 돌 수 있을 때만 지켜진다 — 탭이 닫히면 '읽는중' 이 그대로 남는다**([reading-state.ts](src/lib/print-scan/reading-state.ts), 코덱스 리뷰 P1). 그때 목록의 '읽기' 를 계속 잠가 두면 **지우고 다시 올리는 것 말고는 되살릴 길이 없다.** `isStalledReading` 이 `updated_at` 으로 30분(`PRINT_READ_STALE_MS`)을 넘긴 줄을 멈춘 것으로 보고 버튼을 열되, 다른 탭이 진짜로 읽는 중일 수 있으므로 **확인창을 한 번 거친다**. 시각을 못 읽으면 멈춘 쪽으로 판정한다 — 영영 잠긴 줄로 두는 게 더 나쁘다. 기기 시계가 앞서 있을 때(음수)는 '방금 시작' 으로 본다
  - ⚠️ **경고를 `ocr_meta` 에만 넣고 완료라고 말하지 않는다**(코덱스 리뷰 P1). 배치 일부가 실패하거나 쪽이 빠지거나 본문이 잘려도 살아남은 내용으로 시험지가 만들어지는데, 예전엔 진행률 칸이 `warnings={[]}` 를 받고 목록·편집기 어디에도 경고가 없어 **선생님이 완성본으로 알고 그대로 인쇄**했다. 이제 `runBundle` 이 `onWarnings` 로 올리고 → 훅이 모아 진행률 칸에 띄우고 → 목록 줄에 `확인 필요 N` 칩이 붙고 → 편집기 왼쪽(원본 옆)에 `OcrProgress` 가 그대로 보여 준다. 토스트도 '완료' 와 **'확인 필요'** 를 가른다. 상태값(`읽기완료`)에 새 값을 더하지 않은 까닭은 CHECK 제약·배지·분기가 전부 따라 바뀌어야 하는데, 경고 유무는 `ocr_meta` 에서 **파생되는 값**이기 때문이다
  - ⚠️ **업로드 실패를 '이미 있음' 으로 치지 않는다**([storage-errors.ts](src/lib/problem-bank/storage-errors.ts), 코덱스 리뷰 P2). `upsert:false` 라 중복이면 실패로 오는데, 모든 오류를 중복으로 뭉뚱그리면 네트워크·권한·용량 실패까지 **없는 경로가 올라간 것처럼** 저장된다 — 원본 칸은 영영 비고, `rerunBundle` 은 '경로가 비어 있는 자리만' 다시 올리므로 그 쪽을 고치지도 않는다. `isDuplicateUploadError`(409 / `Duplicate` / `already exists`)만 성공으로 치고 나머지는 **결과에서 뺀다**
  - **'다시 읽기' 는 올려 둔 원본 PDF 로 한다.** 그래서 `print_scans.file_path` 를 반드시 남긴다 — 파일이 없으면 다시 읽을 방법이 아예 없다. 업로드 때와 **같은 `runBundle`** 을 쓴다(상태 전이·시험지 만들기가 두 벌이 되면 조용히 갈라진다)
  - **행을 AI 호출보다 먼저 만든다.** 읽다가 실패하거나 탭이 닫혀도 목록에 남아 이어갈 수 있다 — 안 그러면 올린 PDF 가 흔적 없이 사라진다. 킬스위치 재확인도 **행을 만들기 전**이다
  - **버킷은 `exam-problem-bank` 를 같이 쓴다**(경로만 `print-scans/` 로 가른다). 정책이 버킷 단위라 새 버킷은 정책 세 벌을 더 관리하게 만들고 허용 MIME 도 똑같다. 업로드는 여기서도 `upsert:false` 다
  - **AI 기능 플래그는 다섯 곳이 한 벌이다** — `AiFeature` 타입 / `isFeatureEnabled` / `/api/ai/status` 의 OFF·응답 / `useAiEnabled` 의 타입과 OFF / `ocrStillEnabled(feature)` 호출부. 하나라도 빠지면 그 키가 `undefined` 가 되어 '꺼짐' 과 '아직 모름' 이 뒤섞인다
  - **AI 추천 빈칸은 고른 즉시 마킹한다**([useConceptPick.ts](src/hooks/useConceptPick.ts)). 목록만 주고 하나씩 누르게 하면 직접 드래그하는 것과 품이 같아 도움이 안 된다 — 대신 **되돌리기를 쉽게** 뒀다(붙인 것만 기억해 개별·전체 해제). ⚠️ 추천은 **띄어쓰기 없는 한 어절**만 통과시킨다: `extractMarks` 가 마킹 구간을 공백으로 쪼개 세므로 구절을 고르면 빈칸이 여러 개가 되고 **마킹 수 = 문항 수 = 합격 기준의 분모**가 부풀어 학원 성적까지 어긋난다
  - ⚠️ **`addMarkByText` 는 boolean 을 돌려준다**(한 텍스트 노드 안에서만 찾는 한계는 그대로다). 못 붙인 것을 조용히 넘기면 "10개 골랐다" 고 해 놓고 7개만 붙는다 — 개수를 세어 사람에게 알린다
  - **(2026-09-15) 추천 개수는 사람이 정하지 않는다**([prompt.ts](src/lib/concept-pick/prompt.ts)). '10개' 를 적게 하는 것은 **본문을 읽기 전에 답을 정하는 일**이라 짧은 본문은 억지로 채우고 긴 본문은 빠뜨린다 — 숫자 칸·`count` state·`ConceptPick*Input.count` 를 전부 없앴다. **(아래 '기준은 손 마킹 실측값' 항목이 눈대중 부분만 뒤집었다 — `CONCEPT_PICK_TYPICAL_COUNT` 는 없어지고 밀도가 그 자리를 대신한다. 개수를 사람이 안 정한다는 결정 자체는 그대로다.)** ⚠️ **상한은 `CONCEPT_PICK_MAX_COUNT` 하나**가 스키마 `maxItems`·프롬프트 문구·파서 자름을 다 먹인다: 셋이 갈라지면 모델이 스키마를 어겨 **출력이 통째로 실패**한다(파서가 스키마를 믿지 않고 한 번 더 자르는 것은 의도된 중복이다). ⚠️ 다시 눌렀을 때 **빈 배열은 정상 응답**이지 오류가 아니다([notice.ts](src/lib/concept-pick/notice.ts)) — `dropped` 가 전부 0 일 때만 '더 추천할 용어가 없어요'(info)라 하고, 골랐는데 전부 걸러진 것은 여전히 경고다. 셋째 갈래가 따로 있다(코덱스 리뷰) — **골라 놓고 `addMarkByText` 가 전부 false** 인 경우는 '찾지 못했다' 가 아니라 '붙이지 못했다' 다. 서식으로 쪼개진 낱말일 뿐이고 사이드바가 그 이름을 이미 보여 주는데, 못 찾았다고 하면 AI 가 아무것도 안 한 것처럼 읽힌다
  - **(2026-09-15) 추천 빈칸은 표·해설에서 고르고 작품 원문은 건드리지 않는다.** 제보는 "추천이 빈약하다 — 시어 풀이표에서 하나도 안 고른다" 였고 원인이 셋이었다:
    - ⚠️ **[plain-text.ts](src/lib/concept-pick/plain-text.ts) 는 표의 칸을 *가장 먼저* 처리한다.** TipTap 은 칸을 언제나 `<td><p>…</p></td>` 로 내보내므로, `</p>` 를 먼저 줄바꿈으로 바꾸면 칸 구분 규칙이 **닿기도 전에 죽는다** — 옛 `</td>`→탭 규칙이 정확히 그래서 **배포된 채로 죽은 코드**였고, 모델에게 시어 풀이표는 '나무 / 꿈을 지닌 대상' 같은 무관한 조각으로 보였다(행의 짝이 깨진다). 이제 표는 `| 칸 | 칸 |`, 제목은 `#`, 목록은 `-` 로 남긴다. ⚠️ 기호는 **앞뒤를 공백으로 띄운다** — 붙이면 그 낱말이 `plain.includes` 에서 영영 '본문에 없음' 이 된다
    - **프롬프트가 '어디서 고르는가' 를 말한다.** 종류(개념어·이름·용어)만 알려 주면 모델이 작품 원문에서 낱말을 줍는다. 잘 되는 형제 기능([print-words/prompt.ts](src/lib/print-words/prompt.ts))이 '어휘 풀이·표의 두 칸·각주' 로 자리를 짚어 주는 것과 같은 수법이다
    - ⚠️ **마킹 자리는 "처음 나오는 곳" 이 아니다**([concept-mark-target.ts](src/lib/concept-mark-target.ts)). 자리 힌트(`context`) → **표 칸** → 첫 자리 순으로 고른다. 같은 시어가 시 본문과 풀이표에 다 있는 것이 개념지의 보통 모양이라, 첫 자리 규칙은 **언제나 시 본문에 구멍을 냈다**(사용자가 명시적으로 싫어한 동작). 되돌리지 말 것
    - `context` 가 쓸 수 없으면 **비우기만 하고 추천은 살린다** — 힌트가 없으면 표 우선 규칙으로 물러서면 된다. 버리면 표에서 고른 추천이 통째로 사라진다
    - 검증에 걸려 버린 수(`dropped`)를 사이드바에 밝힌다. 표의 풀이는 구절이 많아 `malformed` 로 걸리는데, 안 보여 주면 "왜 이렇게 적게 나오지" 만 남는다
    - ⚠️ **접는 세기가 둘이고, 섞으면 반드시 하나가 깨진다**([fold.ts](src/lib/concept-pick/fold.ts), 코덱스 리뷰 1~2R). 견주는 두 글자가 서로 **다른 표현**에서 온다: 평문에는 우리가 넣은 기호(`|`·줄머리 `#`·`-`)가 있고 편집기 문서(`textContent`)에는 없다.
      - `foldStrict` — 기호만 공백으로 바꾸고 **어절 경계는 지킨다.** 뜻이 프린트에 실제로 적혀 있는지 보는 자리(`print-words` 의 `meaningIsPrinted`)는 **반드시 이쪽**이다. 공백까지 지우면 '아버지가 방에' 와 '아버지 가방에' 가, '-3' 과 '3' 이 같아져 **지어낸 뜻이 그대로 등록된다** — 그 함수가 막으려던 바로 그 일이다(1R 에서 느슨한 쪽으로 고쳤다가 2R 에 되돌렸다)
      - `foldLoose` — 공백과 기호를 모두 지운다. 표 한 행(`| 시어 | 뜻 |`)을 편집기 문서의 같은 자리(`시어뜻`)와 맞추려면 이 세기가 필요하다. **자리를 찾는 데만** 쓴다
      - ⚠️ 평문 모양을 바꿀 때는 **`print-words` 를 반드시 함께** 볼 것 — `htmlToPlainText` 는 두 기능이 공유한다. 뜻이 표의 두 칸에 걸치면 그 사이에 `|` 가 끼어 멀쩡한 뜻이 `unverified` 로 몰렸다
    - ⚠️ **자리 힌트는 '조상이 담고 있는가' 로 판정하지 않는다**(코덱스 리뷰 2R). 한 문단에 같은 말이 두 번 나오면 두 자리가 **똑같은 조상**을 가져 앞것이 늘 이긴다. 힌트가 덮는 **구간의 글자 범위**를 되찾아(`foldWithMap` 의 자리 표) 그 안에 있는 자리만 고른다. 그리고 **엄격한 세기를 먼저** 본다 — 느슨한 쪽은 `-` 를 지워 '가-나의 대비' 와 '가나의 대비' 를 같게 만든다
  - **(2026-09-15) 추천 빈칸의 기준은 짐작이 아니라 손 마킹 120장의 실측값이다**([concept-pick-criteria.md](docs/concept-pick-criteria.md)). 제보는 "자동 마킹이 여전히 별로다 — 우리가 수동으로 마킹해 둔 걸 참고해 달라" 였다. `exam.concept_sheets` 를 세어 보니 **선생님 1,000자당 52개 / 그날 AI 가 뚫은 프린트 3장은 9~14개** — 양이 1/5 이었다. 실측표를 프롬프트로 옮겼고, 되돌리지 말아야 할 것들:
    - ⚠️ **양은 '장당 몇 개' 가 아니라 밀도다**(`CONCEPT_PICK_DENSITY_PER_100`, 100자당 5개). 절대 개수로 되돌리면 본문이 길수록 더 크게 어긋난다 — 옛 '10개 안팎' 이 정확히 그 실패였다. 다만 **전부 원문·발문인 조각에서는 0개도 정답**이라고 함께 적는다(안 적으면 작품에 구멍을 낸다)
    - ⚠️ **'흔한 일반어는 고르지 않는다' 를 되살리지 말 것.** 선생님이 가장 자주 뚫는 말이 목적(29장)·이해(27)·주제(26)·사실(20)·상황(19)·사람·생각·말 이다. 기준은 낱말이 흔한지가 아니라 **그 문장의 알맹이인지**다
    - ⚠️ **표는 라벨 칸이 아니라 오른쪽 내용 칸**이다(2열 표 2번째 칸 4,364개 vs 1번째 칸 392개). 학교 프린트 시험지는 표가 거의 없고 `N. 물음 … 답: 정답` 꼴이라 **'답:' 뒤 문장에만** 뚫는다 — 옛 마킹은 발문에도 뚫어서(`2. 수만이의 【괴롭힘】`) 문제 자체를 지웠다
    - **한 글자·두 어절·되풀이 마킹은 알고도 안 한다.** 선생님은 한다(각각 5.6%·4.3%·23.9%). 한 글자는 `findMarkTarget` 이 더 긴 낱말 **안쪽**을 잡고, 두 어절은 `extractMarks` 가 쪼개 세어 **문항 수 = 합격 기준의 분모**를 부풀리며, 되풀이는 `addMarkByText` 가 한 자리만 붙인다. 되살리려면 그 셋을 먼저 고칠 것
  - **(2026-09-15) 추천은 본문을 묶음으로 나눠 묻고, 받는 즉시 마킹한다**([chunk.ts](src/lib/concept-pick/chunk.ts), [run.ts](src/lib/concept-pick/run.ts)). 밀도 기준으로 뚫으면 시험지 한 장에 수백 개라 한 응답에 못 담는다 — 담으면 스키마 상한에 잘려 **뒤쪽 본문이 통째로 빈칸 없이** 남는다:
    - ⚠️ **묶음은 줄 경계로만 자른다.** `plain-text.ts` 가 표 한 행을 `| 칸 | 칸 |` 한 줄로 만들어 둔 것을 줄 중간에서 자르면 행의 짝이 깨져, 그 표가 무관한 조각으로 보인다(그 파일이 애써 남긴 구조가 여기서 죽는다). 상한보다 긴 줄은 **쪼개지 않고** 그 줄 하나로 한 묶음이 된다
    - ⚠️ **'본문에 있는가' 는 그 묶음 안에서 본다**(`parseConceptPicks` 에 `plain: chunk`). 본문 전체로 견주면 모델이 보지도 않은 다른 묶음의 말이 통과한다
    - ⚠️ **묶음이 끝날 때마다 `onChunk` 로 올려 그 자리에서 붙인다.** 다 모아서 한 번에 붙이면 취소·실패가 곧 전부 잃는 일이 된다 — 묶음이 여럿이면 몇 분이 걸린다. 그래서 취소 토스트도 '오류' 가 아니라 **"n개까지 마킹하고 멈췄어요"** 다
    - **앞 묶음에서 고른 말을 다음 묶음의 '이미고른용어' 로 넘긴다** — 안 넘기면 같은 말이 묶음마다 다시 온다
    - **`CONCEPT_PICK_MAX_COUNT`(60)의 뜻이 '한 번의 상한' 에서 '묶음 하나의 상한' 으로 바뀌었다.** 스키마 `maxItems`·프롬프트 문구·파서 자름이 그 값 하나를 보는 계약은 그대로다
    - ⚠️ **실행 자리는 킬스위치 확인보다 먼저 맡는다**(코덱스 리뷰 2R). `ocrStillEnabled` 가 `await` 라 재진입 관문(`abortRef.current`)을 그 뒤에 채우면, 확인을 기다리는 사이의 두 번째 누름이 그대로 통과해 **생성이 둘 돌고** 먼저 끝난 쪽이 다른 쪽의 취소 손잡이를 지우고 되돌리기 잠금까지 푼다. 대신 확인에 실패하면 맡은 자리를 **도로 놓아야** 한다(안 놓으면 영영 다시 못 누른다)
    - ⚠️ **그러면 킬스위치 확인을 취소로 끊을 수 있어야 한다**(코덱스 리뷰 3~4R — 2R 수정이 만든 결함). 자리를 그 확인보다 먼저 맡으므로, `/api/ai/status` 요청이 멎으면 '취소' 가 끊을 것이 없어 **화면이 영영 잠긴 채** 남는다(`authFetch` 에는 타임아웃이 없다). `ocrStillEnabled(feature, signal)` 로 신호를 넘기되 **그것만으로는 모자라다**(4R): `authFetch` 안의 세션 조회·갱신(`getSession`/`refreshSession`)은 신호를 아예 안 받아서, 그쪽이 멎으면 확인이 영영 안 풀린다. 그래서 기다림 자체를 **경주**(`Promise.race` + `whenAborted`)로 둔다 — 늦게 끝날 요청은 부작용 없는 조회라 그냥 버린다. 돌아온 뒤 `'aborted'`/`signal.aborted` 로 **'사람이 끊은 것' 과 '기능이 꺼진 것' 을 가른다** — 취소를 '기능이 꺼졌어요' 라고 알리면 거짓말이다
    - ⚠️ **생성 중에는 되돌리기·개별 빼기를 잠근다**(코덱스 리뷰 2R). 되돌린 뒤 남은 묶음이 같은 말을 다시 붙이면 '되돌렸는데 그대로' 가 되고 완료 토스트의 수도 실제와 어긋난다
    - **묶기 비용은 줄과 줄 *사이*의 줄바꿈만 센다**(코덱스 리뷰 2R). 마지막 줄에까지 한 글자를 물리면 딱 맞는 본문이 공연히 두 묶음으로 갈려 AI 호출이 한 번 더 늘고 앞뒤 문맥이 끊긴다
    - **못 붙인 추천도 `taken` 에 남긴다**(의도). `addMarkByText` 는 문서 전체를 뒤지므로 한 번 못 붙인 말은 다음 묶음에서도 못 붙인다 — 다시 받아 봐야 '자리를 못 찾은 용어' 목록만 같은 이름으로 불어난다
    - **근거 한 줄(`reason`)을 스키마에서 뺐다.** 묶음마다 수십 개가 오는데 하나하나에 문장을 붙이면 출력 토큰이 곱절이라 기다리는 시간이 그만큼 늘고, 사이드바에서는 아무도 읽지 않았다(목록도 낱말 칩으로 바꿨다)
  - **(2026-09-15) 거꾸로 스캔한 프린트는 읽기 전에 바로 세운다**([lib/page-orientation](src/lib/page-orientation/)). 되돌리지 말아야 할 판단들:
    - ⚠️ **조용한 실패가 진짜 문제였다.** 프롬프트가 "읽을 내용이 없는 쪽도 html 을 빈 문자열로" 라고 시키므로 **빈 본문은 그 자체로는 오류가 아니다** — 180도 뒤집힌 쪽은 경고 **0개**로 빈 시험지가 되고 토스트는 "읽어 시험지를 만들었어요" 라고 말했다. 그래서 방향 판정이 `hasText` 를 함께 받아, **글자가 있다고 본 쪽이 비어 돌아오면** 경고한다(`unreadablePageWarnings`). 빈 뒷면까지 경고하면 경고 전체를 안 믿게 된다
    - ⚠️ **회전은 [renderPdfPage](src/lib/pdf/pdfRenderer.ts) 한 곳에만 둔다.** AI 로 보내는 이미지와 Storage 에 저장하는 원본 이미지가 **같은 함수**를 쓰는데, 한쪽만 돌리면 본문은 바로 섰는데 편집 화면 원본 대조만 거꾸로다. `PageCropper` 와 `renderPagesToImages` 가 **같은 `rotations`** 를 받는 이유다. 회전은 PDF 자체의 `/Rotate` 에 **더한다**(문서가 이미 눕혀 둔 쪽을 되돌리지 않기 위해)
    - **2단 가르기는 돌린 뒤에 한다** — 뒤집힌 쪽은 왼쪽 단과 오른쪽 단도 뒤바뀌어서, 안 돌리고 가르면 `describeImages` 의 "왼쪽 단 → 오른쪽 단" 설명이 거짓이 된다
    - **판정은 fail-open 이다.** 취소만 던지고 나머지 오류는 '돌리지 않음' 으로 두고 읽는다 — 보조 단계가 본 기능을 막으면 안 된다. 모르는 각도도 0 으로 본다(엉뚱하게 돌리면 멀쩡한 쪽까지 못 읽는다)
    - **각도를 저장하지 않는다.** 작은 이미지로 방향만 묻는 싼 단계라 '다시 읽기' 때 다시 물으면 되고, 그래야 옛 묶음도 그대로 고쳐진다
    - **스캔 전체를 한 번에 판정하고 `runBundle` 에 넘긴다** — 여러 묶음이 같은 문서를 나눠 쓰므로 묶음마다 물으면 같은 쪽을 여러 번 묻는다. 쪽 이미지를 **올리기 전에** 판정해야 저장된 원본도 바로 선다
    - ⚠️ **기출(problem-ocr)에는 아직 안 붙였다** — 모델이 준 좌표로 그림·문항을 잘라내므로 이미지를 돌리면 크롭 좌표계까지 같이 돌려야 한다. 파이프라인(`renderPdfPage` 의 `rotation`)은 이미 있으니 붙일 때 그것을 쓸 것
    - **업로드 화면의 썸네일·크게 보기는 원본 그대로**(뒤집힌 채) 둔다. 아직 AI 를 부르기 전이라 판정 결과가 없고, 쪽을 고르는 데는 지장이 없다
    - ⚠️ **'다시 읽기' 는 쪽 이미지를 늘 다시 만들되, 옛 파일을 지우지 않는다**(코덱스 리뷰 1~2R·정지 리뷰). 세 번 고친 자리다:
      - `uploadProblemFile` 은 `upsert:false` 라 그냥 올리면 **거꾸로 된 옛 이미지가 그대로 남는다** — 본문은 바로 섰는데 대조할 그림만 뒤집힌 채가 된다
      - '돌려야 하는 쪽만' 다시 올리는 것으로는 모자란다. 지난번에 180도로 저장해 둔 쪽이 이번 판정에서 0도가 되면 그 쪽을 건드리지 않아 **여전히 거꾸로** 남는다(각도를 저장하지 않으므로 알 길이 없다)
      - ⚠️ **지우고 다시 올리지 말 것**(`replaceProblemFile`). 지운 뒤 업로드가 실패하면 **멀쩡하던 원본이 사라지고** 되돌릴 길이 없다. 대신 `printScanPagePath` 에 **판 꼬리표**를 붙여 새 경로에 올리고, 성공한 쪽만 갈아 끼운다(실패한 쪽은 옛 경로 그대로 — 방향이 어긋난 그림이라도 없는 것보다 낫다). 버킷에 UPDATE 정책이 없어(제자리 덮어쓰기 금지) 같은 경로를 두 번 쓸 수 없기 때문이기도 하다. 쓰지 않게 된 옛 파일은 **남겨 둔다** — 선택 삭제·출처 삭제와 같은 규약이다
    - ⚠️ **판정 단계는 쪽 하나가 실패해도 멈추지 않는다**(코덱스 리뷰). 미리보기 렌더를 `try` 밖에 두면 한 쪽의 렌더 실패가 **스캔 전체를 '실패' 로** 만든다 — fail-open 이라는 말이 무색해진다. 못 그린 쪽은 묶음에서 빼되, **순번이 곧 쪽**이므로 `asked` 에서도 빼야 답이 엉뚱한 쪽에 붙지 않는다. 취소는 묶음이 아니라 **쪽마다** 확인한다
    - ⚠️ **빈 본문 판정은 문자열이 아니라 보이는 글자로 한다**(코덱스 리뷰). 빈 줄을 `<p></p>` 로 옮기라고 시켜 놨으므로 `html.trim() !== ''` 는 그 쪽을 '글이 있다' 고 본다 — 정작 한 글자도 못 읽은 쪽이 경고를 빠져나간다. 공백 엔티티는 이름(`&nbsp;`)으로도 숫자(`&#160;`)로도 오므로 **둘 다** 푼다. 그리고 모델이 **아예 안 낸 쪽**(`missing`)은 파서가 이미 경고했으므로 또 하지 않는다: 중복 경고는 상한(40) 자리를 먹어 진짜 경고를 밀어낸다
    - ⚠️ **빠진 쪽 경고는 파서 경고 목록의 맨 앞**이다(코덱스 리뷰 2R). 모델이 낸 경고가 상한(20)을 채우면 뒤에 붙은 그 경고가 잘리는데, 그 쪽은 `missing` 이라 뒤에서 다시 경고하지 않으므로 **쪽 하나가 통째로 빠진 사실이 어디에도 안 남는다**
    - ⚠️ **취소는 묶음 안에서도, 돌아가기 직전에도 본다.** 마지막 묶음이 통째로 렌더에 실패하면서 그 사이 취소되면 아무 검사도 안 거치고 **정상 반환**이 되어 부른 쪽이 취소를 못 알아챈다
  - **`exam.sync_school_name` / `exam.sync_school_material_name` 의 정본은 sql/26 이다.** sql/01·15 는 무자격(`public` 가능) 정의라 이번에 `exam.` 으로 다시 만들고 **트리거도 다시 걸었다**. 묶음도 학교명·프린트명을 텍스트로 복사해 들고 있어 이름 변경을 따라가야 한다(앱 폴백도 [schools.ts](src/lib/category-master/schools.ts)·[school-materials.ts](src/lib/category-master/school-materials.ts) 에 함께 넣었다). **(2026-09-14)** 이제 이 트리거를 발화시키는 주 경로는 `ensureSchoolMirror` 의 upsert 와 ara-system mig481 트리거다 — 관리자시스템의 학교 rename 이 exam 스키마에 닿는 길이 그 둘뿐이다
  - **편집 화면은 [ConceptSheetWorkspace](src/components/exam-builder/ConceptSheetWorkspace.tsx) 한 벌**이고, `useConceptSheetEditor` 는 경로를 **인자로 받는다**(`sheetId`·`listHref`). 두 벌로 두면 인쇄·마킹·저장이 갈라진다
  - **신규 부트스트랩 범위는 01~27 이다**
- [2026-09-12] **출처 삭제는 DB 행만 지운다 — Storage 는 건드리지 않는다**([useSourceDelete.ts](src/hooks/useSourceDelete.ts), [source-delete.ts](src/lib/problem-bank/source-delete.ts)). 되돌리지 말아야 할 판단들:
  - **`deleteSource` 는 예전부터 있었지만 부르는 곳이 하나도 없었다.** DB 는 막는 게 없다(sql/17 의 `FOR ALL` 정책에 DELETE 가 포함되고, 지문·문항은 CASCADE, `problem_paper_items.problem_id` 는 SET NULL). "삭제가 아예 안 된다" 는 제보의 원인은 **버튼이 없던 것뿐**이었다
  - **Storage 파일은 남긴다.** `deleteProblems` 와 같은 근거다 — 잘라 둔 `problems/`·`passages/` 이미지는 문제지 스냅샷이 들고 있어 지우면 인쇄물이 빈칸이 된다. `sources/{id}/` 아래(원본 PDF·쪽 이미지·답지)는 스냅샷이 참조하지 않아 지워도 안전하지만, **규칙을 하나로 두는 편이 낫다고 보고 전부 남긴다**. 그래서 `removeProblemFiles`·`sourceFolder` 는 계속 미사용이다 — 나중에 정리 기능을 만든다면 그 둘이 출발점이다
  - ⚠️ **`supabase.delete()` 는 0행을 지워도 `error` 가 null 이다.** `deleteSource` 는 `.select('id')` 로 지운 행을 돌려받아 비어 있으면 던진다. 이걸 빼면 "성공 토스트가 뜨는데 목록에는 그대로" 가 되어 **이번 제보와 똑같은 모양의 버그**가 다시 생긴다
  - **확인 문구는 `lib/` 의 순수 함수**(`sourceDeleteConfirmMessage`)다. 문장마다 조건이 달라(문제지 수 > 0, 상태가 '추출중') 화면에 흩어 두면 "문제지가 0개인데 문제지 얘기가 나오는" 거짓말이 조용히 생긴다. `selection.ts` 의 `bulkDeleteConfirmMessage` 와 같은 규약이고, 스냅샷 안내 문장은 **글자 그대로 같은 것**을 쓴다
  - **미저장 수정(`dirtyIds`) 확인은 일부러 건너뛴다.** 어차피 출처를 통째로 버리는 참이고, 되돌릴 수 없는 확인창 위에 확인창을 하나 더 얹으면 읽지 않고 누르게 된다. 같은 이유로 훅을 `useReviewGuards` 에 넣지 않았다 — 그쪽은 `review`+`dirtyIds` 를 요구하는데 목록 화면에는 둘 다 없다
  - ⚠️ **지울 수 있는 목록은 offset 페이징이 통째로 위험해진다**([source-list.ts](src/lib/problem-bank/source-list.ts), [sources/page.tsx](src/app/(main)/problems/sources/page.tsx)). 출처 목록은 '더 보기' 로 이어 붙이는데, 한 행이 사라지면 **뒤쪽 offset 이 전부 하나씩 당겨진다**. 세 함정을 모두 피해야 한다(코덱스 리뷰 P1·P2):
    - ① 예전처럼 `sources.length` 로 다음 쪽을 역산하면 한 행이 빠진 순간 '더 보기' 가 **0쪽을 다시 불러 중복 행**을 만든다
    - ② '불러온 쪽 수' 카운터만 두고 이어 받으면 **더 나쁘다** — 경계에 걸린 행 하나가 **영영 안 보인다**(선생님은 그게 지워진 줄 안다). 그래서 지운 뒤에는 `refetchSources(펼친 쪽 수)` 로 **0쪽부터 통째로** 다시 읽는다
    - ③ 그 재조회가 **실패하면 목록은 어긋난 채로 남는다.** 토스트만 띄우고 넘어가면 다음 '더 보기' 가 ②를 그대로 일으킨다 — `needsResync` 로 '더 보기' 를 닫고 **다시 읽기만 내준다**
    - 쪽 수는 **state 가 아니라 ref**(`loadedPagesRef`)다. 확인창을 띄운 사이 '더 보기' 가 끝나면 삭제가 붙들고 있던 옛 값으로 다시 읽어 뒤쪽 한 쪽이 통째로 사라진다
    - 목록 요청에는 **세대 번호**(`reqSeq`)를 붙여 늦게 온 응답을 버린다. 재조회와 '더 보기' 가 겹치면 서로를 덮어써 목록과 쪽 수가 어긋나고, 그때부터 쪽이 통째로 건너뛰어진다. 같은 이유로 `useSourceDelete` 는 **`onDeleted` 를 await** 한 뒤에야 잠금을 푼다
    - `selection.ts` 의 `pageAfterDelete` 는 여기 안 맞는다 — 그건 '현재 쪽' 이 있는 offset 페이징용이고 이 화면은 이어 붙이는 '더 보기' 다
  - ⚠️ **여러 쪽을 한 번에 다시 읽을 때 PostgREST 기본 상한(1,000행)을 넘기지 말 것.** 34쪽(1,020행)을 한 번에 청하면 20행이 **말없이** 빠지고, 그 뒤 '더 보기' 는 1,020 부터 이어 받아 빠진 20행이 영영 안 보인다. `sourceRefetchChunks` 가 상한 아래로 나누고 `refetchSources` 가 순서대로 받아 잇는다
  - **삭제 훅의 잠금은 state 가 아니라 `busyRef` 다** — 같은 실행 흐름에서 두 번 부르면 state 는 아직 안 바뀌어 있어 두 번째가 통과한다(화면의 `disabled` 는 렌더 뒤에나 걸린다). 그리고 `await deleteSource` **뒤에도** 생존을 확인한다 — 지우는 사이 다른 메뉴로 떠난 사람을 목록으로 끌고 가면 안 된다(성공 토스트는 사실이므로 그대로 띄운다)
  - **순수 로직은 supabase 를 부르지 않는 파일에 둔다**([source-delete.ts](src/lib/problem-bank/source-delete.ts)). 이 저장소에는 supabase 를 목으로 감싸 테스트한 전례가 없어서, 확인 문구와 쪽 나누기처럼 값만 다루는 부분을 떼어 두어야 클라이언트 없이 검증된다
  - **목록 행을 통째로 `<Link>` 로 감싸지 않는다.** 그 안에 버튼을 넣으면 잘못된 HTML 이고 키보드로도 못 쓴다 — [papers/page.tsx](src/app/(main)/problems/papers/page.tsx) 와 같은 모양(flex 래퍼 + `flex-1` 링크 + 형제 버튼)으로 나눈다
- [2026-09-11] **순환 import 는 상수를 조용히 `undefined` 로 만든다 — 그래프를 테스트로 고정한다**([src/lib/__checks/import-cycles.test.ts](src/lib/__checks/import-cycles.test.ts)). 프로덕션이 이것 때문에 멈췄다: 이미지 예산이 `pdfPages` 에 있고 `pdfColumns` 가 그것을 import 하는데 `pdfPages` 도 `pdfColumns` 를 import 해 순환이 됐다. 번들러가 CommonJS 로 풀면 나중에 초기화되는 쪽이 `undefined` 를 읽어 단 이미지 예산이 **NaN** 이 되고, `url.length <= NaN` 이 늘 거짓이라 **모든 2단 쪽이 조용히 건너뛰어져** 기출 읽기가 통째로 실패했다
  - ⚠️ **테스트(Vite/ESM)는 이것을 못 잡는다.** 평가 순서가 번들러와 달라 그냥 통과한다 — 그래서 값이 아니라 **import 그래프 자체**를 본다. `tsc`·`eslint`·`next build` 도 전부 통과했다
  - **예산·인코딩의 단일 출처는 [pdfBudget.ts](src/lib/pdf/pdfBudget.ts)** 다. `pdfPages` 와 `pdfColumns` 는 **거기서만** 가져오고 서로에게서 값을 가져오지 않는다(타입은 `import type` 으로만)
  - `encodeWithinBudget` 은 예산이 숫자가 아니면 **터뜨린다.** 조용히 모든 쪽을 건너뛰는 것보다 낫다 — 이 사고의 증상이 정확히 그 침묵이었다

- [2026-09-11] **기출 OCR 2차 — 검수량 줄이기·그림 제자리·쪽 넘김 복구**([sql/22](sql/22_problem_bank_merge_passages.sql), [sql/23](sql/23_problem_bank_figures.sql)). 되돌리지 말 것:
  - **2단 쪽은 단별 이미지 두 장으로 보낸다**([columnDetect.ts](src/lib/pdf/columnDetect.ts), [pdfColumns.ts](src/lib/pdf/pdfColumns.ts), `OCR_SPLIT_COLUMNS`). 까닭이 둘이다 — ① 쪽 전체를 한 장으로 보내면 시각 모델이 **왼쪽 단 첫 줄 다음에 오른쪽 단 첫 줄**을 읽어 두 글을 한 문단으로 섞는다. 갈라 보내면 읽을 순서가 하나뿐이라 그 실수가 **구조적으로** 사라진다. ② 인코딩 사다리의 `maxSide` 는 **긴 변(세로)** 에 걸리는데 단 이미지는 세로가 쪽과 같고 가로만 절반이라, 배율을 3으로 올려도 사다리가 세로만 되돌리고 **가로 화소는 남는다**(A4 단 하나 595px → 892px)
    - ⚠️ **홈을 못 찾으면 가르지 않는다.** 1단 조판을 반으로 자르면 모든 줄이 두 동강 난다 — 못 갈라 손해 보는 쪽이 훨씬 낫다. 양쪽에 실제로 글자가 있어야 2단으로 본다
    - ⚠️ **가로만 자른다.** 세로를 건드리면 모델이 주는 `top`·`bottom` 이 쪽 기준이 아니게 되어 기존 크롭(`boxToBbox`)이 통째로 어긋난다
    - ⚠️ `쪽수 × 2 ≤ MAX_TURN_IMAGES` 를 [constants.test.ts](src/lib/problem-ocr/constants.test.ts) 가 고정한다. 넘으면 `generateDraft` 가 이미지를 **말없이 잘라내** 내용이 조용히 사라진다
  - **`renderPagesToImages` 의 `rendered` 는 `{page, part}[]` 다**(쪽 번호 배열이 아니다). 한 쪽이 두 장이 되므로 "이미지 순서 = 이 쪽" 약속을 쪽 번호만으로는 지킬 수 없다. 프롬프트는 이미지마다 몇 쪽의 어느 단인지 밝히고, **단과 단 사이는 쪽 경계가 아니라고** 못박는다(안 그러면 한 지문이 둘로 쪼개진다)
  - **본문 절단은 반드시 경고한다**(`OCR_HTML_MAX`). 예전 6,000자 `slice` 는 말이 없어서 "AI 가 안 읽었다" 와 "우리가 잘랐다" 를 구분할 수 없었다. **쪽 넘김 지문 뒷부분이 사라지던 첫 번째 경로**다
  - **실패한 묶음은 쪽을 쪼개 한 번 더 읽는다**([batch-attempt.ts](src/lib/problem-ocr/batch-attempt.ts)). 겹침이 1쪽뿐이라 묶음 가운데 쪽은 그 묶음만 보는데, 죽으면 통째로 사라졌다(**두 번째 경로**). 국어 지문 전사는 출력이 길어 워치독에 걸리기 쉬운데 쪽을 줄이면 대개 통과한다. 살려 낸 쪽은 실패로 세지 않고, 보고할 원인은 **첫 시도**의 것이다(재시도 쪽 오류가 더 흐릴 때가 많다). 한도·권한·취소는 다시 시도하지 않는다
  - **`findOpenPassage` 는 닫힌 지문에도 붙인다.** 모델이 `continues` 를 자주 빠뜨리는데 ①만 보면 뒷부분이 주인 없는 지문으로 떨어졌다(**세 번째 경로**). 붙이기 전 `alreadyContains` 로 중복을 거른다 — 앞 묶음이 이미 뒷부분까지 읽어 뒀으면 또 붙어 같은 글이 두 번 인쇄된다. ⚠️ **앞부분만 견주면 안 된다.** 앞 묶음이 다음 쪽 첫 문단까지만 읽어 둔 경우가 흔한데, 앞부분이 맞는다고 조각을 통째로 버리면 **되찾은 뒷부분이 사라진다.** 끝부분까지 맞을 때만 버리고, 앞부분만 겹치면 **붙이고 경고한다**(겹쳐 보이는 편이 사라지는 것보다 낫다 — 눈에 띄고 지울 수 있다)
  - **머리글은 `normalizeLabel` 로 통일한다**(물결표 변종·전각·괄호). `[1~3]` 과 `[1 ∼ 3]` 이 갈리면 중복 판정 키가 갈려 **잘린 지문 두 개**가 남는다. 파서 저장값과 병합 키가 **같은 함수**를 쓴다
  - **기계 대조는 확실할 때만 말한다**([verify-structure.ts](src/lib/problem-ocr/verify-structure.ts), [verify-text.ts](src/lib/problem-ocr/verify-text.ts)). 번호가 한 번이라도 겹치면 빠짐 검사를 **아예 건너뛰고**(문제집은 절마다 1번부터 다시 센다), 형식이 뒤섞인 자료에는 '보통 몇 개' 기준을 세우지 않는다. 틀린 경고가 섞이면 선생님이 경고 전체를 안 믿게 되고 진짜 문제까지 묻힌다
  - **그림은 부분만 잘라 본문 제자리에 끼운다.** 저장 HTML 에는 URL 없는 자리표시자 `<figure data-figure="n">` 만 둔다([figure-placeholders.ts](src/lib/problem-bank/figure-placeholders.ts)) — `<img>` 금지는 그대로다(서명 URL 은 만료돼 본문에 굳힐 수 없다). 숫자는 `figure_paths` 의 1-based 순번이고 그리는 쪽이 그 자리에서 갈라 끼운다
    - ⚠️ **편집기에 TipTap 노드가 있어야 한다**([FigurePlaceholderNode.ts](src/components/problem-editor/FigurePlaceholderNode.ts)). 없으면 글자 하나만 고쳐도 자리표시자가 통째로 사라진다(`data-box` 가 겪었던 함정과 같다)
    - ⚠️ **그림 파일 이름을 배열 길이로 정하면 안 된다.** 가운데를 지우면 배열은 줄어도 남은 파일 이름은 그대로라, 다음 그림이 **쓰고 있는 파일을 덮어쓴다**(`freeFigurePath`)
    - ⚠️ **뺄 때는 그 파일만 지운다.** 번호는 당겨지지만 이름은 그대로라, 이름만 보고 지우면 쓰고 있는 그림이 사라진다
    - ⚠️ 그림을 붙이거나 뺄 때는 **본문과 경로를 한 번에** 저장한다. 따로 저장될 틈을 주면 자리표시자와 그림 수가 어긋나 빈칸이 남는다
  - ⚠️ **이미지 출제 지문에 뒷부분을 이어 붙였으면 저장할 때 '글로' 되돌린다.** 인쇄는 잘라 둔 **시작 쪽** 이미지를 쓰므로, 안 바꾸면 되찾은 글이 여전히 인쇄물에 안 나간다. 그 표시는 **저장이 성공했을 때만** 내린다(실패했는데 내리면 다시 눌러도 이미지인 채로 남는다)
  - ⚠️ **'다음 쪽 이어 읽기' 는 그림 자리표시자를 지운다.** 그 길은 좌표를 받지 않아 그림을 잘라 낼 수 없는데, 번호만 남기면 그 지문에 **이미 있던 다른 그림**이 그 자리에 그려진다. 대신 `has_figure` 로 받아 "직접 잘라 넣어 달라" 고 알린다 — 공용 서식 규약(`BODY_FORMAT_RULES`)이 `<figure>` 를 시키므로 이 길에서만 따로 막아야 한다
  - ⚠️ **끌어 잡기는 브라우저 기본 끌어놓기를 꺼야 한다**(`draggable={false}` + `preventDefault`). 안 끄면 HTML drag 가 시작되며 포인터 흐름이 `pointercancel` 로 끊겨 잡기가 아예 안 된다
  - ⚠️ **자리표시자 규칙은 다른 속성이 섞여도 알아봐야 한다.** 편집기가 내는 HTML 은 정화를 거치기 **전에** 지역 state 로 들어온다. 속성 순서까지 따지면 그때 못 알아봐 그림을 뺄 때 경로만 빠지고 표시가 남는다 — 남은 그림이 엉뚱한 자리에 그려진다. 노드도 `class` 를 내지 않는다(모양은 `figure[data-figure]` 선택자가 맡는다)
  - ⚠️ **자리표시자 번호는 늘 그 판의 그림 목록 기준이다.** 좌표를 못 읽어 그림을 버릴 때는 **번호를 옮겨 붙이고**(`remapFigurePlaceholders`), 조각을 갈아 끼울 때는 **글과 그림을 함께** 간다. 한쪽만 바꾸면 1번 자리에 딴 그림이 그려진다 — **없는 것보다 나쁘다**(없는 것은 눈에 띄지만 딴 그림은 안 띈다). 버리는 판이 그림을 더 알아봤으면 조용히 넘기지 말고 경고한다. 문항도 같다 — 빈 발문을 갈아 끼울 때 그림을 함께 간다(`fillGaps`). 그림을 **뒤늦게 알아본** 경우도 발문과 함께 받는다: 첫 판이 좌표를 못 내면 파서가 자리표시자까지 지우므로, '자리표시자가 있을 때만' 으로 두면 영영 못 받는다. 글이 짧아지는 판으로는 바꾸지 않는다
  - ⚠️ **미저장 판정에 그림 경로와 밀린 출제 방식을 센다.** 그림을 뺐는데 저장이 실패하면 화면에서만 사라지는데, '저장 안 됨' 이 안 뜨면 검수를 마친 뒤 되살아난다
  - ⚠️ **그림을 더 알아봤는지는 글 길이와 따로 본다.** `textOf` 가 자리표시자를 지우므로, 같은 글을 옮겼는데 이번에만 도표를 알아본 판은 길이가 똑같아 갈아 끼우지 못한다
  - ⚠️ **오래 걸리는 일이 끝난 뒤의 본문을 읽는다**([useTrackedState](src/hooks/useTrackedState.ts)). '그림 추가' 를 누른 뒤 원본을 끌기까지, 파일이 올라가는 동안, 다음 쪽을 읽는 수십 초 동안 선생님은 계속 글을 친다. 시작 시점 값을 닫아 두면 그 사이 친 글이 **말없이 되돌아간다**. 바뀐 값은 **저장을 기다리기 전에** 화면에 반영한다(그 왕복도 같은 창이다). ref 는 **설정 함수 안에서만** 쓴다(`react-hooks/refs` 가 렌더 중 쓰기를 막는다)
  - ⚠️ **조각은 모델이 낸 그대로 들고 있다가 합칠 때 한 번만 민다**(`joinFragments`). 밀어 둔 글을 들고 있으면 겹쳐 읽은 더 온전한 판으로 갈아 끼울 때마다 밀기를 되걸어야 하고, 그때 그림 목록까지 함께 갈지 않으면 새로 알아본 그림이 조용히 사라진다. 갈아 끼우기는 **글과 그림을 함께**(`replaceFragment`)
  - ⚠️ **자리표시자가 없는 그림은 본문 끝에 붙여 그린다** — 화면·인쇄 **양쪽 다**. 한쪽만 하면 선생님이 화면에서 본 그림이 인쇄물에서만 사라진다
  - ⚠️ **그리기는 HTML 을 자리표시자에서 자르지 않는다**([figure-render.ts](src/lib/problem-bank/figure-render.ts)). 그림은 〈보기〉 상자·표 칸 **안**에 있을 때가 많은데, 거기서 문자열을 자르면 여는 태그와 닫는 태그가 갈려 상자가 먼저 닫히고 **그림과 뒷글이 상자 밖으로 튀어나온다.** 그 자리의 태그만 `<img>` 로 바꾼다 — **정화 뒤에** 부르고 결과를 다시 정화하지 말 것(넣은 `<img>` 가 사라진다). 인쇄는 `splitHtmlBlocks` 로 **먼저** 쪼갠 뒤 그림만인 조각을 가려낸다
  - ⚠️ **몇 쪽에 걸쳤는지는 세지 말고 계산한다**(`lastPage - page_no + 1`). 겹쳐 읽은 묶음이 같은 이어짐을 두 번 내면 세기는 부풀고, 글이 이미 있어 안 붙인 경우에는 아예 안 는다. 그 값으로 `save.ts` 가 '이미지로 출제' 를 정하므로 1로 남으면 여러 쪽 지문이 **시작 쪽 이미지 하나로** 인쇄돼 뒷부분이 사라진다
  - ⚠️ **글자 레이어에서 가운데를 가로지르는 조각(머리글·전면 표)을 빼지 않는다.** 두 단 어디에도 안 들어가는데 버리면 그 글이 참고 텍스트에서 통째로 사라지고, 대조가 멀쩡한 문항을 '다르다' 고 짚는다
  - ⚠️ **이어지는 조각의 그림은 개수가 아니라 자리로 견준다.** 앞 묶음이 시작 쪽 그림 하나를 잡아 뒀는데 이번 조각이 **다음 쪽의 다른 그림** 하나를 알아본 경우, 개수만 보면 '이미 있다' 가 되어 그 그림이 잘리지도 저장되지도 않는다
    - **쪽 넘김 그림 지문의 오랜 한계가 여기서 풀린다** — 그림은 **자기 쪽에서** 잘리고(`FigureRegion` 이 쪽 번호를 함께 든다) 조각을 이어 붙일 때 번호를 민다. 그림을 하나라도 건진 지문은 경고에서 뺀다
    - **문제지 스냅샷에도 실어야 한다**(sql/23). 안 실으면 아카이브 화면은 멀쩡한데 **인쇄물에서만** 그림이 사라진다 — 가장 알아채기 어려운 결함이다
  - **참고 텍스트는 이미지보다 위에 두지 않는다**([pdfText.ts](src/lib/pdf/pdfText.ts), [page-text.ts](src/lib/problem-ocr/page-text.ts)). 글자만 믿게 하고 **구조는 이미지에서** 보게 한다 — 반대로 시키면 밑줄이 통째로 빠져 '밑줄 친 ㉠' 문항을 아무도 못 푼다. 복합기 자동 OCR 레이어는 `hasUsableText` 로 걸러낸다(우리가 쓰려는 것보다 못한 인식 결과라 **없느니만 못하다**)
  - **기출은 대부분 스캔본이다**(운영 확인, 2026-09-11). 글자 레이어는 드물게만 있다 — 정확도 대책의 우선순위를 여기에 맞춘다
  - ⚠️ **중복이라 안 붙여도 그 조각의 키는 등록한다.** 안 하면 겹쳐 읽은 다음 묶음이 같은 이어짐을 또 냈을 때 표에서 못 찾고, 그 사이 `lastPage` 가 그 쪽까지 와 있어 `findOpenPassage` 도 거른다 — **주인 없는 지문이 하나 더 생기고** 처음 보는 문항이 거기 붙는다. ⚠️ 다만 그 키는 **가리키기만 하는 자리**(`FragmentRef.alias`)로 표시한다 — 가리키는 것이 그 조각이 아니라 통째로 담은 큰 조각이라, 나중에 더 긴 이어짐이 와서 갈아 끼우면 지문 앞부분과 그림이 통째로 날아간다. 갈아 끼우지 않되 **새로 읽어 낸 것은 붙인다**(겹침 판정을 다시 하고 경고한다) — 그냥 넘기면 그것이 사라진다
  - ⚠️ **지문 조회 차례는 읽는 순서가 아니다.** `page_no` 다음이 **무작위 UUID** 라, 한 쪽에 지문이 둘이면 화면 차례가 원본과 무관해진다. 그 차례로 '앞 지문에 붙이기' 대상을 고르면 **엉뚱한 글에 이어 붙고 되돌릴 수 없다** — 읽어 온 뒤 `sortByReadingOrder`(쪽 → 단 → 위에서 아래)로 다시 세운다
  - ⚠️ `exam.merge_passages` 는 두 행을 `FOR UPDATE` 로 **잠그고** 읽는다. 안 그러면 그 사이 남이 고친 본문을 덮어쓴다. 합친 뒤 `render_mode` 는 **글로 되돌린다** — 잘라 둔 이미지는 한쪽 쪽만 담고 있다. **그림 경로를 이어 붙이고 자리표시자 번호를 민다**. 상한(9)을 넘으면 **아무것도 건드리지 않고 멈춘다** — 넘치는 그림만 버리고 성공했다고 알리면 지문이 지워진 뒤라 되돌릴 길이 없다([sql/24](sql/24_problem_bank_merge_figures.sql)) — 안 밀면 뒤 지문의 '1번' 이 앞 지문 그림을 가리킨다
  - ⚠️ **검수에서 그림을 빼도 Storage 파일은 지우지 않는다.** 이미 만든 문제지가 그 경로를 스냅샷에 들고 있어 지우면 **인쇄물에서 그 자리가 빈칸**이 된다(선택 삭제와 같은 규약). 그래서 사람이 끌어 잡은 그림은 **한 번만 쓰는 이름**을 붙인다(`capturedFigurePath`) — 순번으로 지으면 다음 그림이 그 이름을 골라 문제지가 쓰는 파일을 덮어쓴다
- [2026-09-11] **기출 업로드의 교과서는 '가장 가까운 내신 슬롯'에서 채운다**([scope-pick.ts](src/lib/problem-bank/scope-pick.ts)). 되돌리지 말아야 할 판단들:
  - **학교만 고르면 조회한다.** 예전에는 학교·학년·학년도·학기·시험 **다섯 키가 전부** 맞는 `public.school_exam_scopes` 행 하나만 찾아서, 2024 기출을 올리는데 내신 관리에 2026 행만 있으면 아무것도 안 채워졌다 — 학교를 고른 순간 그 학교의 교과서는 사실상 정해져 있는데도 매번 손으로 골랐다(선생님이 실제로 제기한 불편). 이제 그 학교의 슬롯을 **전부 읽어**(`fetchSchoolScopeRows`, 표가 작다: 학교 20 × 학년 ≤6 × 몇 해 × 4) 줄을 세운다
  - **줄 세우는 순서가 곧 계약이다**: 학년 일치 → 학년도 거리(가까운 해, 미지정이면 무시) → 학기 일치 → 시험 일치 → 최근 학년도 → **중간이 기말보다 먼저**(ara-system `semesterTextbookId` 의 학기 대표 교과서 규칙과 같다). 사전순 배열 비교라 앞 칸이 절대 우선 — 순서를 바꾸면 '다른 학년의 올해'가 '같은 학년의 재작년'을 이겨 다른 책을 자신 있게 채운다
  - ⚠️ **다른 학년에서 빌리는 것은 중등만**(`canBorrowAcrossGrades`). 고등은 학년마다 책이 다르다(공통국어 ↔ 문학·독서) — 빌리면 틀린 책을 자신 있게 채운다. 중등도 **같은 학년이 늘 먼저**이고(운영 데이터에 중1 미래엔·중2 비상인 학교가 실제로 있다) 그 학년에 등록이 없을 때만 빌린다. 학년 미지정이면 어느 학년이든 힌트로 본다
  - **범위(단원)는 정확한 슬롯의 것만 쓴다.** 교과서만 빌리고 단원은 빌리지 않는다 — 다른 학년도의 단원을 '이번 시험 범위'라고 보여 주면 거짓말이 된다. `noExam` 판정도 정확한 슬롯 기준이되, **그 학교의 다른 슬롯에서 교과서는 빌릴 수 있다**(안 보는 시험이라고 교과서가 없는 건 아니다)
  - **무엇을 보고 채웠는지 반드시 밝힌다**(`describeBasis` → '내신 관리 2026 중1 1학기 중간 기준'). 근거 없이 채우면 틀렸을 때 고칠 실마리가 없다
  - **`textbookAuto` 는 `titleAuto` 의 거울이다**([source-form.ts](src/lib/problem-bank/source-form.ts)). 직접 고른 교과서는 힌트가 못 덮고, 자동으로 채운 값은 조건이 바뀌면 따라 바뀌며(학년을 바꾸면 그 학년 책), 새 조건에 교과서가 없으면 **비운다** — 칸과 아래 안내가 어긋나면 안 된다. ⚠️ 제목과 **한 가지가 다르다**: 교과서는 `'미지정'` 도 **직접 고른 값**이라 자동을 끈다 — 비었다고 다시 켜면 방금 고른 '미지정' 을 힌트가 곧바로 되돌려 놓아 '교과서를 모르겠다'고 말할 방법이 없어진다(코덱스 리뷰 1R 이 가리킨 자리이되, 되살리는 쪽이 아니라 계약을 분명히 하는 쪽으로 풀었다). 자동은 **학교·학교급을 바꿀 때** 다시 켜지고 그때는 칸도 함께 비운다 — 폼은 업로드 한 번짜리라 손으로 고른 값이 오래 남지 않는다. ⚠️ **힌트를 `onChange({ textbook })` 로 돌리면 안 된다** — 자동값이 '직접 고른 값'으로 둔갑해 그다음 학교·학년을 바꿔도 안 따라온다. `applyTextbookHint` 는 바뀐 게 없으면 **같은 객체**를 돌려준다(StrictMode 는 효과를 두 번 돌린다)
  - **학교를 바꾸면 교과서를 비운다.** 이전에는 학교급 변경 때만 비웠는데, 옛 학교의 교과서가 남아 있으면 새 학교의 힌트가 '이미 골라 뒀다'고 물러난다 — 자동 채움이 안 되던 두 번째 까닭이다
  - **학교 칸이 보이는 유형(내신기출·프린트)만 조회한다**(`visibleFields(...).includes('school_name')`). 유형을 바꿔도 `school_id` 는 남아 있어, 안 가리면 **화면에 안 보이는 학교**로 문제집의 교과서를 채운다
  - ⚠️ **학년도 `''` 는 `null` 이다**(`toScopeWanted`). `Number('')` 은 0 이고 `Number.isInteger(0)` 은 true 라, 예전 코드는 학년도 미지정일 때 **0학년도 슬롯**을 조회하고 있었다
  - **검수 화면은 자동 저장하지 않는다**([SourceTextbookPicker.tsx](src/components/problem-review/SourceTextbookPicker.tsx)) — 교과서가 비어 있을 때만 힌트를 읽어 '넣기' 버튼을 보여 준다. 화면을 열었다는 이유로 공유 표를 고치면 붙어 있던 단원 태그가 말없이 지워진다(교과서 변경은 태그를 지운다)
  - **운영 데이터 확인(2026-09-11)**: 중등 15곳 중 교과서가 등록된 곳은 9곳이고 그중 **1학기 중간 슬롯까지 정확히 맞는 곳은 5곳뿐**이었다 — 나머지는 2학기 행만 있어 옛 방식으로는 영영 안 채워졌다. **고등 5곳은 교과서 등록이 0건**이라 이 기능은 사실상 중등용이다
  - 조회 훅은 업로드·검수가 **한 벌을 쓴다**([useScopeHint.ts](src/hooks/useScopeHint.ts)) — 키 파생·취소 가드·fail-soft 를 두 벌로 만들면 한쪽만 고쳐진다
- [2026-09-11] **선택 칸은 `OptionSelect` 로 만든다 — 값과 이름을 한 배열에서 꺼낸다**([option-select.tsx](src/components/ui/option-select.tsx)). 되돌리지 말아야 할 판단들:
  - **`items` 와 `SelectItem` 이 같은 배열에서 나와야 한다.** 호출부마다 `items=` 를 따로 적는 방식은 이미 한 번 실패했다 — `CategoryForm` 에서 이 버그를 겪고 그 파일에만 고쳐 뒀는데, 그 뒤에 생긴 아카이브 필터·단어 정렬·AI 모델·검수 화면이 전부 같은 구멍에 빠졌다. 한 배열에서 둘을 만들면 어긋날 수가 없다
  - **원시 `Select` 의 `items` 는 타입으로 필수다**([select.tsx](src/components/ui/select.tsx)). 런타임 경고를 넣는 안은 버렸다 — `useSelectRootContext` 가 공개 진입점에 없어 `SelectValue` 가 Root 의 `items` 를 볼 수 없고, 이 저장소에는 logger 도 없다. 타입 쪽이 더 강하다: 렌더 전에, 모든 호출부에서, CI 에서 잡힌다
  - **`SelectItem` 의 `label` 을 '중복'이라고 지우지 말 것.** `items` 는 트리거 라벨을, `label` 은 글자를 쳐서 찾기(타이프어헤드)를 맡는다. 하나만 지우면 조용히 반쪽이 죽는다 — [option-select.test.tsx](src/components/ui/option-select.test.tsx) 가 고정한다
  - **값 = 이름인 칸도 옮겼다.** 그쪽은 폴백이 우연히 맞아 지금도 정상이지만, 남겨 두면 타입 강제의 그물에 구멍이 남고 다음 사람이 어느 쪽을 따라야 할지 모른다
  - **필터 줄의 규칙은 [filter-axes.ts](src/lib/problem-bank/filter-axes.ts) 한 곳에 있다**(`ProblemFilterBar` 259줄 → 87줄). '전체'·'미지정' 센티널, 조건이 걸린 칸을 남기는 `showAxis`(코덱스 3R), 패싯에 없는 현재 값을 끼우는 `withValue`(코덱스 4R), '미지정' 을 자유 텍스트 축에 두지 않는 규칙이 전부 여기다 — 화면으로 되돌려 흩지 말 것
- [2026-09-11] **문법 축은 마스터로 훑는다 — 0건 개념도 보여 준다**([grammar-browse-tree.ts](src/lib/problem-bank/grammar-browse-tree.ts), [GrammarTreePanel](src/components/problem-bank/GrammarTreePanel.tsx)). 되돌리지 말아야 할 판단들:
  - **이 트리만 패싯이 아니라 마스터로 만든다.** 나머지 셋은 '문항이 실제로 있는 값만' 인데(그 규약은 그대로 유효하다) 문법은 일부러 반대다. 까닭 셋: ① 마스터가 닫혀 있고 교재 목차대로 고정이라 다 펼쳐도 개수가 안 튄다(학교·작품은 데이터에서 자란다), ② 문법 태그는 업로드가 아니라 **나중에 손으로** 붙어서 패싯만 쓰면 **태그 0건 → 선택지 0건 → 붙일 길 없음** 의 닭-달걀이 된다(실제로 그래서 sql/21 이후 아무도 못 썼다), ③ `(0)` 이 "아직 아무도 안 붙였다" 는 정보다 — '이 학교 기출 0건' 과 달리 쓸모가 있다
  - **문법 필터 칸에는 `showAxis` 를 걸지 않는다.** 선택지가 마스터라 빌 일이 없어 그 가드의 근거(조건이 걸렸는데 칸이 없어 끌 수 없다)가 생기지 않는다. **다른 축에서는 반드시 유지할 것**
  - **가지 선택은 `(전체)` 잎으로 한다.** `FacetTree` 의 클릭 규약(잎만 고름)을 바꾸면 이미 배포된 트리 셋의 조작이 달라지고 '펼치기 vs 고르기' 어포던스를 새로 만들어야 한다. 교과서 단원 트리가 이미 쓰는 방식이라 선생님도 아는 조작이고, `FacetTree` 는 선택적 prop 둘(`dimmed`·`defaultExpandedDepth`)만 늘었다. ⚠️ 담화·어문 규정은 **2단에서 끝난다** — 그 가지에도 `(전체)` 가 붙는지 테스트가 고정한다
  - **`defaultExpandedDepth={1}`.** 기본값 2 면 대분류·중분류가 다 열려 첫 화면에 100줄이 넘게 쏟아진다
  - **건수는 전역값이고, 조상은 문항 단위로 중복을 없앤다**([grammar-counts.ts](src/lib/problem-bank/grammar-counts.ts)). 잎 건수를 더하면 한 문항이 피동·사동을 함께 들고 있을 때 조상에서 두 번 세어진다. 상위 검색이 잎들의 `overlaps`('하나라도 걸리면')라, 문항 단위 중복 제거라야 `(n)` 이 눌렀을 때의 목록 길이와 맞는다. 필터를 반영하는 건수는 **안 만든다** — `search` 가 필터에 있어 글자마다 20k행을 다시 훑게 된다
  - ⚠️ **문법 트리도 다른 축을 비운다(네 트리가 서로).** 처음엔 '문법은 직교 축이니 안 비운다' 로 잡았다가 뒤집었다 — 건수가 전역값이라, 안 비우면 `(12)` 라고 적힌 가지를 눌렀는데 남은 교과서 조건 때문에 0건이 나와 **숫자가 거짓말을 한다.** '상현중 ∩ 음운 변동' 은 위쪽 필터 줄에서 겹치면 된다(CLAUDE.md 가 원래 적어 둔 길이다)
  - ⚠️ **반대 방향도 같이 넣었다** — `schoolExamFilterPatch`·`workFilterPatch`·`UnitTreePanel` 이 `grammar_path` 를 비운다. 예전엔 문법만 빠져 있어 계약이 반쪽이었다(문법을 고른 뒤 학교를 고르면 조용히 겹쳤다)
  - **`GRAMMAR_AXIS_CLEARED` 는 `filters.ts` 에 둔다.** 트리 모듈에 두면 순환 import 가 된다(`school-exam-tree`·`work-tree` 가 `filters` 를 쓰고, 문법 트리는 그 둘을 쓴다)
  - **`GrammarTreePanel` 만 `picked` state 가 없다.** 다른 패널은 노드 키가 조건으로 온전히 복원되지 않아(같은 이름 단원, `''`↔`'__none__'`) 따로 기억하지만, 문법은 고른 경로가 곧 `filters.grammar_path` 라 렌더에서 파생하면 된다 — '통일' 하려고 state 를 넣지 말 것
  - **DB 마이그레이션은 없다.** sql/21 이 `grammar_paths`·GIN·`add_grammar_paths` 를 이미 만들어 뒀고 이번 작업은 읽기 집계 + 화면이다. 건수 RPC 도 만들지 않았다 — 조상 펴기가 TS 마스터에 있어 `GROUP BY unnest` 로는 그 중복 제거를 못 한다
- [2026-09-10] **기출 업로드의 파일 순서는 안내로만 지킨다 — 자동 정렬·재정렬 UI 를 넣지 말 것.** OCR 은 올라온 순서를 그대로 믿는다(문제 쪽은 `problemPages` 오름차순, 답지 사진은 [answer-key-input.ts](src/lib/problem-ocr/answer-key-input.ts) `answerKeyStoragePlan` 의 배열 인덱스가 곧 문항 번호). 쪽의 **진짜 순서는 내용에만 있어서**, 알아내려면 이미지를 모델에 한 번 더 보내야 하고 그게 곧 토큰이다 — 얻는 것에 비해 값이 크다고 보고 안 한다. **파일명 자연 정렬(`naturalCompare`)은 공짜지만 그것도 답이 아니다**: 카톡으로 받은 사진·스캐너 기본 이름에는 순서가 안 담겨 있어 무력하면서, 선생님이 일부러 고른 순서를 뒤집는다. 그래서 **파일 고르는 자리마다 안내를 붙이고**([SourceFilePickers.tsx](src/components/problem-ocr/SourceFilePickers.tsx) 두 칸 · [PdfPageSelect.tsx](src/components/problem-ocr/PdfPageSelect.tsx) · [업로드 페이지](src/app/(main)/problems/upload/page.tsx) 맨 위와 읽기 직전), 고른 답지 사진에는 **번호를 붙여 보여 준다** — 예전 `truncate` 한 줄은 세 장만 넘어도 뒤가 잘려 뒤바뀐 순서를 눈치챌 방법이 없었다. **읽기 시작을 막는 확인창은 만들지 않았다**: 앱은 순서가 맞는지 알 방법이 없어(알려면 OCR) 못 지킬 확인이 되고 클릭만 늘린다
- [2026-09-09] **기출 문제 은행 — 문법 분류 축**([sql/21_problem_bank_grammar.sql](sql/21_problem_bank_grammar.sql), [grammar-tree.ts](src/lib/problem-bank/grammar-tree.ts)). 되돌리지 말아야 할 판단들:
  - **이 축만 저장 모양이 다르다.** `area_path`·`unit_path` 는 `TEXT[]` 하나가 **경로 하나**라 문항에 한 개만 붙지만, 수능 문법 문항은 개념 두셋을 걸친다. 그래서 `grammar_paths` 는 경로를 `' > '` 로 이어 붙인 **문자열을 원소로** 담는다(`{"단어 > 품사 > 명사","문장 > 문법 요소 > 피동 표현"}`). 그 결과 **상위 검색이 `contains`(@>)가 아니라 `overlaps`(&&)** 다 — `@>` 는 "준 것을 전부 가진 행"이라 잎을 여럿 넘기면 **전부 태깅된 문항만** 나온다. `filters.ts` 의 `toProblemQuery` 가 `grammarPathsUnder` 로 잎을 펴서 넘기고 `queries.ts` 가 `&&` 를 건다 — 한쪽만 바꾸면 상위 검색이 0건이 된다
  - **조상을 함께 저장하지 않는다.** `{'단어','단어 > 품사','단어 > 품사 > 명사'}` 로 펴 두면 `@>` 한 번으로 상위 검색이 되지만, 마스터를 고치는 순간 **저장된 조상만 낡아** 검색이 어긋난다. 저장은 사람이 고른 잎뿐이고, 펴는 일은 늘 손안에 있는 마스터가 그때그때 한다
  - **마스터는 DB 가 아니라 코드 상수다**(`GRAMMAR_TREE`). 수능 문법 체계는 교재 목차대로 고정이라 관리 화면·마스터 표를 두지 않았다. 덕분에 이 축에는 **트리를 못 읽어 검증을 건너뛰는 fail-soft 경로가 없다**(area/unit 과 다른 점 — 그쪽은 `longestKnownPrefix` 가 빈 트리를 통과시킨다). 항목을 고치려면 그 파일을 고쳐 배포한다
  - **선언 순서가 곧 화면 순서다.** 이름순으로 정렬하지 말 것 — 9품사(명사·대명사·수사…)나 음운 변동은 학교문법이 가르치는 순서가 따로 있고 선생님이 그 순서로 찾는다. 필터 선택지도 `GRAMMAR_ORDER`(목차 순서)로 정렬한다
  - **깊이가 가지마다 다르다.** 담화·어문 규정은 2단에서 끝나고 나머지는 3단이다. `AreaPathPicker` 는 `optionsAt` 이 비면 그 칸을 안 그려서 UI 가 알아서 처리한다 — 그래서 **DB CHECK 는 경로 길이가 아니라 태그 개수**(`cardinality <= 5`)를 건다
  - **`passages` 에는 넣지 않았다.** 문법 문항은 지문에 딸리지 않는 단독 문항이고 〈보기〉는 지문이 아니라 발문 안 상자다. 축을 하나만 두어 패싯·필터·병합이 전부 절반이다
  - **일괄 태깅은 RPC 한 번이고 합집합이다**(`exam.add_grammar_paths`). PostgREST 로는 배열 append 를 못 해서 행마다 읽고-합치고-쓰면 한 쪽에 60왕복이고 그 사이 남이 붙인 태그를 덮어쓴다. 덧붙이기만 하므로 **낙관적 동시성 조건이 필요 없다**. ⚠️ 합칠 때 **기존 태그를 앞에 두고 새 것을 뒤에 붙인 다음 상한까지 자른다** — 정렬해서 자르면 이미 5개인 문항에서 기존 태그가 밀려 사라져 붙이려던 것이 **지우는 일**이 된다. 이미 다 붙어 있는 행은 `IS DISTINCT FROM` 으로 건너뛴다(헛되이 `updated_at` 을 올리면 그 문항을 열어 둔 탭의 저장이 까닭 없이 충돌로 튕긴다)
  - **OCR 병합에서 이 축만 합집합**이다([merge-fill.ts](src/lib/problem-ocr/merge-fill.ts) 의 `fillGaps`). 나머지는 '비어 있을 때만 채운다' 지만 문법 태그는 서로 배타적이지 않고, 겹쳐 읽은 묶음이 각각 다른 개념을 알아볼 수 있다 — 빈 칸 규칙을 그대로 쓰면 나중 묶음이 본 개념이 통째로 버려진다
  - **모델에게 받는 모양과 저장 모양이 다르다.** JSON 스키마는 **마디 배열의 배열**(`[['단어','품사','명사']]`)이다 — 마디를 하나씩 내야 트리와 대조할 수 있다. `parse.ts` 가 그것을 경로 문자열로 접으므로 **`OcrItem.grammar_paths` 는 `string[]`**(접은 뒤)이고 스키마 객체는 `string[][]` 다. 둘을 같게 맞추려다 되돌리지 말 것
  - **문법 문항이 아니어도 칸을 막지는 않는다.** `isGrammarArea` 는 **접어 둘지**만 정하고 '+ 문법 분류 추가' 로 늘 열 수 있다 — 닫아 걸면 영역을 아직 안 고른 문항은 영영 분류할 수 없다(검수에서 교과서를 고칠 수 있게 만든 것과 같은 근거). 펼침은 **렌더에서 파생**한다(`opened || suggested || value.length > 0`) — 효과로 setState 하면 `set-state-in-effect` 에 걸리고, 영역을 문법으로 바꾸는 순간 칸이 따라 열려야 한다
  - ⚠️ **문법 영역 이름은 운영 마스터를 직접 보고 맞춘다**(`GRAMMAR_AREA_NAMES`). ara-system 마이그 335 의 **시드에는 `중등 국어 > 문법` 이 있지만 운영 마스터는 다르다** — 2022 개정 교육과정으로 바뀌어 중등·고등이 `화법과 언어 > 언어` 이고 `문법` 대영역은 초등에만 남아 있다. 시드 파일만 보고 판정 목록을 짜면 **이미 올라와 있는 문법 문항 전부에서 칸이 안 열린다**(실제로 그렇게 배포됐다가 실데이터를 보고 고쳤다). 마스터는 ara-system 이 소유하고 따로 갱신되므로 `public.exam_area_nodes` 를 조회해 확인할 것
  - **문법 트리는 OCR 프롬프트에 늘 실린다**(2,180자 ≈ 전체 프롬프트의 37%). 출처가 문법 시험지인지는 읽기 **전에** 알 수 없고, 이미지 3장이 훨씬 크므로 감당할 값으로 봤다
  - ⚠️ **마이그레이션 번호는 21 이다.** 같은 날 `sql/20_problem_bank_works.sql`(작품 축)이 먼저 들어갔다 — 번호가 곧 신규 부트스트랩 적용 순서라 겹치면 안 된다. 새 파일을 만들기 전에 `ls sql/` 로 최대 번호를 **다시** 확인할 것

- [2026-09-09] **기출 문제 은행 4차 — 상세 창·경고 위치·작품 축**([sql/20_problem_bank_works.sql](sql/20_problem_bank_works.sql)). 되돌리지 말아야 할 판단들:
  - **경고의 `ref` → 행 id 변환은 병합(merge.ts)이 한다.** 파서는 id 를 모르고(id 는 병합이 만든다) 병합이 끝나면 `ref` 가 사라진다 — 그 사이의 `refToId` 가 유일한 접점이다. ⚠️ 변환은 그 묶음의 **지문·문항 두 루프를 마친 뒤**에 해야 한다. 예전 자리(루프 앞)에서 풀면 표가 비어 있어 모든 경고가 쪽 대상으로 떨어진다. 겹쳐 읽어 이미 담은 문항도 `refToId` 에 넣어야 두 번째 묶음의 경고가 길을 잃지 않는다
  - **버려진 항목의 경고에는 `ref` 를 싣지 않는다**(보내지 않은 쪽·중복 항목). 행이 안 생기므로 가리킬 카드가 없고, 억지로 살아남은 항목을 짚으면 엉뚱한 카드를 지목한다 — 쪽만 알린다
  - **경고 중복 판정은 메시지가 아니라 `warningKey`(메시지+대상)** 다. 메시지로만 거르면 '선지를 읽지 못했어요' 가 첫 문항에서만 남고 나머지 문항은 표시가 사라진다
  - 단계별 상한(`OCR_MAX_WARNINGS` 20)과 **최종 상한**(`OCR_MAX_MERGED_WARNINGS` 60)이 다르다. 경고가 항목마다 붙게 되어 20 이면 30문항 시험지에서 뒤쪽이 잘린다. 병합 뒤에 붙는 경고(정답표·크롭·그림 지문)는 각자 상한을 안 거치므로 **저장 직전 run.ts 에서 한 번** `capWarnings` 한다
  - `OcrMeta.warnings` 는 `(string | {message,targets})[]` 다. **문자열을 지우지 말 것** — 이미 저장된 출처의 경고가 전부 문자열이라 객체만 받으면 옛 출처의 경고가 화면에서 통째로 사라진다
  - **읽기 전용 렌더는 인쇄 스코프 `.pb-sheet` 를 재사용한다**(`.pb-sheet--screen` 은 글자 크기만 덮는다). `data-box` 선택자 목록이 이미 인쇄·편집기 두 벌이라 세 번째 사본을 만들면 어느 한 벌만 고쳐져 화면과 인쇄가 조용히 갈라진다. 반대로 `renderPaperBlocks` 재사용은 **안 된다** — 번호를 자리 순서로 다시 매기고(7번이 01 로 나온다) 머리글을 합성한다
  - **드래그 손잡이는 `onClick` 도 막아야 한다.** `useListDrag` 의 `preventDefault` 는 호환 mouse 이벤트만 막고, `setPointerCapture` 때문에 pointerup 대상이 손잡이라 **끌기를 끝낼 때마다** click 이 카드로 번져 상세 창이 열린다. 카드 열기는 `window.getSelection()` 이 비어 있을 때만 — 글자를 긁어 읽던 사람이 창에 갇히면 안 된다
  - **작품 축은 `problems.work_title` 하나다**(인덱스·`search_text` 가 이미 여기 있다). 지문 제목과의 동기화는 **DB 트리거**가 맡는다 — OCR 저장·검수 수정·직접 SQL 어느 길로 와도 규칙이 같아야 한다. 앱에서만 맞추면 경로마다 어긋난다
  - **작품명 채우기 트리거는 `aa_` 로 시작해야 한다**(`aa_fill_work_title_problems`). 트리거는 알파벳 순으로 도는데 `problems_search_text` 가 `work_title` 을 검색 평문에 넣으므로, 뒤에 두면 물려받은 작품명이 **검색에서 조용히 빠진다**(sql/17 의 `aa_enforce_user_id` 와 같은 규약)
  - **지문 제목 변경은 딸린 문항의 `updated_at` 을 올린다**(트리거 → `update_updated_at`). `useProblemReview.savePassage` 가 제목이 바뀐 경우에만 문항을 다시 읽고 `reloadSeq` 를 올린다 — `removePassage` 와 같은 이유다. 안 하면 그 문항의 다음 저장이 **아무도 안 고쳤는데 충돌**로 튕긴다. ⚠️ 카드 key 에 `updated_at` 을 섞어 "바뀐 카드만" 다시 마운트하는 안은 버렸다 — `toggleRenderMode` 가 `render_mode` 만 저장해도 그 카드가 다시 마운트돼 치던 발문이 사라진다
  - **되돌림 백필**(문항 작품명 → 지문 제목)은 sql/20 에 한 번만 있다. 옛 프롬프트가 title 을 설명하지 않아 운영 데이터의 지문 제목이 **전부 비어 있었다**(2026-09-09: 지문 12건 전부 `''`, 문항 9건이 '동백꽃'). 안 하면 작품 트리가 '지은이 미입력' 한 폴더가 되고 묶음 머리가 '제목 없는 지문' 이 된다. **문항들이 한 작품으로 일치할 때만** 올린다 — (가)(나) 지문에서 하나를 고르면 거짓이 된다
  - **작품명 정규화는 앱 `normalizeWorkTitle` ↔ DB `exam.normalize_work_title` 1:1 거울**이다(`category-name.ts` ↔ sql/16 과 같은 계약). 감싸는 기호(「」『』〈〉""'')만 **양끝에서** 벗긴다 — 안쪽 기호는 '봄봄 · 「동백꽃」' 처럼 뜻이 있다. ⚠️ OCR 저장(`save.ts`)은 모델 값을 그대로 넣으므로 **파서 단계 정규화가 필수**다
  - **세 트리는 서로의 축을 비운다**(교과서·학교 기출·작품). 남기면 '상현중 기출 ∩ 동백꽃' 이 조용히 0건이 되는데 화면에 이유가 없다(학교마다 실리는 작품이 다르다). 작품명은 자유 텍스트라 `UNSPECIFIED_AXIS` 를 쓰지 않는다
  - **작품으로 볼 때만 목록을 지문별로 묶는다.** 그때만 정렬도 `source_id → passage_id → page_no → number` 로 바뀐다. ⚠️ 남은 한계: 한 쪽이 60개라 문항이 많은 지문은 두 쪽에 걸쳐 머리가 두 번 나온다
  - **이미지로 출제하는 문항은 상세 창에서도 이미지가 본문이다.** 글만 보여 주면 그림이
    통째로 빠진 문항을 보고 문제지에 담게 된다(프롬프트가 "옮길 수 있는 글자만" 적게 했다)
  - **'지문을 못 읽었다' 와 '지문이 없다' 를 가른다**(`passageId` 는 있는데 `passage` 가 null).
    못 읽은 것을 '지문 없는 문항' 이라고 적으면 거짓말이 된다(코덱스 리뷰 4R)
  - ⚠️ **남은 한계**: 작품명 저장 뒤 문항을 다시 읽는 **한 왕복 동안** 그 지문의 문항 카드를
    계속 칠 수 있고, 그때 친 내용은 사라진다. 지문 삭제·교과서 변경과 같은 성질의 창이라
    같은 수준으로 두었다(카드를 걷어 내는 처리는 조회 실패 때 오히려 입력을 잃게 만들어 되돌렸다)
  - ⚠️ **(가)(나) 복합 지문은 작품명 하나로 묶인다.** 프롬프트가 ' · ' 로 이어 적게 하지만 트리에서는 한 작품처럼 보인다. 문항별로 다른 작품을 적어 두면 트리거가 보존한다

- [2026-09-08] **기출 문제 은행 3차 — 선택 삭제·학교 기출 트리·별도 답지**([sql/19_problem_bank_answer_key.sql](sql/19_problem_bank_answer_key.sql)). 되돌리지 말아야 할 판단들:
  - **선택 삭제는 Storage 를 건드리지 않는다.** `removeProblemFiles` 를 부르고 싶어지지만, 이미 만든 문제지가 `render_mode:'image'` 항목의 `image_path` 를 스냅샷에 들고 있어 파일을 지우면 **인쇄물에서 그 문항만 빈칸**이 된다([problem-paper/blocks.ts](src/lib/problem-paper/blocks.ts)). 단건 삭제도 같은 이유로 파일을 남긴다 — 고아 파일은 감수한 값이다
  - **선택은 "지금 보이는 행"과 늘 교집합을 낸다**([selection.ts](src/lib/problem-bank/selection.ts)). 아카이브는 60개씩 쪽을 나누므로 선택한 채 쪽을 넘기면 화면에 없는 id 가 남는다. 효과로 비우면 `set-state-in-effect` 에 걸리므로 **렌더에서 파생**하고, 조건을 바꾸는 길(트리·필터바·페이저)은 전부 페이지의 `patch` 래퍼를 지나 선택을 비운다
  - **두 트리는 서로의 축을 비운다.** 교과서 단원 트리와 학교 기출 트리는 탭으로 갈린 **대안 경로**다. 조건을 남기면 '상현중 기출 ∩ 천재 3단원' 이 조용히 0건이 되는데 화면에 이유가 없다(학교마다 교과서가 다르다). 일부러 겹치려면 위쪽 필터 줄에서 고른다
  - **학교 기출 트리는 패싯으로 만든다** — `public.schools` 마스터를 그리면 문항이 하나도 없는 학교가 잔뜩 나온다(영역 필터와 같은 규약). 노드 키는 `JSON.stringify` 로 만든다: 학교 이름은 자유 텍스트라 `'|'` 구분자를 쓰면 이름에 그 글자가 든 순간 키가 섞인다
  - **별도 답지는 `sources/{id}/answer-key/` 에 둔다.** `pages/{n}.jpg` 와 같은 자리에 넣으면 답지 3장이 원본 1~3쪽을 덮어써 **검수 화면의 원본 대조가 통째로 망가진다**. 같은 이유로 답지 쪽을 `ocr_meta.pages` 에 넣지 않는다(그 배열이 검수 화면의 쪽 탭이다)
  - **답지 사진은 늘 다시 인코딩한다**([imageToJpeg.ts](src/lib/pdf/imageToJpeg.ts)). 버킷이 `application/pdf`·`image/jpeg` 만 받아 PNG 는 그대로 올리면 거부되고, 휴대폰 사진은 4000px·5MB 라 예산을 넘으며, `<img>` 로 그려야 EXIF 회전이 반영된다(누운 정답표는 모델이 못 읽는다). Storage 로 갈 Blob 은 반드시 `canvas.toBlob` — data URL → `fetch()` 는 CSP `connect-src` 가 막는다
  - **PDF 안 정답표와 별도 답지는 묶음을 섞지 않는다**([run-answer-key.ts](src/lib/problem-ocr/run-answer-key.ts)). 서로 다른 문서라 섞으면 "보낸 이미지는 N쪽" 안내가 거짓이 되고, 순서가 곧 번호인 정답표에서 순서가 엉킨다. 답지 PDF 는 문서를 한 번 더 여므로 `finally` 에서 반드시 `destroy` 한다
  - `insertSource` 는 **답지가 있을 때만** `answer_key_paths` 를 보낸다. 늘 보내면 마이그레이션이 늦은 순간 답지 없는 업로드까지 PGRST204 로 죽는다
  - **필터에서 `''` 는 '전체'라 '미지정만'을 따로 표시해야 한다**(`UNSPECIFIED_AXIS = '__none__'`, [filters.ts](src/lib/problem-bank/filters.ts)). 빈 값을 그대로 실으면 학교 기출 트리의 '미지정' 잎이 **그 축 전체**를 불러온다(코덱스 리뷰 2R). 그래서 `toProblemQuery` 는 빈 값을 **빼고** 센티널만 `''` 조건으로 바꾸며, `queries.ts` 는 참거짓이 아니라 **`!== undefined`** 로 조건 유무를 가른다 — 되돌리면 '미지정만' 이 조용히 사라진다. 셀렉트에도 같은 값의 '미지정' 칸이 있어야 화면과 조건이 어긋나지 않는다
  - ⚠️ **남겨 둔 한계**: 삭제 요청이 도는 동안 아카이브를 떠났다가 **돌아와서** 그 목록 조회가 DELETE 커밋보다 먼저 끝나면, 새 화면에 지운 문항이 잠깐 남는다. 옛 화면은 이미 언마운트돼 새 화면에 알릴 길이 없다. 고치려면 컴포넌트 수명 밖의 무효화 통로(모듈 수준 버스)가 필요한데, 데이터는 멀쩡하고 다음 조회에서 저절로 사라지는 표시 문제라 그 복잡도를 들이지 않았다(코덱스 리뷰 4R)
  - **필터 칸은 조건이 걸려 있으면 반드시 보이고, 걸린 값은 반드시 선택지에 있어야 한다**([ProblemFilterBar.tsx](src/components/problem-bank/ProblemFilterBar.tsx) 의 `showAxis`·`withValue`). 패싯은 **문항이 실제로 있는 값**만 모으므로, 선택지 유무로만 칸을 보이면 트리에서 고른 조건이 화면에서 사라지고(끌 수도 없다), 값이 목록에 없으면 셀렉트가 '전체'인 것처럼 비어 보인다(코덱스 리뷰 3R·4R)
  - **`aliveRef` 는 정리에서 false 로 두는 것만으로 부족하다** — 효과 본문에서 **true 로 되돌려야** 한다. StrictMode 는 마운트 → 언마운트 → 재마운트라, 안 되돌리면 재마운트 뒤 그 화면의 기능이 **조용히 통째로 죽는다**(선택 삭제가 확인창조차 안 뜨는 상태로 발견됨, 코덱스 리뷰 3R)
  - **지울 대상의 세대(`targetSeq`)는 조건·쪽·선택 **셋 다**에서 올린다.** 확인창 앞에 문제지 수를 묻는 왕복이 있어 그 틈에 체크를 바꾸면 확인창의 개수와 실제로 지울 문항이 어긋난다. 삭제 **뒤**의 쪽 보정에도 같은 검사가 필요하다 — 지우는 사이 필터가 바뀌면 옛 쪽 번호를 새 조건에 얹어 빈 목록을 띄운다(코덱스 리뷰 2R)
  - ⚠️ `updateSource` 는 `ocr_meta` 를 **통째로 덮어쓴다**. `answerKeyFiles` 를 성공·실패 두 분기 모두에 넣어야 검수 화면이 "답지를 안 올렸다"고 거짓말하지 않는다

- [2026-09-08] **기출 문제 은행 2차 — 학교 마스터·교과서 단원·서식**([sql/18_problem_bank_units.sql](sql/18_problem_bank_units.sql)). 되돌리지 말아야 할 판단들:
  - **학교급(중등/고등)은 저장하지 않는다.** 업로드 폼에서 학교·학년 선택지를 좁히는 데만 쓰고 DB 에서는 `grade`('중2') 접두사로 되찾는다(`levelFromGrade`). ara-system 마이그 420 과 같은 근거 — 저장하면 '중1인데 고등' 같은 어긋난 행을 CHECK 로 또 막아야 한다
  - 학교의 원본은 **관리자시스템 `public.schools`** 다(중등 15·고등 5, mig375 로 authenticated SELECT 열려 있다). ⚠️ **(2026-09-14 갱신)** 여기 적혀 있던 "이 앱의 `exam.schools` 는 외부지문·프린트용 **별도** 이름 마스터" 는 이제 **틀리다** — sql/27 이후 `exam.schools` 는 `public.schools` 의 **거울**이고 **id 가 같다**. 아래 2026-09-14 '학교 마스터 연동' 항목을 볼 것. `problem_sources.school_id` 는 **FK 가 아니다**(학교가 지워져도 기출은 남아야 한다). 표시·필터·문제지 스냅샷은 계속 `school_name` 을 쓴다
  - **교과서 단원의 정본은 이 앱의 카테고리 관리**(`exam.publishers › major_chapters › sub_chapters`)다. 관리자시스템의 '교과서' 탭(`public.curriculum_textbooks`)은 그것을 매일 밤 복사한 사본이라 정본이 아니다(운영 데이터의 47권이 전부 `source_app='ara-exam'`)
  - `unit_path` 는 `area_path` 와 같은 **이름 경로 스냅샷**이다(최대 2단, 마스터 id 가 아니다). 마스터 전개 카테고리는 실제 UUID 가 없고(`master-major-…` 합성 id), 개념지도 텍스트 복사 방식이라 같은 규약을 따른다. ⚠️ **남은 빈 구멍**: 카테고리 관리에서 단원 이름을 바꾸면 이미 태깅된 `unit_path` 는 따라가지 않는다. 필요해지면 `sync_major_chapter_name`/`sync_sub_chapter_name` 트리거를 `problems.unit_path[1]`/`[2]` 까지 넓히는 것이 정공법이다
  - (2026-09-08 3차 보정) 아카이브 필터에 **학기 축이 생겼다**(`ProblemFilters.semester`, 주소 `sem`). 학교 기출 트리가 쓴다 — 다만 교과서 단원 트리는 여전히 학기를 걸지 않는다(저장되는 것이 단원 **이름**이라 두 학기에 같은 이름이 등록돼 있으면 결과가 같은 편이 맞다)
  - **구역 상자는 `data-box` 하나로 간다.** 값의 종류가 곧 인쇄 모양이다(보기·자료·조건 → 〈보기〉 테두리, 가~마 → (가) 머리글, A~E → [A] 세로선). 괄호는 CSS 가 붙이므로 값에는 넣지 않는다. 허용 목록([src/lib/box-labels.ts](src/lib/box-labels.ts))을 넓히면 **인쇄·편집기 CSS 선택자 목록도 같이** 넓혀야 한다(값별로 나열한다 — `attr()` 로 종류를 가를 수 없다)
  - **`data-box` 값 정규화는 정화보다 먼저**다. 정화 훅은 허용 밖 값을 `keepAttr=false` 로 지워 되돌릴 수 없다 — 실제로 첫 OCR 에서 모델이 낸 `data-box="(가)"` 가 통째로 사라져 (가)(나) 지문 구분이 없어졌다. `normalizeBoxAttributes` 를 파서에서 `sanitizeProblemHTML` **앞에** 부른다
  - **배점은 읽지도 보여 주지도 않는다.** OCR 스키마·프롬프트·정답표에서 뺐고 화면·인쇄에서도 지웠다. 단 `problems.score` 컬럼과 `PaperSettings.showScore` 키는 **남긴다** — RPC 가 저장할 때마다 그 키를 화이트리스트로 다시 조립하고 이미 만든 문제지에 값이 들어 있다. 문제지 머리의 '점수란'(`ExamPrintHeader showScoreRow`)은 배점이 아니라 채점자가 쓰는 칸이라 그대로 둔다
  - **TipTap 3.x 두 가지**: ① `@tiptap/react` 는 `shouldRerenderOnTransaction` 이 기본 false 라 툴바에서 `editor.isActive()`·`getAttributes()` 를 직접 읽으면 커서를 옮겨도 갱신되지 않는다 → `useEditorState` 로 구독한다. ② StarterKit 3.x 는 `underline`·`hardBreak`·`horizontalRule`·`trailingNode` 를 이미 포함하므로 `@tiptap/extension-underline` 을 따로 등록하면 확장이 두 벌이 된다
  - **교과서를 바꾸면 이미 붙은 단원 태그는 지운다**(검수 화면에서 물어보고, `exam.set_source_textbook` RPC 가 **한 트랜잭션**으로 처리한다 — UPDATE 세 번으로 나누면 중간 실패 때 '태그만 사라지고 교과서는 그대로' 가 되어 손으로 붙인 분류를 잃는다. **지울지 말지는 DB 가 판단한다**: 앱이 미리 센 개수로 정하면 그 사이 다른 탭이 붙인 태그가 새 교과서 아래 남는다). 바꾼 뒤에는 본문을 다시 읽고 카드도 다시 마운트한다(카드가 단원·발문을 지역 state 로 들고 있어 옛 값이 되살아난다). 남겨 두면 아카이브가 '새 교과서 + 옛 단원' 으로 묶여 아무도 못 찾는다. 아카이브 트리에서 **학기는 필터 축이 아니다** — 저장하는 값이 단원 이름이라 1·2학기에 같은 이름이 등록된 교과서(운영 데이터 2건)는 두 폴더가 같은 결과를 낸다
  - **원문의 빈 줄은 빈 문단 `<p></p>`** 로 담고 인쇄 CSS 가 `p:empty::before` 로 한 줄 높이를 준다. `:empty` 는 공백 한 칸만 있어도 안 맞으므로 모양 통일은 파서(`normalizeBlankParagraphs`)가 한다. 반대로 `trailingNode` 가 표·상자 뒤에 **자동으로** 붙이는 빈 문단은 인쇄에서 걷어낸다(`html-trim.ts`) — 가운데 빈 줄은 원문이므로 남긴다
- [2026-09-08] **기출 문제 은행 도입**([sql/17_problem_bank.sql](sql/17_problem_bank.sql)). 표 5개(`problem_sources`/`passages`/`problems`/`problem_papers`/`problem_paper_items`)와 RPC `exam.create_problem_paper`. 굵직한 결정들:
  - **DB 헬퍼는 `public` 이 아니라 `exam` 스키마에 있다**(ara-system mig254 가 만들었다) — `exam.is_allowed_domain()`, `exam.enforce_user_id_from_auth()`, `exam.update_updated_at()`, `exam.audit_log`. sql/01~14 의 `public.is_allowed_domain()` 표기는 **옛 standalone 프로젝트 기준이라 지금은 틀리다**. 새 마이그레이션은 `exam.` 헬퍼를 쓰고 guard 블록에서 존재를 확인한다
  - 아카이브 3표는 `concept_sheets` 처럼 **공유 FOR ALL**(선생님들이 함께 검수한다). 문제지는 `exams` 처럼 **SELECT/DELETE 만 + RPC 한 곳**으로 잠갔다 — 본문이 스냅샷이라 직접 INSERT 를 허용하면 위조가 된다
  - **`passages`·`problems` 의 INSERT 는 감사하지 않는다.** OCR 한 번이 수백 행이라 본문 HTML 째로 `audit_log` 에 복사하면 표가 폭발한다. "언제 누가 무엇을 읽었나"는 `problem_sources.ocr_meta` + 그 행의 UPDATE 감사로 남는다. 사람이 하는 수정·삭제는 전부 감사한다
  - **`UNIQUE(source_id, number)` 를 두지 않았다.** 문제집·프린트는 절마다 번호가 1부터 다시 시작하고, PostgREST 일괄 INSERT 는 원자적이라 중복 하나에 OCR 결과 전체가 실패한다. 중복 정리는 클라이언트 병합이 맡는다
  - Storage 버킷 `exam-problem-bank`(비공개). **UPDATE 정책을 일부러 만들지 않았다** — 있으면 클라이언트가 `upsert:true` 로 되돌아가도 통과해 버리는데, 그 조합은 ara-system `exam-papers` 버킷을 1년 가까이 조용히 죽여 놨던 형태다. 바꿔 올릴 땐 지우고 새로 올린다(`replaceProblemFile`)
- [2026-09-10] **맥 지원 — 설치는 터미널 한 줄, 실행은 LaunchAgent**. 되돌리지 말아야 할 판단들:
  - **Safari 로는 못 쓴다.** WebKit 은 https 문서의 `ws://127.0.0.1` 을 mixed content 로 **동기 차단**한다(실측: 프로덕션 페이지에서 0ms onerror, 같은 문서의 `wss://` 는 차단 지점을 통과, 같은 주소가 http 문서에서는 열림). 127.0.0.1 을 예외로 두는 것은 **Chromium 뿐**이라, [codex/README.md](src/lib/ai/codex/README.md)·[localNetworkAccess.ts](src/lib/ai/localNetworkAccess.ts) 의 "mixed content 가 아니다" 주석은 Chrome 기준으로 읽어야 한다. 권한도 브릿지 실행도 이걸 풀지 못하므로 `hintKind` 가 **다른 어떤 판정보다 먼저** `browser_unsupported` 를 낸다 — 순서를 내리면 이미 브릿지를 켠 선생님에게 "브릿지를 켜세요" 가 나가 원인을 영영 못 찾는다
  - **선생님이 터미널에 붙여넣는 것은 한 줄뿐이다.** 그 스크립트가 codex 설치(`npm install -g --prefix ~/.ara-ai/npm` — 전역은 root 소유라 sudo 가 필요해서 홈에 깐다)와 `codex login` 까지 한다. ⚠️ 그래서 **맥에는 `codex login` 을 치라고 안내하면 안 된다** — 선생님 셸 PATH 에 `codex` 가 없어 command not found 다. 맥의 복구 경로는 언제나 **설치 명령 재실행**이다
  - **설치 스크립트(`install-mac.sh`)도 ara-system 이 호스팅한다.** 브릿지와 같은 이유다 — 선생님 PC 에 한 벌만 깔리고, 여기서 다시 배포하면 포트 8899 를 두고 프로세스가 다툰다. 이 앱은 [macInstaller.ts](src/lib/ai/macInstaller.ts) 로 **명령 문자열만** 만든다
  - **OS 자동 감지는 어느 탭을 먼저 펼칠지만 정한다** — 탭을 없애지 말 것. 선생님이 다른 선생님 컴퓨터에 깔아 주려고 이 화면을 여는 경우가 실제로 있다
  - 맥 안내에서 **'다시 내려받기'·'바탕화면 아이콘' 어휘를 쓰지 말 것**. 맥은 받는 파일이 없고 배경에서 도는지라 찾을 수 없는 물건을 찾게 만든다. [ConnectionHint.test.tsx](src/components/ai/ConnectionHint.test.tsx) 가 고정한다
- [2026-09-08] **AI 는 서버가 아니라 선생님 PC 에서 돈다**(ara-system 의 코덱스 브릿지를 이식). 학원 서버는 `AI_OCR_BETA` 플래그만 판정하고 `/api/ai/status` 로 알려 줄 뿐 AI 를 호출하지 않는다 — 그래서 토큰 비용이 0 이고 `auth.json` 이 선생님 PC 를 벗어나지 않는다. 대신 **컴퓨터마다 설치가 필요**하고 브릿지가 안 떠 있으면 못 쓴다. 자세한 프로토콜 함정은 [src/lib/ai/codex/README.md](src/lib/ai/codex/README.md)
  - **브릿지는 두 앱이 한 벌을 공유한다.** 원본은 ara-system `public/ara-ai/bridge.cjs` 이고 거기 `ALLOWED_ORIGINS` 에 이 앱 주소가 들어 있어야 한다(v2 부터). 여기서 파일을 다시 호스팅하면 포트 8899 를 두고 프로세스 둘이 다툰다
- [2026-09-12] **윈도우 설치는 파일 하나(`ara-ai.cmd`)이고 브릿지는 켤 때마다 스스로 갱신된다**(ara-system `bridge.cjs --update`, v4). 되돌리지 말아야 할 판단들:
  - **이 앱은 링크·명령 문자열만 만든다**([winInstaller.ts](src/lib/ai/winInstaller.ts) — `macInstaller.ts` 의 거울). 파일 본체는 ara-system 이 호스팅한다
  - ⚠️ **옛 3파일 설치에 "알아서 최신이 된다" 고 말하면 안 된다.** 그분들의 바로가기는 `start-codex.cmd` 를 가리켜 **갱신을 하지 않는다** — 낡은 브릿지를 영원히 다시 켜게 되고, 그게 2026-09-11 장애가 길어진 것과 **정확히 같은 잘못**(틀린 복구 경로 안내)이다. '한 번만 새로 받으세요' 를 **먼저** 말하고 자동 갱신은 그 뒤라고 밝힌다. [ConnectionHint.test.tsx](src/components/ai/ConnectionHint.test.tsx) 가 **순서까지** 고정한다
  - **`BRIDGE_MIN_VERSION` 은 브릿지가 새 주소를 배운 버전과 맞춘다.** 브라우저는 버전을 알아낼 방법이 없어(핸드셰이크 뒤는 raw TCP) 안내문이 이 숫자에 기댄다 — 안 올리면 "v2 면 됩니다" 라고 거짓말을 한다
  - **포트 변경 안내는 깔린 자리(`%LOCALAPPDATA%\ara-ai\ara-ai.cmd`)를 가리킨다.** 받은 폴더(다운로드)는 선생님이 치우는 자리라 몇 주 뒤에 깨진다
- [2026-03-09] 포인트 컬러 `#81D8D0`을 CSS 변수 `--primary`로 통합 → Tailwind `text-primary`, `bg-primary` 등으로 일관되게 사용
- [2026-03-09] 카테고리 포맷팅/그룹화 로직을 `lib/format.ts`로 추출 → words, exam/create 등 여러 페이지에서 중복 제거
- [2026-03-09] words/new 페이지를 CategoryForm + WordEntryTable로 분리 → 300줄 제한 준수
- [2026-03-09] App Router route 그룹 `(auth)`, `(main)`으로 인증 필요/불필요 영역 분리
- [2026-03-09] 네이버 웍스(LINE WORKS) OAuth 로그인으로 전환 → email/password 인증 제거, `@araeducation.co.kr` 도메인만 허용, Supabase admin API + magic link로 세션 수립

- [2026-03-10] 카테고리 마스터 테이블 도입 (publishers, major_chapters, sub_chapters, schools, school_materials) → 모든 사용자 공유, RLS는 authenticated만 체크. 기존 categories 테이블은 단어 그룹 매핑용으로 유지 (텍스트 값 저장)
- [2026-03-10] 외부지문 트리 구조: 학교명 > 프린트/작품명 (2레벨), 중등/고등: 출판사 > 학기 > 대단원 > 소단원 (4레벨)
- [2026-03-10] CategoryTree 컴포넌트를 단어 관리/시험지 생성에서 공유 → 단일 선택(onSelect) / 다중 선택(onToggle+multiSelect) 모드 지원

- [2026-04-16] concept_sheets Stored XSS 대응으로 `isomorphic-dompurify` 기반 화이트리스트 sanitize 를 다층 방어 (저장 경로 + exam-transform 세 함수 entry) 로 적용. CSP 는 [next.config.ts](next.config.ts) 의 정적 `headers()` 로 부여 — prod 에서도 Next App Router 부트스트랩 inline script 때문에 `script-src 'unsafe-inline'` 은 유지하되 `object-src 'none'`, `frame-ancestors 'none'`, `connect-src` 화이트리스트(`*.supabase.co`, NaverWorks) 로 유출 경로를 차단. DOMPurify 가 1차 방어, CSP 가 safety net 역할. RLS 는 학원 협업 모델을 위해 `authenticated` 공유를 유지. 향후 개선 후보: (1) `editor_html` → JSONB (TipTap `getJSON`), (2) nonce 기반 CSP, (3) concept_sheets 쓰기를 서버 라우트로 이전해 감사/rate limit 적용

- [2026-06-16] 카테고리 단일 출처화(방향 A). 단어지(`words`/`exams`)는 `categories` 를 **id 로 참조**해 마스터 이름 변경이 자동 반영되지만, 개념지(`concept_sheets`)는 같은 `categories` 트리에서 고르되 텍스트(`publisher`/`unit`/`subunit`)를 **복사 저장**해 rename 이 반영되지 않는 비대칭이 있었다. 완전 정규화(concept_sheets→categories FK) 대신, 위험이 낮은 **rename 트리거 동기화 확장**을 선택 — `sync_publisher_name`/`sync_major_chapter_name`/`sync_sub_chapter_name` 가 categories 와 함께 concept_sheets 도 갱신한다([sql/07_migration_sync_concept_sheets.sql](sql/07_migration_sync_concept_sheets.sql)). 향후 무결성이 더 필요하면 FK 정규화(B)로 승격 가능

- [2026-06-16] SQL 파일을 **적용 순서대로 번호(01~12) 접두사**를 붙여 정리했다(`sql/01_schema.sql` … `sql/12_fix_audit_log_insert_policy.sql`). 번호가 곧 신규 부트스트랩 적용 순서이며, 의존성 기반으로 결정됨(테이블 생성 → 감사/공유 → 도메인 RLS → categories 유니크 → exam_words 잠금 → 감사 함수 패치). 과거/대체된 마이그레이션(`00_apply_2026-05-26_security`, `migration_retake*`, `migration_enforce_user_id`, `migration_exam_rpc`)은 `sql/archive/` 로 이동해 이력만 보존(정의가 포인터 주석으로 무력화돼 신규 적용 대상 아님). 새 마이그레이션을 추가할 땐 다음 번호를 붙이고, 다른 SQL 파일을 참조하는 주석/문서 링크는 번호 포함 경로로 쓸 것
- [2026-06-16] **(위 항목 정정) `migration_enforce_user_id` 는 archive 에 두면 안 되는 *실행 대상* 트리거였다** — user_id(NOT NULL) 를 채우는 유일 경로라 archive 에만 있으면 신규 환경 생성이 전부 깨진다. 그래서 `sql/13_migration_enforce_user_id.sql` 로 승격했고 **신규 부트스트랩 범위는 01~13** 이다(`sql/archive/migration_enforce_user_id.sql` 은 SUPERSEDED 헤더로 무력화). 13 은 `CREATE OR REPLACE` + `DROP TRIGGER IF EXISTS` 라 멱등 — 기존 운영 DB(이미 트리거 보유)에 재적용해도 안전. archive 의 나머지(`00_apply...`, `migration_retake*`, `migration_exam_rpc`)는 여전히 포인터 무력화된 신규 적용 비대상

- [2026-08-15] **인쇄를 '측정 기반 명시적 페이지네이션'으로 전환** — 모든 인쇄 문서(시험지·답안지·객관식·단어장·개념지 5종)가 브라우저 자동 분할을 버리고 **JS 가 블록 높이를 실측해 고정 크기 A4 낱장 N 장에 배정**하는 방식으로 바뀌었다([src/lib/print/](src/lib/print/), [src/hooks/useA4Pagination.ts](src/hooks/useA4Pagination.ts), [src/components/print/](src/components/print/)). A4 를 96dpi px 로 못 박아(210mm=793.7px, 297mm=1122.52px) `@page { margin: 0 }` + 낱장 `break-after: page` 로 인쇄하므로 **화면 미리보기와 인쇄물이 1:1** 이다. 낱장 높이가 고정이고 본문이 `flex:1` 이라 푸터는 마지막 페이지에서도 바닥에 붙는다(페이지 번호 `n / N` 추가). 1페이지는 전체 헤더, 2페이지부터는 `CompactPageHeader` 가 반복된다. ⚠️ **되돌리지 말 것**: (1) `table + thead/tfoot` 자동 반복 — tfoot 은 '페이지 하단'이 아니라 '내용이 끝나는 자리'에 붙어 마지막 페이지 푸터가 중간에 뜬다(5회 이상 재발한 원인), (2) `position: fixed` 푸터 — 과거 시도 후 revert(d52c6e6→eae221b), (3) 인쇄 시 CSS multicol(`columns: 2`) — `column-fill: balance` 가 페이지 경계에서 콘텐츠를 유실시킨다. 2단은 엔진이 컬럼별로 블록을 배정해 해결한다. 같은 저자의 검증된 선례는 ara-system `app/r/omr/[token]/_sections/printCss.ts`(고정 px A4 면 + 시트별 page-break)
- [2026-08-15] **개념지 카테고리 출처를 단어지와 통일**했다. 이전엔 세 화면이 서로 다른 소스를 썼다 — 단어 관리/시험지 생성은 `categories` 테이블, 개념지 **편집기**는 마스터 3테이블 조합(`getAllSchoolLevelCategories`), 개념지 **목록**은 저장된 `concept_sheets` 텍스트 합성. 편집기가 `schools`/`school_materials` 를 아예 조회하지 않아 **외부지문 노드가 트리에 나오지 않았고**, 소단원이 있는 대단원은 "대단원 단독" 행을 만들지 않아 단어 등록에서는 되는 선택(소단원은 선택 사항)이 개념지에서는 불가능했다. `getAllSelectableCategories()` ([src/lib/category-master/aggregate.ts](src/lib/category-master/aggregate.ts)) 가 **마스터 중등/고등 전개 ∪ 마스터 외부지문 전개 ∪ `categories` 테이블** 을 자연키로 dedupe 해 단일 출처가 된다(중복 시 `categories` 행 우선 — 실제 UUID 를 가진 쪽). "단어가 없는 빈 카테고리도 선택 가능"이라는 기존 계약은 마스터 전개가 계속 포함되므로 유지된다. 개념지 목록 트리는 저장 텍스트 합성을 그대로 두되 key 에 `year`/`school_name` 을 포함시켰다(빼면 학교·년도가 다른 개념지가 한 노드로 뭉친다). 조회 실패도 더 이상 삼키지 않는다 — 예전엔 RLS/네트워크 실패가 "카테고리가 없습니다" 한 줄로만 보였다

- [2026-08-15] **외부지문 및 프린트에 년도·학년 도입**([sql/15_migration_external_year_grade.sql](sql/15_migration_external_year_grade.sql)). 계층이 `학교 > 프린트` → **`학교 > 년도 > 학년 > 프린트`** 가 됐다. 년도/학년은 `school_materials` 에 붙고(`UNIQUE(name, school_id, year, grade)` — 같은 이름 프린트를 해마다 따로 둘 수 있다), `categories`/`concept_sheets` 에도 복제된다. 타입은 INTEGER 가 아니라 **TEXT** 이고 **빈 문자열이 "미지정"** 이다 — 기존 컬럼(`grade`/`semester`)과 표기를 맞추고 자연키 유니크 인덱스의 `NULLS NOT DISTINCT` 의미를 흔들지 않기 위해서다. UI 표시값 `'미지정'` ↔ 저장값 `''` 변환은 [src/lib/external-category.ts](src/lib/external-category.ts) 의 `toStoredValue`/`toOptionValue` **한 곳에서만** 한다. 년도 Select 옵션은 롤링 윈도(내년~4년 전) **∪ 데이터에 실제로 존재하는 년도** — 윈도만 쓰면 오래된 년도의 프린트가 목록에서 영영 안 보인다. 부수 효과로 `ensureCategoryId` 가 외부지문 `grade` 를 `''` 로 강제하던 것이 사라져, `levelGradeToDivision` 이 제대로 판정되고 ara-system 이 외부지문을 전부 중등부로 등록하던 문제가 해소된다

- [2026-08-15] **ara-system public 스키마 읽기 전용 연동 도입** — 시험지 생성 페이지에 "내신 시험범위 불러오기" 카드(`src/components/exam/NaesinScopeLoader.tsx`)를 추가. ara-system 수업 > 내신 관리가 저장한 `public.school_exam_scopes`(+ `public.schools`, `public.curriculum_textbooks`)를 `supabase.schema('public')` 체이닝(`src/lib/supabase-public.ts` `publicDb()`)으로 직접 읽어, 학교/학년/학년도/시험 선택 → 저장된 단원 키를 `src/lib/naesin-scope/match.ts`(순수 매처)로 `exam.categories`에 텍스트 매칭해 자동 체크한다. **public 스키마는 읽기 전용 — 이 클라이언트로 쓰기 금지**(원본 관리는 ara-system). 매칭은 제목 문자열 기반이라(대단원/"대단원 > 소단원" 키, 공백 정규화) 미매칭 단원은 반드시 UI에 표면화한다(조용한 유실 금지, unmatchedUnits).

- [2026-08-30] **ara-system 이 `school_exam_scopes.scope` 의 의미를 바꿨다**(그쪽 mig458) — 예전엔 체크한 단원으로 자동 조립된 '시험범위 전체 문구'였지만, 이제 **프린트·부교재·외부지문만** 적는 칸이다(교과서 범위의 진실원은 `units`). 자동 선택은 원래부터 `units` 로만 하므로 매칭 동작은 그대로고, 화면 라벨만 '범위' → '프린트·외부지문' 으로 바꿨다(`NaesinScopeLoader.tsx`). 이 값을 다시 '전체 범위'로 취급하지 말 것 — 단원 문구는 여기 오지 않는다.

- [2026-09-04] **(아래 2026-09-03 항목을 뒤집음) 개념지 단 수는 다시 글자 수만 본다.** 열 폭 맞춤(0.1.14)이 넓은 표를 2단 칸에 맞춰 주므로 1단 강제의 근거가 사라졌고, 사용자가 페이지 수를 우선했다("2단이어야 A4 용지가 줄어"). **예외: 래퍼(`blockquote`/`li`) 안의 표는 맞춤도 분할도 못 하므로**(`rootTable` 가드) 그런 넓은 표(3열 이상)가 있을 때만 1단으로 되돌린다(`maxWrappedTableColumns` — 루트 표는 세지 않는다). 코덱스가 잡은 회귀다. 열이 아주 많은 루트 표(7열 이상)가 2단 칸에서 읽기 어려우면 같은 함수에 루트 표 열 수 조건을 다시 넣으면 된다
- [2026-09-03] **(2026-09-04 에 뒤집음) 개념지 단 수를 '글자 수'에서 '글자 수 + 표 열 수'로 바꿨다**([src/lib/print/sheet-columns.ts](src/lib/print/sheet-columns.ts)). 300자 초과면 무조건 2단이었는데, 표가 있는 개념지는 거의 항상 300자를 넘어 **열 3~5개짜리 표가 폭 ≈328px 칸에 밀려 들어가** 잘렸다. 이제 `maxTableColumns` 가 3 이상이면 시트 전체를 1단(≈680px)으로 되돌린다. **혼합 폭(본문 2단 + 표만 전체 폭)은 일부러 안 했다** — 5종 문서가 공용하는 인쇄 엔진(`paginate`/`A4Document`)에 블록별 폭 개념을 새로 넣어야 해서 범위·위험 대비 이득이 작다. 단 수는 저장하지 않는 파생값이라 표를 지우면 자동으로 2단으로 돌아온다

## Gotchas
- [2026-09-19] **작품 컬럼은 둘씩 있고 하나는 파생이다.** `passages.works`/`problems.work_titles` 가 원본이고 `passages.title`·`author`·`problems.work_title` 은 **DB 트리거가 채운다**(sql/33). 앱에서 파생 컬럼을 보내면 목록과 문자열 가운데 어느 쪽이 참인지 알 수 없어진다 — `ProblemPatch`·`PassagePatch` 에 아예 없다. 읽기는 계속 파생 문자열로 해도 된다(인쇄 스냅샷·검색 평문·지문 고르기가 그렇게 쓴다). 조회 조건만은 **반드시 배열 포함**(`contains('work_titles', [제목])`)이다 — `.eq('work_title')` 로 돌리면 `(가)(나)` 지문 문항이 한 편으로는 하나도 안 나온다
- [2026-09-13] **개념지에도 합격 기준이 생겼다(sql/25) — 그리고 재시험은 ara-system 에서 더 이상 회차를 만들지 않는다(그쪽 mig477).** 되돌리지 말아야 할 것들:
  - **합격 개수 계산식(CEIL)은 네 곳이 1:1 미러다.** ① `exam.create_exam_with_words` RPC ② [pass-count.ts](src/lib/pass-count.ts) `passCountOf` ③ ara-system migration 477(`register_concept_exam` 의 `v_pass`) ④ ara-system `scripts/backfill-araexam-grades.js`. 하나만 바꾸면 **인쇄물의 '합격 N개 이상' 과 성적의 합격/불합격 판정이 어긋난다** — 선생님은 시험지를 믿고 학부모는 성적표를 믿는다. 올림인 이유: 80% 17문항이면 13.6 → 14개라야 기준을 지킨다(내림이면 76%도 합격). ⚠️ **JS 쪽은 반드시 `(pct × total) / 100` 순서로 쓴다** — `(pct/100) × total` 은 2진 부동소수라 55%×100문항이 `55.00000000000001` → **56** 이 된다. Postgres 는 numeric(10진)이라 55 를 주므로, 딱 떨어지는 기준에서만 하나씩 어긋나는 가장 찾기 어려운 불일치가 생긴다(코덱스 리뷰 1R, `pass-count.test.ts` 가 고정)
  - **`concept_sheets.pass_percentage` 는 NOT NULL DEFAULT 80** 이라 기존 121장도 전부 80이 채워졌다(원장이 정한 값). 개념지별로 다르게 두려면 편집기 위쪽 '합격 기준 (%)' 칸에서 바꾼다
  - ⚠️ **`handleSave` 의 deps 에 `passPercentage` 를 반드시 넣을 것**([useConceptSheetEditor.ts](src/hooks/useConceptSheetEditor.ts)). 빠뜨리면 방금 바꾼 기준이 아니라 **마운트 시점 값으로 저장**된다 — 화면엔 70%인데 저장은 80%가 되는 조용한 어긋남이라 아무도 못 찾는다
  - **합격 기준 줄은 채점하는 시트(1~3단계)에만 찍는다**([ExamSheetRenderer.tsx](src/components/exam-builder/ExamSheetRenderer.tsx) 의 `config.showScore`). 개념지·답안지는 시험이 아니다(`ExamPrintHeader` 가 기출 문제지에 합격선을 안 그리는 것과 같은 근거)
  - ⚠️ **재시험 동기화가 409 를 돌려줄 수 있다.** ara-system 은 재시험을 **원본 회차에 딸린 시험지**(`exam_retake_papers`)로 붙이므로 원본이 먼저 등록돼 있어야 한다. 원본 미등록이면 `409 parent_not_registered` 가 오고 [grade-sync-client.ts](src/lib/grade-sync-client.ts) 가 '원본 시험지가 학원 성적에 아직 등록되지 않았어요' 로 알린다. **예전처럼 별도 회차로 폴백 등록하는 동작을 되살리지 말 것** — 그렇게 만든 회차는 학생별 차수(`exam_results.attempt_no`)와 이어지지 않아 재시험 점수가 원본과 따로 논다. 밀린 건은 ara-system 백필 스크립트가 원본을 먼저 등록한 뒤 붙인다
  - **`ConceptSheetListItem` 에서 `pass_percentage` 를 뺀 것은 의도**다 — 목록·트리는 합격 기준을 안 쓰고, `LIST_COLUMNS`(조회 컬럼)와 타입을 맞춰 둬야 "타입엔 있는데 조회엔 없는" 조용한 undefined 가 안 생긴다
- [2026-09-13] **ara-system 성적 자동등록이 두 달간 0건이었다 — 원인은 코드가 아니라 Vercel env 였고, 실패가 전 구간에서 조용했다.** 제보("개념/단어 시험이 자동등록 안 된다") 당시 실측: `exam.exams` 139건 **전부 미등록**, 개념지는 mig257 일괄분(2026-07-20) 이후 62건 미등록. 2026-07-19 Supabase 통합 때 Vercel env 를 교체하면서 `ARA_SYSTEM_INTAKE_URL`·`ARA_SYSTEM_INTAKE_SECRET` 이 빠진 것이 원인이다(운영 라우트에 인증 없이 POST 하면 `not_configured` 가 나온다 — 이게 진단의 결정타였다)
  - **침묵이 세 겹이었다.** ① 발신 라우트의 `not_configured` 가 **HTTP 200** 이라 상태 코드로도 안 보였고, ② 클라이언트 세 곳이 `.catch(() => {})` 로 응답을 통째로 버렸으며, ③ 양쪽 라우트에 로그가 한 줄도 없었다. 지금은 ① 503, ② [grade-sync-client.ts](src/lib/grade-sync-client.ts) 가 응답을 읽어 **경고 토스트**, ③ `console.error` 로 셋 다 막았다 — **되돌리지 말 것**
  - ⚠️ **`ARA_SYSTEM_INTAKE_URL` 은 반드시 `https://www.araeducation.co.kr`.** apex·`admin.` 호스트는 ara-system next.config.ts 가 `/api/*` 까지 www 로 307 리다이렉트하는데, fetch 는 **크로스오리진 리다이렉트에서 Authorization 헤더를 버린다** → 영구 401 인데 증상은 똑같이 '조용히 등록 안 됨'이다
  - **시크릿은 양쪽이 같아야 한다** — ara-exam `ARA_SYSTEM_INTAKE_SECRET` == ara-system `VOCAB_INTAKE_SECRET`. 수신부는 `timingSafeEqual` 이라 끝 공백·줄바꿈 하나에도 401 이다
  - ⚠️ **실제로는 양쪽 다 비어 있었다.** 이 앱에 두 변수가 없었을 뿐 아니라 **ara-system Vercel 에도 `VOCAB_INTAKE_SECRET` 이 등록된 적이 없었다** — 이쪽만 고쳤어도 수신부가 401 로 전부 거절했을 상황이었다. 한쪽만 보고 "고쳤다" 고 하지 말 것
  - **복구 완료(2026-09-13)**: 새 공유 시크릿(hex 64자)을 두 프로젝트 production·preview 에 등록하고 `ARA_SYSTEM_INTAKE_URL=https://www.araeducation.co.kr` 을 넣은 뒤 **양쪽을 재배포**했다(env 는 재배포해야 함수에 실린다). 검증은 위 curl 한 줄 + 운영 인테이크에 유효 시크릿을 실어 빈 본문 POST(400 이면 인증 통과)
  - 확인 한 줄: `curl -sS -X POST -H 'content-type: application/json' -d '{}' https://test.araeducation.co.kr/api/sync-to-grades` → `not_configured` 면 env 미설정, `unauthorized` 면 정상(로그인 토큰이 없어서 거절된 것)
  - 밀린 등록은 **ara-system `scripts/backfill-araexam-grades.js`** 가 메운다(공유 DB 에서 미등록 건을 찾아 운영 인테이크 라우트로 재전송, 멱등). payload 조립이 이 앱의 두 발신 라우트와 1:1 복제라 **계약을 바꾸면 그 스크립트도 같이** 고쳐야 한다
- [2026-09-08] **`isomorphic-dompurify` 는 인스턴스가 하나뿐이라 `addHook` 이 전역이다.** 문항용 정화를 별도 모듈에서 훅으로 추가하면 개념지 정화에도 그 훅이 발화한다. 그래서 훅은 [sanitize-profile.ts](src/lib/sanitize-profile.ts) 에 **한 번만** 걸고 문서 종류별 차이는 활성 프로필로 바꾼다(`withProfile`). 프로필은 `finally` 로 반드시 되돌린다 — 예외가 나도 다음 호출이 남의 규칙을 물려받으면 안 된다. 새 정화 프로필을 추가할 땐 여기에 얹을 것
- [2026-09-08] **OCR 묶음은 3쪽·겹침 1이다**([problem-ocr/constants.ts](src/lib/problem-ocr/constants.ts)). 정답표 읽기의 5쪽보다 잘게 자른 이유는 국어 지문 전사가 **출력 토큰**을 훨씬 많이 쓰기 때문이고(지연을 지배하는 건 이미지 장수가 아니다), 겹치는 이유는 **쪽 경계를 넘는 지문**이다 — 겹치지 않으면 어느 묶음도 그 지문을 통째로 못 봐서 반씩 잘린 지문 두 개가 남는다. 겹침을 0으로 되돌리지 말 것
- [2026-09-08] **병합 규칙이 지문과 문항에서 다르다**([merge.ts](src/lib/problem-ocr/merge.ts)). 지문은 **더 완전한(글이 긴) 쪽이 이기고**, 문항은 **먼저 온 쪽이 이기되 빈 칸만 나중 것이 채운다**. 지문에 first-wins 를 쓰면 겹침의 목적(잘린 지문을 온전히 본 묶음의 결과)이 사라진다. ⚠️ **정답(answer)과 유형(question_type)은 한 덩어리로** 옮긴다 — 유형이 바뀌면 `'1'` 이 선지 번호인지 답안 문자열인지가 달라진다
- [2026-09-08] **좌표는 x/y/w/h 로 받지 않는다.** 시각 모델의 가로 좌표는 부정확하고 국어 시험지는 거의 2단 조판이라, `{column, top, bottom}`(단 번호 + 세로 비율)만 받아 단 폭 전체를 여유 있게 잘라낸다([crop.ts](src/lib/problem-ocr/crop.ts)). 네 숫자로 되돌리면 크롭이 글자를 자른다
- [2026-09-08] **문제지의 지문 묶음 연속성은 앱과 DB 가 따로 검사한다.** 같은 지문의 문항이 흩어지면 인쇄에서 지문이 여러 번 나오고 머리글 범위가 거짓말을 한다. 앱만 검사하면 RPC 직접 호출로 뚫리고, DB 만 검사하면 사람이 어디를 고쳐야 할지 모른다. `isContiguous`([compose.ts](src/lib/problem-paper/compose.ts))와 RPC 의 gaps-and-islands 검사를 **같이** 유지할 것
- [2026-09-08] **문제지 인쇄에서 지문은 문단 단위 블록으로 쪼갠다**([problem-paper/blocks.ts](src/lib/problem-paper/blocks.ts)). 인쇄 엔진의 블록은 쪼갤 수 없는 최소 단위라, 지문을 통째로 넣으면 한 쪽을 넘는 순간 `transform: scale()` 로 깨알같이 줄어든다. 상자 윤곽은 조각마다 좌우를 그리고 위·아래는 첫·마지막 조각만 그려서(`pb-passage-part--first/--last`) 단·쪽이 갈려도 하나로 보이게 한다
- [2026-09-08] **`pdfjs-dist` 는 `wasmUrl: '/pdfjs-wasm/'` 없이는 스캔본을 백지로 렌더한다**(JBIG2 디코딩). 자산은 `predev`/`prebuild` 훅([scripts/copy-pdfjs-wasm.js](scripts/copy-pdfjs-wasm.js))이 `node_modules` 에서 복사하고 git 에는 넣지 않는다. 끝 슬래시 필수
  - ⚠️ **파일만 있어서는 안 되고 CSP 도 열려 있어야 한다.** 프로덕션 `script-src` 에 `'wasm-unsafe-eval'` 이 없으면 워커의 `WebAssembly.instantiate` 가 막혀 **프로덕션에서만** 스캔본이 백지가 된다(개발 모드의 `'unsafe-eval'` 은 wasm 도 허용해서 드러나지 않는다). 이 권한은 wasm 컴파일만 열고 JS eval 은 열지 않는다 — [next.config.ts](next.config.ts)
- [2026-09-08] **'이미지로 출제' 에는 자동으로 못 푸는 한계가 둘 있다.** 자동으로 고를 수 없으니 **경고로 드러내고 사람이 정한다** — 조용히 한쪽을 고르면 인쇄물에서만 내용이 사라진다
  - 잘라 둔 이미지에는 **원본 시험지의 문항 번호가 그대로 찍혀 있다**(그래야 선지까지 안 잘린다). 문제지에서 자리가 바뀌면 이미지의 '17' 과 우리가 찍는 '01' 이 함께 보인다. `renumberedImageItems` 가 그런 문항을 찾아 문제지 화면에서 알린다
    - ⚠️ **[2026-09-15] 화면 경고만으로는 모자랐다**(코덱스 리뷰). 그 경고는 `data-no-print` 라 **인쇄물에 안 나가고**, 아래로 스크롤해 인쇄 버튼만 누르면 두 번호가 함께 찍힌 시험지가 그대로 나간다 — 학생은 '01' 이라고 적힌 문항 안에서 '17' 을 보고 답안지의 1번과 맞추지 못한다. 그래서 인쇄 버튼이 `renumberedPrintConfirmMessage` 로 **한 번 더 묻는다**([blocks.ts](src/lib/problem-paper/blocks.ts)). **번호를 자동으로 지우는 안은 여전히 없다** — 잘라 둔 이미지의 화소이고 번호 자리를 알 방법이 없다. 인쇄를 아예 막는 안은 버렸다(자리를 원래 번호에 맞추거나 글 문항으로 바꾸는 것이 정답인데, 급할 때 그대로 뽑아 손으로 고치는 길까지 막힌다 — 사용자가 고른 안)
  - **여러 쪽에 걸친 그림 지문은 어느 쪽으로도 온전하지 않다** — 글만 쓰면 그림이 빠지고, 이미지로 쓰면 잘라 둔 시작 쪽만 나가 뒷부분이 사라진다. 그래서 자동 제안은 한 쪽짜리(`pageSpan === 1`)에만 하고 나머지는 검수 경고로 남긴다
- [2026-09-08] **CSP `connect-src` 에 `ws://127.0.0.1:*` 이 있어야 브릿지에 붙는다**([next.config.ts](next.config.ts)). ara-system 은 CSP 가 아예 없어 겪지 않은 문제다. 127.0.0.1 은 potentially trustworthy 라 https 문서에서도 mixed content 가 아니고 `upgrade-insecure-requests` 도 loopback 은 면제하지만, **프로덕션 배포 후 DevTools 로 한 번 확인할 것** — 만약 wss 로 올라가면 핸드셰이크가 조용히 실패한다
- [2026-09-08] **`react-hooks/set-state-in-effect` 가 이 저장소에서는 에러다.** 효과 안에서 동기 setState 를 하면 lint 가 막는다. 쓰던 패턴의 대안: ① 로딩 플래그 → "무엇을 이미 불러왔는가"(`loadedKey`)를 기억하고 로딩을 파생, ② prop 이 바뀌면 폼 초기화 → 호출부에서 `key` 로 다시 마운트, ③ 유효하지 않은 선택값 되돌리기 → 렌더 단계에서 파생, ④ 비동기 조회 → `.then()` 안에서만 setState
- [2026-09-04] **표 열 폭은 인쇄 직전에 JS 가 정한다 — CSS 만으로는 표현할 수 없다.** `overflow-wrap: anywhere`(아래 2026-09-03 항목) 는 셀 min-content 를 한 글자로 무너뜨려, 자동 레이아웃이 긴 글 열에 폭을 몰아주고 2~3글자 열을 한 글자 폭으로 누른다. 그래서 [table-col-fit.ts](src/lib/print/table-col-fit.ts) 가 열 폭을 나눠 퍼센트 `<colgroup>` + `table-layout: fixed` 로 박는다. **되돌리면 안 되는 것들**
  - **프로브는 `width: max-content` 래퍼 + 표 `width: auto` 둘 다 필요하다** ([table-measure.ts](src/lib/print/table-measure.ts) 의 `measureMaxContentWidths`). 래퍼가 없으면 절대배치 shrink-to-fit 이 컨테이너 폭에 걸려 **접힌 폭**을 재고, 표의 `width` 를 안 되돌리면 이미 박힌 `fixed` + 100% 를 그대로 읽는다. 측정 컨테이너(`.a4-measure`)는 `visibility: hidden` 이라 레이아웃이 살아 있어 여기서 재는 게 맞다 — 시트와 같은 타이포그래피 스코프(`.eb-sheet-table .sheet-body`)여야 폭이 맞으므로 프로브도 `SHEET_BODY_CLASS` 를 쓴다
  - **실측 폭에 여유(`COLUMN_SLACK_PX` 2px)를 반드시 얹는다.** max-content 에 딱 맞게 주면 퍼센트 환산·`border-collapse` 겹침에서 1픽셀 미만이 깎여 **마지막 글자가 접힌다**. WebKit 렌더로 확인한 실제 회귀다(`구분` → `구/분`)
  - **긴 열 하한은 `min(96px, 칸 폭 × 1/5)`** (`longColumnFloor`) 이고, 짧은 열 보호가 그 하한을 못 지키면 **가장 넓은 보호 열부터 푼다**. 안 그러면 짧은 열이 폭을 다 먹고 긴 열이 실처럼 좁아진다. **하한을 절대값 96 으로 두지 말 것** — 2단 칸(≈328px)에서 긴 열이 둘만 돼도 실현이 안 되어 보호를 전부 풀고 비례로 떨어져 짧은 열이 다시 한 글자로 눌린다(`45/45/45/300/400` → `18/18/18/…`). **균등 몫(칸 폭/열 수)까지 올리지도 말 것** — 보호가 실패한 표에서 하한이 폭을 다 먹어 긴 열이 짧은 열과 같은 폭이 된다(2단계 박스 4열 `46/46/62/174` → `82×4`). 둘 다 WebKit 렌더 비교로 잡은 회귀다
  - **폰트 로딩 전에 맞췄으면 폰트가 온 뒤 한 번 더 맞춘다**(`useConceptSheetBlocks` 의 `fitRef`). 폴백 폰트가 좁으면 보호한 열이 실제 폰트에서 접힌다. 같은 본문을 폰트 적용 후 맞췄으면 다시 하지 않는다 — 이것이 재맞춤 루프의 종료 조건이다
  - 맞춤은 **분할 전 원본 블록에서 다시 시작**한다. 폭이 바뀌면 행 높이가 달라져 옛 조각은 못 쓴다
- [2026-09-04] **`paginate` 는 '남은 자리에 안 들어감'과 '한 장에도 안 들어감'을 다르게 다룬다.** 전자는 `splitRequests`(쪼개서 남은 자리를 채우라), 후자는 `oversized`(축소해서 담으라)다. 한 블록이 둘 다일 수 있다. 요청을 남긴 뒤에도 **배치는 예전 로직 그대로 계속**해야 한다 — 호출부가 안 쪼개도 레이아웃은 유효해야 하기 때문이다. 훅은 **첫 요청 하나만** 반영한다(앞 표를 쪼개면 뒤 표의 남은 자리가 달라진다). 조각을 붙일 때 `tableRowCount` 로 측정과 상태가 같은 블록인지 확인한다 — 어긋난 요청을 그대로 쓰면 엉뚱한 표가 쪼개진다
- [2026-09-03] **개념지 표는 '가로는 CSS, 세로는 분할기'가 각각 책임진다 — 한쪽만 고치면 증상이 남는다.** 가로: 자동 표 레이아웃에서 표 폭의 하한은 셀 min-content 합이라 `width:100%` 로는 못 줄인다. 셀에 **`overflow-wrap: anywhere`**(`break-word` 는 min-content 계산에 반영되지 않아 효과 없다)를 주고, 1·2단계 박스 묶음 래퍼([src/lib/exam-transform.ts](src/lib/exam-transform.ts) 의 `BLANK_RUN_CLASS`)의 `white-space: nowrap` 을 **표 셀 안에서만** `normal` 로 푼다(본문 `<p>` 에서는 한 단어의 박스가 갈리면 안 되므로 nowrap 유지). 인접 inline-block 박스 사이 줄바꿈은 nowrap 만 풀면 브라우저가 알아서 준다 — `<wbr>`/ZWSP 를 넣지 말 것(`textContent` 가 오염돼 제목 행 판정이 틀어진다). 세로: 넘치면 `.a4-sheet__body { overflow: hidden }` 이 **조용히 잘라낸다** — 인쇄물에서만 보이는 증상이라 화면만 보고 판단하지 말 것
- [2026-09-03] **`splitTableByRows` 의 `hasRowSpan` 전체 포기(bail-out)를 되살리지 말 것.** 예전엔 rowspan 셀이 하나라도 있으면 표를 안 쪼개고 `transform: scale` 로 축소해 깨알 표가 됐다(편집기에 '셀 병합'이 있어 흔한 표다). 지금은 [src/lib/print/table-row-plan.ts](src/lib/print/table-row-plan.ts) 의 `legalCutFlags` 가 **'이 행 앞에서 잘라도 되는가'** 를 계산해 병합 묶음 경계에서만 자른다 — 비합법 지점에서 끊으면 다음 조각에 병합 셀이 없어 **나머지 셀이 앞 열로 밀린다**. 묶음 하나가 용량보다 크면 그 조각만 용량을 넘겨 내보내고 엔진이 축소한다
- [2026-09-03] **조각 표의 제목 행 판정은 규칙이 세 겹이고, 코덱스 리뷰 4라운드로 다듬은 것이다 — 단순화하려다 되돌리지 말 것.** [split-html-blocks.ts](src/lib/print/split-html-blocks.ts) 의 `detectHeaderRows`.
  - **(1) 명시 제목 vs 추측**: `<th>` 전체거나 `<thead>` 안이면 작성자가 명시한 제목(`isExplicitHeaderRow`)이고, '비지 않은 셀이 전부 `<strong>`' 은 추측이다. 편집기가 `withHeaderRow: false` 로 표를 넣어 제목이 보통 `<td><strong>` 이라 추측이 꼭 필요하다. **다음 행으로 이어 가는 것은 명시 제목일 때만** — 이 규칙 하나가 (a) 굵은 데이터 행이 제목으로 승격되는 것과 (b) 제목 셀이 rowspan 으로 뻗은 첫 데이터 행이 제목 묶음에 끌려오는 것을 동시에 막는다. 이어가기를 무조건 허용하면 실측으로 `["구분뜻/가/나2", "구분뜻/가/다3", …]` 처럼 **데이터 행이 매 페이지 반복**된다
  - **(2) 합법 지점으로 후퇴**: 제목 묶음의 끝은 합법 절단점이어야 반복할 수 있다. 그래서 합법 지점으로 끝나는 마지막 후보만 `best` 로 확정하고, 더 가져가려다 실패하면 거기로 후퇴한다. `legalCut[count] ? count : 0` 처럼 **마지막 count 하나만 보면 앞서 확보한 합법 제목까지 통째로 버린다**(2행 th 제목의 둘째 행이 본문까지 뻗을 때 제목이 0 이 됐다)
  - **(3) 빈 텍스트 ≠ 빈 셀**: 2단계 박스는 빈 span, 3단계 밑줄은 `&nbsp;` 뿐이고 `stripSpaces` 의 `\s` 가 **NBSP 까지 지운다**. 그래서 개념어로만 이뤄진 제목 행이 '빈 행' 으로 오판된다. `countBlankMarks` 로 `.eb-blank-run`/`.eb-stage3-blank` 개수를 세고, `isStrongOnly` 는 텍스트와 표시 개수를 **따로** 비교한다(이어 붙여 비교하면 굵은 조각이 여럿일 때 텍스트-표시 순서가 어긋나 굵은 제목을 놓친다). 1단계는 초성 글자가 남아 무관하다
- [2026-09-03] **`legalCutFlags` 는 rowspan 을 행 그룹(`thead`/`tbody`)으로 클램프하지 않는다 — 일부러 그렇다.** HTML 규격상 rowspan 은 행 그룹 끝에서 끝나고 `rowspan="0"` 은 '그룹 끝까지' 지만, `buildChunk` 가 모든 행을 **단일 `<tbody>` 로 평탄화**하므로 절단 합법성만 그룹으로 좁히면 조각에서는 그 셀이 더 멀리 뻗어 **열이 밀린다**(합법성과 출력이 어긋난다). 지금은 뻗은 대로 인정하고 `rowspan="0"` 은 표 끝까지로 본다 — 보수적이라 최악이 '분할 못 해 축소 인쇄' 이고 절대 열이 밀리지 않는다. TipTap 은 `<thead>` 를 만들지 않고 단일 tbody 만 내보내 실제 콘텐츠에선 그룹이 하나뿐이라 클램프가 no-op 이기도 하다. 제대로 하려면 조각에 행 그룹을 보존하고 `rowspan="0"` 을 유한값으로 바꾸는 것까지 **같이** 해야 한다
- [2026-09-03] **`splitTableByRows` 는 쪼갤 수 없으면 입력 문자열을 `[tableHtml]` 그대로 돌려줘야 한다 — 그게 재분할 루프의 종료 조건이다.** [useConceptSheetBlocks](src/hooks/useConceptSheetBlocks.ts) 는 조각 수 > 1 일 때만 state 를 갱신하므로(`changed`), 억지로 조각 2개를 만들면 측정할 때마다 상태가 갱신돼 렌더 루프가 된다. `MAX_SPLIT_PASSES = 2` 는 그 위의 안전망일 뿐 진짜 가드가 아니다
- [2026-09-04] **조각 표의 열 폭은 원본의 `<colgroup>` 복제가 1순위다**(실측 셀 폭은 맞춤이 실패한 표의 대비책). 열 폭 맞춤이 원본에 박아 둔 colgroup 을 그대로 물려받아야 조각의 열 폭·행 높이가 원본과 같다. 순서를 뒤집으면 실측 폭이 이겨 조각마다 폭이 미세하게 달라진다. 아래 2026-09-03 항목의 "편집기 colwidth 는 인쇄에 반영되지 않는다"는 지금도 그대로다 — 여기서 말하는 colgroup 은 **인쇄 엔진이 스스로 만든 것**이다
- [2026-09-03] **조각 표는 각자 독립된 표라 auto 레이아웃이면 페이지마다 열 폭이 달라진다.** 그래서 측정 단계에서 '모든 셀이 colspan 1 이고 셀 수가 열 수와 같은 첫 행'의 셀 폭을 재([table-measure.ts](src/lib/print/table-measure.ts) 의 `measureColumnWidths`) 조각에 퍼센트 `<colgroup>` + `table-layout: fixed` 로 박는다. 기준 행이 없으면(모든 행에 병합 셀) colWidths 없이 auto 로 둔다. **이 `<colgroup>`/`<col>` 은 sanitize 화이트리스트에 없지만 문제없다** — 분할은 렌더 직전 단계라 `sanitizeConceptHTML` 을 다시 거치지 않는다. 반대로 편집기가 저장한 `colwidth` 는 `getHTML()` 이 colgroup 으로 직렬화하지 않고 sanitize 도 `col` 을 지우므로 **인쇄에 전혀 반영되지 않는다**(옛 colgroup 분기는 사실상 죽은 코드였다)
- [2026-08-30] **SQL 파일마다 대상 스키마가 다르다 — 실행 전에 어느 프로젝트에 붙었는지 확인할 것.** `sql/01~14` 는 `public` 을 대상으로 쓰였고(`SET search_path = public, pg_temp` 또는 무자격), **`sql/15`·`sql/16` 만 `exam`** 을 대상으로 한다. 리포에 `CREATE SCHEMA exam` 은 없다 — `exam` 스키마는 ara-system 의 마이그레이션 254(`exam-schema-from-araexam.sql`)가 만들었고, 앱은 [src/lib/supabase.ts](src/lib/supabase.ts) 의 `db: { schema: 'exam' }` 로 **그 공유 프로젝트**를 본다. 따라서 옛 standalone ara-exam 프로젝트(테이블이 `public` 에 있는 쪽)의 SQL Editor 에서 15/16 을 돌리면 조용히 어긋난다 — `SET search_path` 는 없는 스키마를 **에러 없이 건너뛰므로** 무자격 객체가 전부 `public` 을 향한다. 실제로 `normalize_category_name` 이 `public` 에 생성됐고, `public.categories` 에 `year` 가 없어 `42703` 이 났다. 이제 15/16 첫머리의 `DO $guard$` 블록이 `exam` 부재 시 명확한 메시지로 중단시킨다
- [2026-08-30] **Supabase SQL Editor 에서 파일 머리의 `SET search_path` 는 뒤따르는 문에 적용된다고 믿으면 안 된다.** 문(statement)별로 다른 백엔드에 갈 수 있어, sql/16 의 무자격 `CREATE OR REPLACE FUNCTION normalize_category_name(...)` 이 **`public` 에 생성된 사례가 실제로 있었다**(`pg_proc` 조회 결과 `exam` 은 없고 `public` 만). ara-system 의 public 스키마를 오염시키는 경로다(다행히 동명 함수가 없어 덮어쓴 피해는 없었다). **DDL 은 반드시 스키마를 명시**할 것 — `CREATE ... FUNCTION exam.normalize_category_name`, `COMMENT ON FUNCTION exam....`. DO 블록 안은 블록 첫머리의 `SET LOCAL search_path = exam, public` 이 있어 안전하지만, 블록 **밖** 최상위 문은 각자 자격을 갖춰야 한다. 확인: [sql/diagnose_category_state.sql](sql/diagnose_category_state.sql) 의 0번 쿼리
- [2026-08-30] **sql/16(중복 병합)은 sql/15(year 추가)보다 먼저 적용한다.** 닭-달걀이 있다 — sql/15 는 `categories` 에 `year` 를 추가하면서 `idx_categories_natural_key` 를 재생성하는데, 표기 변형으로 갈라진 **중복 행이 남아 있으면 유니크 인덱스 생성이 실패**한다. 편집기가 파일 전체를 한 트랜잭션으로 묶으면 앞의 `ADD COLUMN` 까지 롤백되어 `year` 없는 상태로 되돌아가고, 그 상태에서 sql/16 을 돌리면 `42703 column "year" does not exist` 가 난다(실제 발생). 그래서 sql/16 은 **pre-15 스키마에서도 돌아가도록** 만들었다 — `categories.year`, `school_materials.year/grade`, `concept_sheets.school_name` 은 `information_schema` 로 존재를 확인해 없으면 파티션 키/SET 절에서 빼고 `EXECUTE format()` 으로 조립한다(pre-15 데이터는 그 값이 아예 없으니 그룹 결과가 동일하다). 권장 순서 **16 → 15 → (16 재실행은 no-op)**. 상태 확인은 [sql/diagnose_category_state.sql](sql/diagnose_category_state.sql)(읽기 전용)
- [2026-08-30] 카테고리 이름 정규화는 **쓰기 경로만으로는 증상이 사라지지 않는다**. `normalizeCategoryName` 은 새로 저장하는 값만 고치므로, 이미 저장된 변형은 [sql/16_migration_normalize_category_names.sql](sql/16_migration_normalize_category_names.sql) 을 실행해야 합쳐진다. 트리를 만드는 읽기 경로 6곳(`buildCategoryTree` 호출처)은 전부 **DB 원시 문자열로 `groupBy`** 하므로 마이그레이션 전에는 계속 갈라져 보인다
  - **개념지 목록만은 앱에서 자가 치유한다** — 필터가 UUID 가 아니라 **값 비교**라, 트리 키와 필터를 같은 정규화 키([src/lib/concept-category.ts](src/lib/concept-category.ts) 의 `conceptCategoryKey`)로 맞추면 표기가 달라도 한 노드로 합쳐지고 **양쪽 표기의 개념지가 모두 보인다**. 트리만 합치고 필터는 원시 비교로 두면 합쳐진 노드를 눌렀을 때 한쪽이 조용히 사라지므로 **둘은 반드시 같은 키 함수를 써야 한다**
  - ⚠️ **`categories` 기반 트리(단어 관리·시험지 생성·시험 기록·단어 이동)에는 같은 수법을 쓰면 안 된다** — 선택이 `category.id`(실제 UUID) 기준이라, 표시만 합치면 대표 UUID 한 쪽의 단어만 필터에 걸리고 나머지 행의 단어가 가려진다(CHANGELOG 0.1.11 의 "표시만 합치면 조용히 가려진다"). 그쪽은 sql/16 의 **실제 행 병합**이 유일한 해법이다
- [2026-08-22] 카테고리 이름(출판사·대단원·소단원·학교·프린트명)은 **반드시 `normalizeCategoryName`** ([src/lib/category-name.ts](src/lib/category-name.ts)) 을 거쳐 저장할 것. 트리 노드는 이름 문자열 완전 일치로 묶이는데([src/lib/category-tree.ts](src/lib/category-tree.ts) 의 `groupBy`), DB 제약(`publishers UNIQUE(name, level)`, `idx_categories_natural_key`)이 전부 **바이트 비교**라 눈에 똑같은 `천재(정호웅)` / `천재 (정호웅)` 이 둘 다 저장되고 폴더가 두 개로 갈라진다. 규칙: 폭 없는 문자(U+200B/U+FEFF) 제거 → NFC → 전각 괄호`（）`→ASCII → 공백류 축약·trim → 괄호 주변 공백 제거. **폭 없는 문자 제거가 NFC 보다 먼저**여야 멱등이다(`ᄀ<ZWSP>ᅡ` 는 NFC 를 먼저 돌리면 ZWSP 를 뗀 뒤에도 자모가 분리된 채 남는다). 이 규칙은 [sql/16_migration_normalize_category_names.sql](sql/16_migration_normalize_category_names.sql) 의 `normalize_category_name()` 과 **1:1 미러**다 — 한쪽만 바꾸면 앱이 저장한 값과 DB 가 병합한 값이 갈라져 중복이 다시 생긴다. 새 이름 필드를 추가하면 쓰기 경로에 정규화를 넣을 것(마스터 CRUD 5개 / `ensureCategoryId` / 개념지 저장 payload)
- [2026-08-22] 중복 `categories` 를 병합할 때 `words` 의 `UNIQUE(category_id, word)` 선처리는 **"dup vs canonical" 만 비교하면 안 된다**(sql/09 의 원래 형태). 한 그룹에 dup 이 둘 이상이고 그 둘이 canonical 에 없는 같은 단어를 갖고 있으면, repoint 에서 둘 다 canonical 로 옮겨가 유니크 제약이 터지고 마이그레이션 전체가 롤백된다. sql/16 처럼 **canonical + 모든 dup 을 한 묶음**으로 보고 `(canonical_id, word)` 당 한 행만 남겨야 한다. 표기 변형은 몇 달에 걸쳐 쌓이므로 3개 이상 묶이는 그룹이 기본이다
- [2026-08-22] sql/16 은 **categories/concept_sheets 를 마스터보다 먼저** 정규화한다. 순서를 뒤집으면 마스터 rename 이 `sync_*_name` 트리거를 발화시키고, 그 `UPDATE categories SET publisher = NEW.name WHERE publisher = OLD.name` 이 이미 존재하는 정규형 행과 `idx_categories_natural_key` 충돌을 일으켜 블록 전체가 롤백된다. 먼저 정규화해 두면 트리거 WHERE 가 0행을 잡는 no-op 이 된다. 또한 `NULL school_name → ''` 를 병합보다 **먼저** 하면 안 된다 — 인덱스가 `NULLS NOT DISTINCT` 라 NULL 행과 '' 행이 공존할 때 충돌한다(sql/09 에선 인덱스 생성 전이라 무해했다)
- [2026-08-16] 외부지문 개념지를 ara-system 성적에 등록할 때 **`publisher` 슬롯에 학교명**을 보낸다([sync-concept-to-grades/route.ts](src/app/api/sync-concept-to-grades/route.ts)). 외부지문은 출판사가 항상 빈 값이라 그대로 두면 학교가 무엇이든 전부 **'기타' 그룹 한 곳**에 뭉친다. 수신부 `register_concept_exam`(ara-system mig257)에서 `p_publisher` 는 그룹 폴더명, `p_grade + ' ' + p_semester` 는 시리즈 폴더명으로 쓰이는 **표시용 문자열**일 뿐이고 학교급 라우팅은 별도 `p_division` 이 담당하므로 슬롯 재사용이 안전하다. **년도는 시리즈에 넣지 않는다** — ara-system 이 회차 라벨(`YY-NN`)을 연도별로 다시 세므로(mig343) 교과서 시리즈도 년도 없이 해마다 재사용한다. ⚠️ 이 매핑을 나중에 바꾸면 시리즈 코드(`md5(pub_key|series_name)`)가 달라져 **이미 등록된 회차가 다른 폴더로 이동하고 `round_number` 가 재계산**된다. 도입 시점에 외부지문 개념지가 0건이라 지금 형태로 고정한 것
- [2026-08-15] `categories` 자연키 유니크 인덱스에 **`year` 가 포함**된다(`level, year, grade, publisher, semester, chapter, sub_chapter, school_name`). `ensureCategoryId` 의 `upsert({ onConflict })` 는 이 목록과 **글자 그대로** 일치해야 하므로 [src/lib/words-save.ts](src/lib/words-save.ts) 의 `CATEGORY_NATURAL_KEY` 와 [sql/15_migration_external_year_grade.sql](sql/15_migration_external_year_grade.sql)·[sql/01_schema.sql](sql/01_schema.sql) 미러를 항상 같이 바꿀 것. **sql/15 를 앱 배포보다 먼저 적용**해야 한다 — 순서가 뒤바뀌면 upsert 가 실패해 단어 저장이 전부 막힌다. 신규 부트스트랩 범위는 이제 **01~16**
- [2026-08-15] **(2026-09-14 부분 무효 — 앱 레벨 fallback 은 삭제됐다. 되살린다면 이 규칙을 함께 되살릴 것)** 외부지문 rename 동기화(`sync_school_material_name`)는 `year`/`grade` 로 좁혀야 한다. 없으면 2026 중2 프린트 이름을 바꿀 때 다른 년도·학년의 동명 카테고리까지 함께 바뀐다(년도·학년 도입 전에는 구분 자체가 없어 무해했다). 앱 레벨 fallback([src/lib/category-master/school-materials.ts](src/lib/category-master/school-materials.ts))도 같은 `.eq()` 체인을 가져야 한다
- [2026-08-15] 마스터 rename 의 **앱 레벨 fallback 은 `categories` 와 `concept_sheets` 를 둘 다** 갱신해야 한다. 예전엔 `categories` 만 갱신해서, DB 트리거가 안 깔린 환경에서는 단어지만 새 이름이 되고 개념지는 옛 이름으로 남았다 — 단어지가 멀쩡해 보여 트리거 누락을 눈치챌 수 없는 형태였다(CHANGELOG 0.1.2 에서 한 번 고쳤던 증상의 재발 경로). 컬럼명 대응 주의: `chapter`↔`unit`, `sub_chapter`↔`subunit`
- [2026-08-15] 개념지 저장 검증은 **level 인지형**이어야 한다. 외부지문은 `publisher` 가 항상 빈 값이라, `grade` + `publisher` 를 무조건 요구하면 외부지문을 고를 수 있게 해도 저장이 막힌다([src/hooks/useConceptSheetEditor.ts](src/hooks/useConceptSheetEditor.ts) 의 `missingCategory`). 함께 `unit` 도 필수로 넣었다 — 예전엔 UI 라벨은 `unit` 유무로 판단하는데 검증은 `grade`/`publisher` 만 봐서 `unit=''` 저장이 통과했고, 인쇄 제목이 `"2026  국어 "` 처럼 공백만 남았다([ExamSheetRenderer.tsx](src/components/exam-builder/ExamSheetRenderer.tsx) 의 `filter(Boolean)` 이 템플릿 리터럴 앞부분을 못 걸렀다)
- [2026-09-08] **(위 2026-08-15 헤더 항목은 무효)** 상단 가로 헤더는 좌측 사이드바로 대체됐고 `Header.tsx` 는 삭제됐다([src/components/layout/AppShell.tsx](src/components/layout/AppShell.tsx) + [Sidebar.tsx](src/components/layout/Sidebar.tsx)). 메뉴가 세로로 쌓이므로 더 이상 '메뉴를 추가하면 헤더가 2줄이 된다' 는 제약이 없다. **본문 위에 남는 상단 바 높이는 CSS 변수 `--app-topbar-h` 가 단일 출처**다([globals.css](src/app/globals.css) — `lg` 이상 0px, 그 미만 56px). sticky 툴바 오프셋과 개념지 편집기의 `100vh` 계산이 전부 이 변수를 쓰니 **`top-16` / `64px` 을 다시 하드코딩하지 말 것** — 데스크톱에선 위에 아무것도 없어 그만큼 빈 띠가 생기고, 모바일에선 상단 바에 가린다. 사이드바·모바일 상단 바는 `data-no-print` 라 인쇄에서 빠진다.
  - 메뉴 활성 판정은 [nav-items.ts](src/components/layout/nav-items.ts) 의 `isNavItemActive` 하나뿐이다. **세그먼트 경계**로 비교하므로(`/words` 가 `/wordsomething` 을 잡지 않는다) 새 메뉴를 추가할 때 순수 `startsWith` 로 되돌리지 말 것. 목록에서만 들어가는 경로(`/exam/view`·`/exam/create` → `단어 시험지`)는 `extraPrefixes` 로 부모 메뉴에 붙인다 — [2026-09-08] 시험지 생성은 메뉴 항목을 없애고 `/exam/history` 화면의 버튼으로만 들어가게 했으므로, 생성 화면의 활성 표시도 목록 항목이 대신한다
- [2026-08-15] ~~헤더의 데스크톱 임계값은 `md:` 가 아니라 `lg:`~~ (2026-09-08 사이드바 전환으로 무효 — 위 항목 참고). 헤더([src/components/layout/Header.tsx](src/components/layout/Header.tsx))의 데스크톱 임계값은 `md:` 가 아니라 **`lg:`** 이고 브랜드 텍스트는 **`xl:` 부터**만 보인다. 관리자 계정은 메뉴가 7개(≈724px)라 로고·로그아웃까지 더하면 `lg`(콘텐츠 960px)에 겨우 들어간다. `flex-wrap` 이 없어 폭이 모자라면 flex 아이템이 줄바꿈되는 게 아니라 **각 아이템 내부 텍스트가 접혀** `h-16` 을 뚫고 헤더가 2줄이 된다 — 메뉴를 추가하려면 이 여유를 먼저 계산할 것. 로그인 이메일은 데스크톱에서 제거했고 모바일 메뉴에만 남아 있다
- [2026-08-15] 인쇄 문서에서 **측정과 렌더가 반드시 같은 폭·같은 래퍼**를 써야 한다. `A4Document` 는 숨김 컨테이너(`.a4-measure`)에 블록을 컬럼 폭으로 렌더해 높이를 재고 같은 값으로 낱장에 배치한다 — 낱장 쪽에 padding/width 를 덧붙이면 줄바꿈이 달라져 페이지 수가 어긋난다. 낱장 크기는 `src/lib/print/constants.ts` 가 단일 출처이고 `A4Sheet` 가 인라인 style 로 적용하므로 **CSS 에 px 를 복사하지 말 것**. 측정 컨테이너는 `visibility: hidden`(`display:none` 이면 높이가 0). 인쇄 대화상자에서 `.a4-measure` 가 `display:none` 이 되면 높이가 0 으로 읽히는데, 훅이 `footerHeight <= 0` 이면 기존 배치를 유지하도록 방어한다. 줄무늬는 `nth-child` 대신 인덱스 기반 클래스(`q-row--band`/`wb-row--band`/`mc-answer-grid--band`, 표는 `tr[data-row-even]`)를 쓴다 — 페이지·조각이 갈리면 nth-child 가 리셋되기 때문
- [2026-03-09] `@supabase/auth-helpers-nextjs`는 deprecated됨. 현재 직접 `@supabase/supabase-js` 사용 중
- [2026-03-09] exam_words 테이블은 단어 스냅샷이므로 원본 단어를 수정해도 기존 시험지에는 영향 없음
- [2026-04-14] `audit_log` 에는 INSERT 정책을 만들지 말 것. SECURITY DEFINER 트리거(postgres 소유, BYPASSRLS)가 RLS 를 우회하여 쓰기를 수행하므로 정책은 불필요하며, 어떤 열린 INSERT 정책이든 authenticated 클라이언트에게 직접 쓰기를 허용해 감사 로그 오염 경로가 된다 (과거 `"System can insert audit_log"` 정책 사례 참고)
- [2026-04-15] SECURITY DEFINER 함수는 반드시 `SET search_path = public, pg_temp` 를 지정할 것. 지정하지 않으면 호출 세션의 search_path 조작으로 함수 본문의 무자격 객체 참조(`audit_log` 등)가 엉뚱한 스키마로 해석되어 감사 로그 우회/오염이 가능해진다. `pg_temp` 는 반드시 마지막에 두어 임시 객체 섀도잉을 차단한다 (감사 트리거 함수 9개 참고 — [sql/11_fix_audit_functions_search_path.sql](sql/11_fix_audit_functions_search_path.sql))
- [2026-04-15] `exams` / `categories` / `concept_sheets` 의 `user_id` 는 BEFORE INSERT 트리거 `enforce_user_id_from_auth` 가 `auth.uid()` 로 강제 채우고, BEFORE UPDATE 트리거 `lock_user_id_on_update` 가 변경을 무효화한다. 클라이언트에서 `user_id` 를 보내지 말 것 — 보내도 무시되며, 이전엔 클라이언트가 임의 UUID 로 생성자 위조가 가능했다. RLS 는 여전히 `authenticated` 만 체크하는 공유 정책이므로 attribution 은 감사/필터용 메타로만 의미를 가진다. RPC `create_exam_with_words` 시그니처에서도 `p_user_id` 가 제거됨. `ensureCategoryId` 는 더 이상 `userId` 를 받지 않으며 매칭에서도 제외해 학원 내 동일 단원이 사용자별로 중복 생성되지 않는다. **[2026-06-16 정정] 이 두 트리거의 정식 정의는 [sql/13_migration_enforce_user_id.sql](sql/13_migration_enforce_user_id.sql) 다** — 그동안 정의가 `sql/archive/migration_enforce_user_id.sql` 에만 있어 번호 마이그레이션(01~12)만 적용한 신규 환경에서는 트리거가 없어 `user_id`(NOT NULL) 가 채워지지 않아 모든 생성이 깨지고 보안 전제도 빠지는 문제가 있었다. 신규 부트스트랩 범위는 이제 01~13 이다(13 은 멱등이라 기존 운영 DB 에 재적용해도 안전)
- [2026-04-15] 재시험 차수(`exams.retake_number`) 는 클라이언트에서 계산하지 말 것 — 두 사용자가 동시에 누르면 같은 `(parent_exam_id, retake_number)` 가 만들어진다. `create_exam_with_words` RPC 가 `p_parent_exam_id IS NOT NULL` 일 때 `pg_advisory_xact_lock(hashtext(parent::text))` 으로 직렬화 후 `MAX(retake_number)+1` 을 계산하고, 제목 접미사 `(재시험 N차)` 도 서버가 `format()` 으로 조립한다. 따라서 재시험 호출에서는 (a) `p_title` 에 **원본 제목만** 넘기고, (b) `p_retake_number` 를 보내지 말고, (c) 토스트/UI 는 RPC 반환 후 새 행을 다시 조회해 서버 차수를 사용해야 한다. 최후 안전망으로 `idx_exams_parent_retake_unique` (`(parent_exam_id, retake_number) WHERE parent_exam_id IS NOT NULL`) 부분 유일 인덱스가 RPC 우회 INSERT 까지 차단한다 — [sql/archive/migration_retake_atomic.sql](sql/archive/migration_retake_atomic.sql) 참조
- [2026-04-22] 객관식 시험 선지 셔플에 glibc LCG `(s * 1103515245 + 12345) & 0x7fffffff` 를 직접 쓰지 말 것. 하위 비트 랜덤성이 나빠 `% 5` 같은 작은 modulo 에서 정답이 4·5번으로만 쏠렸다(실측 16/20, 4/20). 추가로 시드를 UUID char code 합으로 계산하면 값 범위가 2300~2900 에 몰려 편향이 증폭된다. `src/lib/exam-choices.ts` 의 **mulberry32 + FNV-1a** 조합을 사용할 것 — 1000회×20문항 시뮬레이션에서 각 위치 ~20% 균등. [MultipleChoiceView.tsx](src/components/exam/MultipleChoiceView.tsx), [MultipleChoiceAnswerView.tsx](src/components/exam/MultipleChoiceAnswerView.tsx) 는 반드시 이 공유 모듈의 `generateChoices` / `getCorrectLabel` 을 써야 시험지와 답안지 매핑이 일치한다 — 셔플 로직을 한쪽만 바꾸면 답지가 틀어진다
- [2026-05-26] 배열 셔플에 `arr.sort(() => Math.random() - 0.5)` 를 쓰지 말 것 — 비교 함수가 비일관적이라 permutation 분포가 치우친다. 문제 순서 셔플(비결정)에는 [src/lib/shuffle.ts](src/lib/shuffle.ts) 의 `shuffle` (Fisher-Yates) 를, 시험지/답안지가 동일해야 하는 결정론적 셔플에는 `exam-choices.ts` 의 `seededShuffle` 를 쓴다
- [2026-05-26] `exam_words` 는 더 이상 클라이언트가 직접 INSERT/UPDATE/DELETE 할 수 없다. RLS 를 SELECT 전용으로 잠갔고(이전엔 `FOR ALL` 공유라 누구나 남의 시험지 단어를 변조/삭제 가능했고 감사도 없었다), 유일한 쓰기 경로인 `create_exam_with_words` 는 **SECURITY DEFINER** 로 바꿔 잠긴 RLS 를 우회해 INSERT 한다(`auth.uid()` 는 DEFINER 에서도 요청 JWT 를 읽어 attribution 유지). **DEFINER 라 테이블 RLS(도메인 조건)를 우회하므로 함수 첫머리에서 `public.is_allowed_domain()` 을 직접 검사**한다. RPC 는 `total_questions`/`pass_count`/`word_ids` 를 서버에서 재계산하고, **신규 생성**(`p_parent_exam_id IS NULL`)은 word·meaning 을 canonical `words` 에서 재조립 + 전 word_id 실재 검증해 내용 위조를 막는다. [2026-06-17] **`category_ids` 도 클라이언트 입력(`p_category_ids`)을 버리고 실제 포함 단어들의 `words.category_id`(NOT NULL) DISTINCT 집합으로 서버 재계산**해 출처/필터 라벨 위조를 차단한다(01_schema.sql 미러 + sql/10 둘 다 갱신 — 재적용 필요). **재시험**(`p_parent_exam_id IS NOT NULL`)은 클라이언트 입력(p_words/p_category_ids/메타)을 전혀 신뢰하지 않고 **서버가 부모 exam·exam_words 를 직접 읽어 재조립**(서버 셔플 순서)한다 — 직접 RPC 호출로 가짜 "재시험 N차" 를 만드는 것을 차단. 부모 스냅샷 복사라 삭제된 단어도 보존된다. RPC 클라이언트 호출 계약(원본 제목만, p_retake_number 미전달)은 그대로 — [sql/10_migration_lock_exam_words.sql](sql/10_migration_lock_exam_words.sql). DEFINER 가 RLS 를 우회하려면 함수 owner 가 테이블 owner/BYPASSRLS 여야 하므로 반드시 postgres 로 적용할 것
- [2026-05-26] `create_exam_with_words` 의 정식 정의는 [sql/10_migration_lock_exam_words.sql](sql/10_migration_lock_exam_words.sql) **한 곳**에서만 관리한다(+ 01_schema.sql 미러). 과거 `archive/migration_enforce_user_id.sql` / `archive/migration_retake_atomic.sql` / `archive/migration_exam_rpc.sql` 이 각자 함수를 재정의하고 `03_shared_exams_audit_log.sql` 이 `exam_words FOR ALL` 정책을 재생성해, 실행 순서에 따라 advisory lock·DEFINER·검증·쓰기잠금·도메인조건이 사라지는 퇴행이 있었다(전부 정의/정책을 제거하고 포인터 주석으로 대체함). 정책 소유권: 01_schema.sql(기준) + 10_migration_lock_exam_words.sql(exam_words SELECT 전용) + 08_migration_domain_restriction.sql(도메인). SQL 적용 순서: [2026-06-16] **파일명 번호(01~12) 순서대로 적용한다** — 번호가 곧 적용 순서다. `10_migration_lock_exam_words.sql` 이 마지막(exam_words 잠금·canonical RPC·도메인 가드 최종 승자)이고 `11`/`12` 는 감사 함수 패치다. `sql/archive/` 의 파일들(`00_apply...`, `migration_retake*`, `migration_enforce_user_id`, `migration_exam_rpc`)은 과거/대체된 마이그레이션으로 **신규 적용 대상이 아니다**(정의가 포인터 주석으로 무력화됨, 이력 보존용). 클라이언트 코드보다 `09_migration_categories_unique.sql`(upsert onConflict 의존)·`08_migration_domain_restriction.sql` 을 먼저 적용할 것. `01_schema.sql` 은 도메인 조건·categories 유니크 인덱스·is_allowed_domain 까지 포함한 **핵심 테이블(words/exams/exam_words/categories + 마스터)** 의 완전한 기준 상태다. 단, [2026-06-16 수정] `concept_sheets` 와 `audit_log` 테이블 DDL 은 01_schema.sql 에 없으므로(각각 [sql/02_create_concept_sheets.sql](sql/02_create_concept_sheets.sql), [sql/03_shared_exams_audit_log.sql](sql/03_shared_exams_audit_log.sql) 에만 존재) **01_schema.sql 단독으로는 신규 환경을 완전히 부트스트랩하지 못한다**. 신규 부트스트랩은 `sql/` 의 **번호 순서(01→12)대로 전부 적용**한다(05_words_audit·06_words_concurrency·07_migration_sync_concept_sheets·11·12 포함). 01_schema.sql 의 rename-sync 트리거가 `concept_sheets` 를 UPDATE 하지만, plpgsql 본문은 런타임에만 검증되므로 테이블이 나중에 생성돼도 트리거 생성 자체는 실패하지 않는다
- [2026-05-26] 도메인 제한(@araeducation.co.kr)은 RLS 헬퍼 `public.is_allowed_domain()` 으로 데이터 계층에서 강제한다([sql/08_migration_domain_restriction.sql](sql/08_migration_domain_restriction.sql)). 세션이 localStorage 에 저장되어(@supabase/ssr 미사용) 서버 미들웨어로 세션을 못 읽기 때문. 모든 공유 테이블 정책의 USING/WITH CHECK 에 `AND public.is_allowed_domain()` 가 붙어 있으니 새 테이블/정책 추가 시 함께 넣을 것. 클라이언트 가드(auth-context)와 OAuth 콜백 검사는 보조(UX)이며 권위는 RLS 다. 도메인 문자열은 [src/lib/constants.ts](src/lib/constants.ts) 의 `ALLOWED_EMAIL_DOMAIN` / `isAllowedEmailDomain` 로 통일
- [2026-05-26] `categories` 는 자연키(`level, grade, publisher, semester, chapter, sub_chapter, school_name`)에 유니크 인덱스 `idx_categories_natural_key`(NULLS NOT DISTINCT)가 있다. `ensureCategoryId` 는 SELECT→INSERT 대신 `upsert({ onConflict: ... })` 단일 호출로 동시 저장 중복을 막는다 — onConflict 컬럼 목록(`CATEGORY_NATURAL_KEY`)은 인덱스와 정확히 일치해야 한다. 인덱스 없이 코드만 배포하면 upsert 가 실패하므로 [sql/09_migration_categories_unique.sql](sql/09_migration_categories_unique.sql) 를 먼저 적용할 것
- [2026-06-16] 마스터 이름 rename 동기화 트리거(`sync_publisher_name`/`sync_major_chapter_name`/`sync_sub_chapter_name`)는 `categories` 와 `concept_sheets` 를 **둘 다** 갱신해야 한다. concept_sheets 는 컬럼명이 다르다: categories `chapter`↔concept_sheets `unit`, `sub_chapter`↔`subunit`(publisher/level/grade/semester 는 동일). 한쪽만 갱신하면 단어지와 개념지에 같은 출판사가 두 표기로 갈라진다. concept_sheets 는 자연키 유니크 제약이 없어 같은 표기로 수렴해도 에러 없이 행이 합쳐져 보인다(categories 는 유니크 인덱스가 있어 두 출판사를 같은 이름으로 통일하면 충돌 가능 — 그쪽은 [sql/09_migration_categories_unique.sql](sql/09_migration_categories_unique.sql) 의 병합으로 처리). 외부지문(schools/school_materials) 트리거는 concept_sheets 대상 아님(중등/고등만). 정식 정의는 01_schema.sql + [sql/07_migration_sync_concept_sheets.sql](sql/07_migration_sync_concept_sheets.sql) 미러
- [2026-06-21] `exams` 도 직접 INSERT/UPDATE 를 막았다(이전엔 `FOR ALL` 공유라 누구나 `create_exam_with_words` RPC 를 우회해 `supabase.from('exams').insert(...)` 로 exam_words 와 매칭 안 되는 가짜 시험지·메타데이터 위조 INSERT, 또는 남의 시험지 제목/합격선/차수를 UPDATE 변조할 수 있었다 — 감사 트리거는 남지만 변조 자체는 못 막았다). 정책을 **SELECT/DELETE 전용**으로 바꾸고, 생성은 오직 SECURITY DEFINER `create_exam_with_words` 로만 한다(DEFINER owner=postgres 라 RLS 우회 INSERT, INSERT 정책 불필요). 삭제는 클라이언트 직접 + `audit_exam_delete` 감사 유지, 수정(UPDATE)은 앱 경로가 없어 기본 deny(향후 편집 기능은 전용 DEFINER RPC 로 추가). 정식 정의: [sql/14_migration_lock_exams_writes.sql](sql/14_migration_lock_exams_writes.sql) + 01_schema.sql·08_migration_domain_restriction.sql 미러(셋 다 SELECT/DELETE — 한 곳만 바꾸면 적용 순서상 `FOR ALL` 부활 위험). **신규 부트스트랩 범위는 이제 01~14**. exam_words 잠금(sql/10)과 같은 결의 구멍이었다. 클라이언트는 exams 를 SELECT/DELETE + RPC 로만 쓰므로 기존 앱과 호환(직접 INSERT/UPDATE 코드 없음)
- [2026-06-21] `create_exam_with_words` **신규 생성** 분기에 검증 3종 추가 — (1) **중복 word_id 차단**(`cardinality(v_word_ids) <> DISTINCT count`; exam_words 에 `(exam_id, word_id)` 유니크 제약이 없어 직접 RPC 호출로 같은 단어를 5번 넣으면 객관식 선지가 5개 미만으로 무너졌다), (2) **표시 문자열 distinct ≥ 5**(`COUNT(DISTINCT words.word)`; [exam-choices.ts](src/lib/exam-choices.ts) 가 `word` 문자열로 선지 중복을 제거하므로 서로 다른 word_id 라도 표기가 같으면 선지 부족), (3) **order_index 서버 canonicalize** — 클라이언트 `order_index` 를 버리고 `jsonb_array_elements(p_words) WITH ORDINALITY` 의 배열 위치(`ord-1`)를 `v_word_ids` 정렬과 `exam_words.order_index` 양쪽에 쓴다(음수/중복/희소 order_index 로 출제 순서를 망가뜨리는 우회 차단). [sql/10_migration_lock_exam_words.sql](sql/10_migration_lock_exam_words.sql) + 01_schema.sql 미러 — **재적용 필요**
- [2026-06-21] OAuth redirect_uri·콜백 리다이렉트 origin 을 Host 헤더가 아니라 환경변수 **`APP_ORIGIN`** 으로 고정한다([src/lib/app-origin.ts](src/lib/app-origin.ts) 의 `resolveAppOrigin`). Host 조작으로 redirect_uri 가 오염되거나 매직링크 `token_hash` 가 공격자 호스트로 새는 것을 막는다. **프로덕션 fail-closed**: `APP_ORIGIN` 미설정/오설정(스킴 누락 등)이면 throw → 두 OAuth 라우트(`naver-works/route.ts`, `callback/route.ts`)가 제어된 500 반환(내부 설정명 비노출, try/catch 로 감쌈). 따라서 **배포 환경에 `APP_ORIGIN` 을 반드시 설정**해야 로그인이 동작한다(로컬은 미설정 시 요청 origin 폴백). [docs/env.example](docs/env.example) 참고
- [2026-06-21] CSP `style-src`/`font-src` 에 `https://cdn.jsdelivr.net` 을 허용한다([next.config.ts](next.config.ts)). [globals.css](src/app/globals.css) 가 Pretendard/GmarketSans 폰트 스타일시트를 jsdelivr 에서 `@import` 하고 그 시트가 같은 CDN 의 woff2 를 불러오는데, `'self'` 만 두면 프로덕션에서 폰트가 차단된다. 폰트 import 호스트를 바꾸면 CSP 도 같이 갱신할 것
- [2026-06-21] `concept_sheets` 저장은 공유 테이블이라 last-write-wins 였다 → `useConceptSheetEditor` 가 로드 시 `updated_at` 을 기억하고 저장 시 `.eq('updated_at', loadedUpdatedAt)` 로 **낙관적 동시성 제어**한다(0행 갱신=충돌이면 새로고침 안내). 저장 성공 시 반환된 `updated_at` 으로 버전을 갱신해 연속 저장이 가능하다. `updated_at` 은 `concept_sheets_updated_at` 트리거가 자동 갱신([sql/02_create_concept_sheets.sql](sql/02_create_concept_sheets.sql))